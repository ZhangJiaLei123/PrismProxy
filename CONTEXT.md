# PrismProxy 项目记忆（CONTEXT）

> 跨任务协同的关键记忆，只记非显而易见的约定、架构决策与踩坑。开发进度不记在这里（见 doc/开发进度.md）。
> 本文件 2026-09-09 合并自 doc/项目记忆.md（该文件现为指针存根）。

## 项目概览
- PrismProxy（棱镜）：Windows 桌面级 HTTP(S) 调试代理（MITM 解密 + 抓包看包），单文件 exe；独立开源工具（`https://github.com/ZhangJiaLei123/PrismProxy.git`），定位/文档/commit/注释**不引入任何具体业务方名称**。
- 技术栈：Go 1.27（代理引擎，标准库为主 + brotli/zstd + modernc.org/sqlite，核心无 CGO）+ Vue 3 + TS + Naive UI + Pinia + Vite 前端；Wails v2 桥接（WebView2）。
- 端口：代理默认 9090，控制 API（ctlapi）9595。
- 前端目录：`frontend/src`（pages/、components/、stores/、composables/、review/）；Wails 绑定在 `frontend/wailsjs/`，前端引用路径为 `../wailsjs/...`（pages 下）或 `../../wailsjs/...`（components 下）。
- 配置全部走**便携模式**：exe 同级 `config/`（settings.json、ca/、projects/<id>/、domains/、ctl-endpoint.json）；旧 %APPDATA%\PrismProxy 配置一次性自动搬迁不删旧文件。wails dev 下 exe 在 build/bin，配置落在 build/bin/config。

## 环境与网络备忘
- PowerShell **5.1**：无 `ForEach-Object -Parallel`（用 Start-Job）；`curl` 是别名必须 `curl.exe`，https 加 `--ssl-no-revoke`（schannel 强查吊销，私有 CA 无 CRL 即使装根也会 TLS 失败；浏览器/.NET/openssl 不硬查）。
- GitHub：`github.com` 可达但间歇超时（出口网络问题，经代理直连模式同构失败可证非代理 bug）；`raw.githubusercontent.com` 被 DNS 污染，用 **jsDelivr** `https://cdn.jsdelivr.net/gh/<user>/<repo>@<branch>/<path>`（另有 fastly./gcore. 备用，缓存延迟数小时）。
- Go 模块：modernc.org/sqlite 依赖树必须 `$env:GOPROXY='https://goproxy.cn,direct'`。
- **PowerShell 编码红线**：绝不用 `Set-Content`/`-replace|Out-File` 重写含中文源码（默认 GBK 毁 UTF-8，gofmt 报 illegal UTF-8）；手工写 JSON 配置用 `[System.IO.File]::WriteAllText` + `UTF8Encoding($false)`——**PS 5.1 `Set-Content -Encoding UTF8` 带 BOM**，settings.json 带 BOM 会 json.Unmarshal 失败静默回退 Default。改源码一律用编辑工具。
- `go mod tidy` 会移除"已声明但未 import"的依赖——先写 import 再 tidy。
- 杀软首次扫描 test exe 报 Access denied，重跑即过。

## 代理引擎关键决策（internal/proxy、internal/capture）
1. **ForceAttemptHTTP2**：为记录上游真实证书链需自定义 TLSClientConfig，Go 一旦自定义就保守禁用 h2，必须显式 `ForceAttemptHTTP2: true`。
2. **流式捕获 tee 语义**：边转发边捕获，Flow 状态机 pending/streaming/done/error；WebSocket/101 升级 http.Transport 无法接管，必须 Hijack。
3. **默认绑 127.0.0.1**：不开放 LAN 匿名代理；局域网绑定是显式开启项。
4. **PID 进程映射**：`GetExtendedTcpTable` 同时查 AF_INET+AF_INET6；accept 即时查不缓存；权限不足取路径失败→降级显示+提权提示。M1 用纯 syscall+LazyDLL 直调 iphlpapi.dll（不用 golang.org/x/sys，零外部依赖）。
5. **解压**：gzip/deflate（标准库）+ br（andybalholm/brotli）+ zstd（klauspost/compress，项目已有包级 Encoder/Decoder，EncodeAll/DecodeAll 并发安全）。透传 Accept-Encoding 后 CDN 会回 zstd。展示层 `capture.DecodeBody` 在 GetFlowBody 时解压，不在捕获路径；Composer 则 DisableCompression 保真透传。
6. **CA**：私钥 ACL 限当前用户；叶子证书 ECDSA P-256、有效期 ≤1 年、LRU + singleflight。
7. **内存**：环形缓冲默认 2000 条/256MB（maxFlows/maxBodyMB 可配），按 body 实际字节统计，body 2MB 截断；事件批量节流 ~50ms + `flow:evict` 淘汰通知。Flow ID 格式 `时间戳毫秒-序号`。
8. **逐跳首部**：proxy `removeHopHeaders`（九项）；旁路记录（compose/未来回放/CLI 造流）必须照做两份清单：① PID 映射填 ProcessInfo；② 逐跳首部剥离（响应侧保留 Content-Length，请求侧含 Content-Length 让 Transport 重算）。
9. store `Add` 按 ID upsert（原位更新发 "update"，否则值拷贝追加发 "new"，覆盖先发 "evict"），订阅锁外回调。

## MITM 协议红线（h2→h1 回写，internal/proxy/mitm.go，2026-09-07 修复）
客户端侧 TLS 恒只协商 http/1.1（NextProtos 只有它，兼容老 Android okhttp/xutils/HttpURLConnection），但上游 ForceAttemptHTTP2 常回 h2 响应。**弃用 `resp.Write`，走 `writeH1Response` 手工组帧**：
1. 状态行恒写 `HTTP/1.1 <code> <reason>`——`resp.Write` 会穿透写出非法 `HTTP/2.0 200 OK`，老 okhttp 抛 ProtocolException 判"无网络"；上游真实协议仍记 `flow.Response.Proto` 供展示（两处分离）。
2. 上游无 Content-Length（h2 常态）一律转 h1 `Transfer-Encoding: chunked`，有 CL 显式写 CL。
3. 状态行+首部写完**立即 flush**；body 32KB 循环读**每块都 flush**（否则 SSE/IM 长连接全积在 4KB bufio 缓冲，客户端连头都收不到；且 goroutine 阻塞使 flow 停 streaming 不落库——"flows 里没有 SSE 流"不代表 App 没发）。
4. HEAD/204/304 剥 Transfer-Encoding/Content-Length 不发体。明文 HTTP 路径走标准 ResponseWriter 无此问题。
- 客户端 TLS 握手失败时 `addBypass(host)`，该 host 后续 CONNECT 直接盲隧道；**全量降级时 UI 无任何报错**（优雅降级）。症状=列表几乎全 CONNECT、明文 HTTP 正常→优先查 CA 指纹是否一致。

## 系统代理（internal/sysproxy）
- 注册表真实路径 `HKCU\Software\Microsoft\Windows\CurrentVersion\Internet Settings`（方案里 `HKCU\Internet Settings` 是简写，reg query 必须全路径；PS HKCU: PSDrive 偶发异常，用 .NET `[Microsoft.Win32.Registry]::CurrentUser.OpenSubKey(...)`）；`InternetSetOption` 广播 39+37；备份落 `config/sysproxy-backup.json` + 崩溃自愈 SelfHeal。
- **ProxyOverride 绕过语义**：分号分隔；**裸域名同时匹配自身与全部子域**；`<-loopback>` 绕过回环。绕过发生在客户端侧（WinINET/Chromium 直接拨号，代理根本收不到 CONNECT），比代理内解密黑名单更靠前——是"代理死了注册表残留"断网的系统级免疫。接管时**合并去重**内置绕过列表+用户原值（前缀保留 `<-loopback>`）绝不覆盖，恢复时精确还原备份。
- 已内置开发机绕过列表（trae/字节系 20 个裸域，备份文件 proxyoverride-backup.txt 留原值）；Trae CN.exe 自家网络栈不尊重 ProxyOverride 属预期。
- **退出清理三场景**：①关窗口/Alt+F4→Wails OnShutdown；②**系统关机/注销→Wails v2.15 不处理 WM_QUERYENDSESSION/WM_ENDSESSION（源码核实，OnShutdown 不触发）**，自建隐藏普通顶层窗口接收（session_windows.go：独立 goroutine+LockOSThread+RegisterClassExW/CreateWindowExW/GetMessage；**HWND_MESSAGE message-only 窗口收不到 ENDSESSION 广播**），WM_ENDSESSION(wParam=TRUE) 同步 `App.onSessionEnd()`（还原系统代理+同步清 ADB 设备代理，adbSessionTimeout=8s）；非 Windows session_other.go 空桩。③taskkill /F/崩溃→下次启动 SelfHeal 兜底。还原统一 `restoreSystemProxy()`（StateOn 才 Disable，幂等）。
- 停止监听前若系统代理已接管须先 SetSystemProxy(false)，否则全网断；headless 模式不管系统代理。
- `upstreamMode=system` 在接管后无效（注册表 ProxyServer 是自己→UpstreamFromSystem 防环返回空=直连）；防回环另见 `proxy.GuardUpstreamLoop`。

## Windows CA 与证书信任
- Root CA 位置：`config/ca/prism-ca.pem` + `prism-key.pem`；每次 StartProxy 重新从盘加载 + bypass 挂在新建 proxy.Server 上——**换 CA 后关再开监听即可生效，无需重启程序**。
- **便携模式只搬 settings.json 不搬 CA 目录**：config/ca 为空时 LoadOrCreateCA **静默生成新 CA**，受信根还是旧的→全 CONNECT 盲隧道。排查=对比 `openssl x509 -in config/ca/prism-ca.pem -noout -fingerprint -sha1` 与受信根 `CN=Prism Root CA` 指纹。修复：装当前 CA（certutil）或拷旧 CA 覆盖。
- 历史指纹：旧 CA（%APPDATA%，模拟器用户 CA 由它签发）sha1=`1953B19BC393481C1237CC42200B5472D56E82AE`；2026-09-05 便携新 CA `1C0ECAC5...`（未装根）。
- 安装：`certutil -addstore -user -f Root <cer>` 免管理员但弹安全警告需人工点"是"；全机版需管理员。Edge/Chrome 信任当前用户存储即生效。
- 分诊工具：`openssl s_client -proxy 127.0.0.1:9090 -connect <host>:443 -servername <host> -brief`（Git for Windows 有 openssl 无 curl；anaconda 也带）。
- **wails build -clean 会清 build/bin/config/ca 换全新 CA**（build.bat clean 的"保留 config"保护对直接调 wails -clean 无效）；增量改 Go 用 `go build -trimpath -ldflags "-s -w" -o build\bin\PrismProxy.exe .`（保留 config）。**build.bat clean 已修**：config 先 move 到 build/_config_keep 暂存再迁回（CA/settings/endpoint 全保）。发布包不含 config/CA，终端用户首装仍需装 CA。

## 模拟器（雷电 LDPlayer14 = Android 14/SDK34，VirtualBox 架构）
- guest 网段 172.16.1.0/24；NAT 由独立进程 **VBoxNetNAT.exe** 完成→模拟器流量统一归因 VBoxNetNAT.exe（非 dnplayer/Ld9BoxHeadless），多开共享归因粒度受限。**`172.16.1.2`=宿主回环别名（ping TTL=128）**，代理绑 127.0.0.1:9090 即可被模拟器经 172.16.1.2:9090 访问，无需绑 0.0.0.0；`.1` 是虚拟路由器不可用。
- **仅设 Windows 系统代理对雷电无效**（VBox NAT 不感知），必须 Android 内 `adb shell settings put global http_proxy 172.16.1.2:9090`（清除 `:0`，跨重启持久保留）。雷电自带 adb `D:\leidian\LDPlayer14\adb.exe`，设备 emulator-5554（另有真机勿误操作）。
- **LDPlayer14 用户 CA 合并不生效**（重启后 conscrypt/system cacerts 均无用户证书）。解法=**root 直注系统 CA bind-mount tmpfs（重启失效需重做）**：`ro.debuggable=1` 可 `adb root`；APEX verity 不可 remount，改为拷 /apex/com.android.conscrypt/cacerts 到 /data/local/tmp/cacerts-inject + 加 `<subject_hash_old>.0`（chmod 644/chown root:root/chcon system_security_cacerts_file）后 `mount --bind ... /apex/com.android.conscrypt/cacerts`；新 fork App 立即信任（已运行的 am force-stop 重开）。hash 用 Git 的 openssl `x509 -subject_hash_old`。模拟器内旧用户 CA 文件 `9e85ecda.0`（1953B19B 签发）。
- LDPlayer 的 su 是 toybox 版（`su -c` 内 -o/分号全被拆错）→脚本 push 后 `adb root` + `adb shell sh /data/local/tmp/x.sh`。
- `adb reboot` 在雷电上可能起不来（adbd 不监听）→`ldconsole.exe quit --index 0` 等 6s 再 `launch`。
- 模拟器自带 curl 8.0.1 **不读 CA store 也不读全局代理**（需显式 -x、验证书加 -k；-k 200 只验链路），**验证 CA 信任必须用 AOSP 浏览器 com.android.browser（WebView 信任注入的系统 CA）**。少量主域 CONNECT 盲隧道多为证书固定 pinning。
- App"未连接网络"分诊：`adb logcat -c`→`am force-stop`→冷启动（入口 `cmd package resolve-activity --brief <pkg>`）→等 20-30s→logcat 搜 `ProtocolException|SocketTimeout|...`；CLI flows list 找非 done 流。截图 `screencap -p` 落盘再 pull（PowerShell exec-out `>` 会把 PNG \n 破坏成 \r\n）。

## ADB 自动代理（设置「ADB 代理」tab，internal/app/bindings_adb.go）
- 全局 `adb.deviceProxyHost`（默认 172.16.1.2）+ 多条 `ADBDevice{Name,Path,Serial,AutoSet}`；端口不存、运行时取实际监听口。binding：PickAdbPath/AdbTest/AdbSetProxy/AdbClearProxy；`runAdb(path,serial,args...)` 20s 超时（adb server 首启慢），serial 非空前置 `adb -s`。
- **代际收敛（核心）**：`adbGen atomic.Uint64` + `adbCh chan adbOp` 单 worker FIFO 串行队列；startProxy 成功/stopProxy 都在持锁区 `adbGen.Add(1)`，任务入队捕获 gen（goroutine 等解锁后拿快照入队，避免持锁重入死锁），worker 执行前 gen 不符即丢弃——热重启 stop 的 clear 旧代际被弃，只有最新 start 的 set 落盘，杜绝 clear/set 乱序静默覆盖。
- `stopProxy()` 拆 `stopProxyWithOpt(true)` 与 `stopProxyNoHooks()`：Shutdown/headless 走 no-hooks（已先做 onSessionEnd 同步清除，避免冗余 goroutine 被进程退出截断）；startProxy 末尾 `go autoSetAdbProxies`。
- **SaveSettings 收敛 `convergeAdbConfigs`**：被删/取消 AutoSet 的设备补一次 clear（否则设备 http_proxy 永久指向死代理=真断网）；新开 AutoSet 且代理在跑补 set（restarted=true 跳过由启动挂钩统一处理）。diff key=TrimSpace(path)+TrimSpace(serial)。
- AdbTest 三态：device/offline/unauthorized；全 unauthorized 报"请点允许 USB 调试"，多设备提示填序列号。监听地址解析用 net.SplitHostPort（支持 `:9090`）。
- CLI 无 adb 子命令（GUI binding），仅 `cli ui settings adb` 可打开该 tab（UISettingsTabs 白名单含 adb）。前端 AdbTab 行身份用 WeakMap 分配 rid（删中间行后 pick 回写不串行）。

## 规则引擎与域名组
- 三层规则：捕获/解密/进程；有序列表首条命中+默认动作。
- **过滤规则组（M4.6，设计唯一事实源 doc/规则设计.md）**：`FilterGroup{id,name,enabled,mode(blacklist/white),hosts,paths,processes}` 替换旧 captureRules/processRules（启动幂等迁移，迁移后清空旧字段）。语义：组内维度 AND、同维度多条目 OR、组间 OR、deny-override（黑名单恒优先；无白名单组默认全显示）。path glob：`*` 跨 `/`；无通配符=精确或子路径段边界（`/telemetry`≠`/telemetryx`），字符串比较非正则，只匹配 path 不含 query。统一入口 `ShouldDisplay(host,rawURL,procName)`（nil receiver 安全返 true）。**信息缺失维度移除规则**：进程未知（VBoxNetNAT.exe）/盲隧道无 path→该维度约束移除，剩余空则组不匹配（纯 path 白名单放不了隧道流是已知局限）。
- @域名组引用：校验存在性但只 warning 不阻塞（Validate 返 error+warnings；SaveSettings 返 `SaveSettingsResult{warnings}`）；hosts 禁裸 `*`；NewEngine 失败=空引擎全放行、保存失败=拒绝保存；Engine.groups map 仅供解密层 @引用（过滤热路径用编译期预展开）。
- **快捷忽略=双内置黑名单组（必须双组）**：`_quick_ignore_hosts`（hosts 维，裸域名含全部子域）/`_quick_ignore_procs`（processes 维），均 blacklist/enabled 组间 OR。单组混维会因组内 AND 互相收窄导致两个动作彼此失效；旧混合组 settings.Migrate(splitQuickIgnoreGroup) 启动幂等拆分。Binding `AddQuickIgnore(target,value)`。忽略只影响后续流量不删存量。
- **规则导入导出**：JSON version=1（exportedAt/filterGroups/decryptRules/bypassList/groups）；整体替换；bypassList 非 nil 才替换（防旧文件缺字段清空绕过）；内嵌域名组自动 WriteUser 补建；来源=文件对话框/本地路径/http(s) URL；err 阻塞 warns 弹窗；落盘后 SetLimits→rebuildEngine，仅监听/上游变化才重启代理。前端导入前 useDialog 强确认。
- **域名组**：`domains/` 22 个 txt + index.json **源码保留提交 git 但不 embed**（exe 无默认组，首启列表空）；txt 每行一域名（裸域名=自身+全部子域，`*.x.com` 等价），头部标准标题 `# 域名组：<名>`，index.json 不重复域名（txt 唯一事实源，增删必须同步索引）。用户组落 `config/projects/<id>/domains/<id>.txt`（项目化后），仅 LoadUser 加载（Custom=true）。URL 导入 ProbeURLImport 自动识别 index/txt（索引 file 相对 URL 解析）；批量导入 ImportDomainGroupsFromIndex(url,ids,overwrite) 发 `index-import-progress` 事件，冲突 overwrite=false 时 os.Stat 命中 Skipped。显示名优先级：txt 标题 > index name > id。限制：仅 http/https、20s、4MB LimitReader、2xx。gid 一律 ToLower(TrimSpace())（ValidateID `^[a-z0-9][a-z0-9-]{0,63}$`，CLI 从文件名派生也要规范化）。

## SQLite 持久化（internal/persist，M7，默认关闭）
- 选型 **modernc.org/sqlite 纯 Go 无 CGO**（mattn/go-sqlite3 依赖 CGO 破坏单 exe，禁用）；`CGO_ENABLED=0 wails build`。DSN `?_pragma=busy_timeout(5000)&_pragma=journal_mode(WAL)&_pragma=synchronous(NORMAL)` + SetMaxOpenConns(1)。每项目独立库 `config/projects/<id>/prism.db`（dbPath 固定，旧 persist.dbPath 配置已废）。
- `Writer{db,queue chan *capture.Flow(2048),...}`：**queue/stop 在 Start() 内创建**（Open 只建表）；Enqueue 仅终态流、先 cloneFlow 深拷贝、`select{case queue<-f:default:dropped++}` 非阻塞丢弃，**绝不阻塞代理热路径**；consumeLoop 100 条/500ms 批量事务。
- 两表：`flows`（id PK + started_at 索引 + 检索字段 + data=完整 Flow JSON，序列化前记 BodyLen 再 Body=nil）、`bodies`（(flow_id,kind) PK，zstd 压缩 blob，≥256B 且压后更短才压）。
- **历史流 body 惰性回查**：LoadRecent 不载入 body，详情经 `persistBody→Writer.LoadBody`（ErrNoRows→(nil,nil) 静默；解压错 log+提示）；Source="history" 防回写（onPersistEvent 跳过 history 流；Pinned 不持久化，加载时强制 false）；DTO `FlowMeta.Historical` PascalCase 键。
- **保留策略**：retention 2s 首跑+每 10 分钟，可取消定时器（Close 后不触发）。必须 `auto_vacuum=INCREMENTAL`（仅建表前生效，旧库 migrate 里 PRAGMA+VACUUM 一次性迁移）；**WAL 收缩铁律顺序**：DELETE→`PRAGMA incremental_vacuum`→`PRAGMA wal_checkpoint(TRUNCATE)`，反了主文件不缩；dbSize=主+wal 合计，enforceSize chunk=64 分批删；retainDays/maxMB=0 不限。体积测试用 crypto/rand 不可压缩体并断言文件字节数。
- **锁重入死锁红线**：pmu 临界区内严禁调用同步触发 store emit 的方法（st.Add）——emit 在解锁后同步回调 onPersistEvent 又取 pmu→同 goroutine 二次加锁永久死锁。**约定：pmu 只保护 writer 指针；订阅与历史补载一律解锁后执行**。锁序铁律：`projMu → a.mu/a.pmu`，禁止反向。
- 热更新：路径不变仅 UpdateRetention（不 Close/Open）；切换项目 persistOwner{w,gen,id} 打标，入队前比对 flow.Gen 与 writer.gen（封代际窗口）；Shutdown/headless 退出前 stopPersist。

## Composer 调试重发（internal/compose，M6）
- 独立 http.Client **直连防回环**（不设 tr.Proxy=直连 9090；仅非 direct 上游时代理 + GuardUpstreamLoop）；HTTP/1.1 only（自定义 TLSClientConfig 后**故意不设** ForceAttemptHTTP2）；Dial 15s/Timeout 30s/重定向限 10；DisableCompression:true。
- 本进程发出须显式 `ProcessInfo{PID:os.Getpid(),Name:filepath.Base(exe),Path:exe}`；剥逐跳首部（见代理引擎第 8 条）。
- **重发流经 Store.Add 直入、绕过过滤引擎是设计**（保证重发必入列表；否则可能被自己的快捷忽略吞掉），Source="composer" 仅标记。前端发送成功 store.select(newFlow) 形成迭代链。无 URL/CONNECT 禁用入口；非法 URL 返回 url.Error 且不落库。
- HTML 预览：iframe `srcdoc` + `sandbox="allow-same-origin"`（**绝不开 allow-scripts**）+ referrerpolicy=no-referrer + 注入 `<base href>`。
- 共享展示三件：HeaderTable.vue / CopyBar.vue（硬依赖 wails GetFlowRawText，复盘不挂）/ BodyViewer.vue（七视图）。
- CLI `compose <URL> [--method --header 'K: V'（fs.Var 可重复；单值内兼容分号）--body --insecure]`。

## ctlapi / AI CLI（internal/ctlapi，M8/M8.5/M10）
- 形态：同 exe 子命令 `PrismProxy.exe cli <cmd>`（main.go flag.Parse 前拦截）+ `-headless` 可组合（同 exe 两角色：-headless 起实例、cli 控实例）。API 仅绑 127.0.0.1:9595，Bearer token（crypto/rand 32B，constant-time 比较，支持 Authorization 头与 X-Prism-Token；`/events` 额外接受 query token）。
- endpoint 发现文件 `config/ctl-endpoint.json`{addr,token}：原子写+0600+icacls 限当前用户，实例 Close 删除，强杀残留→cli 友好报错、下次覆盖。**token 每次实例启动重新生成**（startCtlAPI→NewServer 第二参传空→newToken() 随机 64 hex，覆盖写 endpoint 文件，旧 token 即失效）——GUI 实测/自动化/外部脚本在实例重启后必须重读 endpoint 文件取新 token。
- **ctlService 适配器**（ctl_bridge.go）包装 *App 实现 ctlapi.Service 接口——App 已有同名 Wails binding，**不要直接在 App 上加 ctlapi 方法**（签名冲突）；接口加方法必须同步 `ctlapi_test.go` fakeService（`var _ Service` 编译期断言，缺桩即编译失败）与 wailsjs 三个生成文件。
- flag 处理：Go flag 遇位置参数即停，自写 reorderFlagsGlobal/reorderFlags 把 flag 前置（bool flag 不吞下一 token）；pos[0]=子系统名。**处理器自管 stdout 原文输出（如 rules export）返哨兵 `errOutputHandled`**，RunCLI errors.Is 跳过 printResult——否则尾随 `null` 破坏 `> file` 重定向。
- 命令面（M10 补齐后）：status / flows list|get|clear|watch|pin|curl / rules list|ignore|enable|disable|decrypt|export|import / sysproxy / settings / ui clear|settings [tab] / project list|switch|create|rename|delete|close / proxy start|stop / ca install / adb devices|test|set|clear / domains list|get|save|delete|import / compose / processes。headless 绕行三式：导出→stdout、导入→实例侧 fetchSourceBytes、adb 路径→--adb/--serial。
- **UI 控制**：数据级（ui clear）GUI 经 flow:evict 自动同步 headless 也执行；纯界面（ui settings）走 `EventsEmit("ui:open-settings",tab)`，tab 白名单 general/network/decrypt/capture/domains/adb/ai，非法 400，headless 返 {ui:false}。
- 启动时序：startCtlAPI 在 startup 开头同步执行（数百 ms）会推后 StartProxy，前端只刷一次会看到"已停止"——**startup 末尾（自动启动/接管完成后）必须发 `proxy:ready` 事件**，前端立即拉+事件推双保险。startup 加同步耗时步骤都要考虑。
- **JSON 字段大小写口径**：FlowMeta/FlowDetail/BodyPayload **无 json tag=PascalCase**（字节字段 base64）；status/sysproxy 手构 map 小驼峰；settings.Settings 有 json tag 小驼峰。
- HTTP server：只设 ReadHeaderTimeout，**禁 WriteTimeout/IdleTimeout（杀 SSE）**；Close 先 Hub.Close() 再 Shutdown(2s)。**ServeMux 精确模式不匹配子路径**：`/api/v1/flows` 不匹配 `/flows/clear`（落子树 404），动作子路径必须显式注册。
- **SSE（M8.5）**：`GET /api/v1/events` 双频道 flows/status，Hub fan-out。五决策：①先订阅后快照（snapshot 帧，缝隙流不丢，重叠靠 upsert 幂等+evict 忽略未知 ID）；②headless 下 ctx 恒 nil，hub fan-out 必须置于 flush() ctx 早退**之前**；③ctlapi 不能 import app（循环依赖），过滤走 Filterable 接口（FlowMeta.FilterFields()）；④`[]Filterable` json.Marshal 成 `[{}]`——FlowsUpsert 自定义 MarshalJSON 逐条 marshal 具体类型；⑤watch 事件流长连接与快照请求**连接隔离**（独立 DisableKeepAlives client，否则 SSE 帧被当 HTTP 响应消费）；背压摘除只 close(reset) 不 close(ch)；订阅上限 32 超限 503；watch 退避 0.5→8s、连续 5 次失败退出码 1、Ctrl+C/Break 码 0。
- status 发布点：bindings_proxy 拆公开 StartProxy/StopProxy（发事件）+ 内部 startProxy/stopProxy（不发，供连调）。

## 多项目隔离（M9）与无项目态（M11）
- 切分：**规则类按项目隔离**（filterGroups/decryptRules/用户域名组→`config/projects/<id>/project.json`+domains/+独立 prism.db）；**环境类全局**（settings.json：监听/上游/ADB/预算/开关 + Projects 元数据 + CurrentProject）。顶栏热切换不重启代理。
- 热切换：**preflight 前置**（加载配置/域名组/规则预编译全部在 `projGen++` 之前，任一步失败整体拒绝不留半态）；切换段 projGen++→ClearAll→换库→引擎→落盘→emit project:changed 不再失败。在途流终态按 flow.Gen 新旧代际比对丢弃（两项目都不留，拍板）。首次启动旧单配置幂等迁移为默认项目（M11 前）。
- SaveSettings 用 SettingsView 合并视图：入参 currentProject/projects 一律忽略（堵 CLI 整结构回传后门），切换唯一入口 SwitchProject；**rulesProject 令牌**与当前项目不一致拒绝保存（防双端覆盖）。apiKey 等投影字段同理（见 M13）。
- CLI：`--project/-P` 仅对 rules/settings/domains 类生效；非当前项目写=读盘→改→ValidateRules/NewEngine 编译预检→落盘（不改 current、不热切换、非法则文件不变）；当前项目写=内存+落盘+热重建。
- **M11 无项目态（三指针置空）**：proj=nil/groups=nil/eng 持 nil（nil receiver ShouldDisplay/ShouldDecrypt 返 true=代理照常全放行）。`enterNoProjectLocked()`（持 projMu）：projGen++→ClearAll→nil 指针→停 writer（不开新库）→eng.Set(nil)→CurrentProject="" 落盘，进入后不失败。CloseProject 幂等；DeleteProject 放开所有约束（可删当前/删光），删当前先 enterNoProject 释放 db 句柄（否则 Windows 占用删目录失败）→删清单→SaveGlobal（失败回滚插回+切回）→删目录→剩余非空自动切首个。
- nil 守卫：projDir/currentID/currentMeta nil 安全助手；所有写规则/域名组操作持锁后显式报"尚未打开任何项目"；GetSettings 无项目返空规则切片 rulesProject=""，SaveSettings 无项目跳过规则半边但**环境热应用照常**（needRestart 重启/bypass Reapply/convergeAdbConfigs 不跳过；CLI 无项目态空入参须放行到环境保存分支）。
- 前端 useProjects composable 模块级单例，`project:changed` 订阅模块级只注册一次（不依赖组件生命周期）；无项目挂全屏 WelcomePage（主界面含头/底栏不挂载，footerRef 为 null 已可选链兜底）；欢迎页设置抽屉复用，项目级三 tab（解密/过滤/域名组）v-if hasOpenProject，无项目态 effectiveTab() 回退 general。
- 教训：**同一文件多个 SearchReplace 不可并行**（竞态丢改动，已踩三次含 App.vue 漏 import 白屏）；并行编辑按文件隔离。

## 前端架构
- **通信**：Go 方法经 Wails Bindings（wailsjs/go/app/App）；后端推送经 EventsOn（wailsjs/runtime/runtime）。关键事件：`proxy:start-error`、`proxy:ready`（底栏状态刷新）、`ui:open-settings`、`flow:upsert`/`flow:evict`、`project:changed`、`index-import-progress`。
- **App.vue（2026-09 重构后）**：纯布局编排（WelcomePage / AppHeader / NSplit[FlowList+FlowDetail] / AppFooter / SettingsPanel / Composer）+ 错误条 startError + 设置抽屉开关。错误条归属 App.vue，「打开设置」按钮保持 showSettings=true 不重置 tab。
- AppHeader.vue（40px）：ProjectSwitcher、流计数/暂停刷新/清空、GitHub（runtime.BrowserOpenURL，浏览器降级 window.open，内联 octocat SVG 无 @vicons）。无 props/emits 直用 store。
- AppFooter.vue（28px）：过滤栏（关键字/正则/方法/状态多选，空串哨兵=全部，非法正则降级子串）、代理状态标签、监听/系统代理开关、设置按钮。自持 status/sysState/showSysSwitch。
  - **defineExpose**：`refreshStatus()`=并发两状态查询（含 StartError 兜底）用于热路径（开关 finally、proxy:ready/start-error 后）；`refresh()`=refreshStatus+GetSettings 读开关可见性，仅挂载时与设置保存后调（避免热路径多一次往返）。
  - **StartError 去重**：lastStartError 记已上报原文，同一错误只 emit 一次；错误文本变化或后端清空后才允许再报。emits：`open-settings(tab)`（network/general 统一 onUIOpenSettings 含 WindowUnminimise）、`error(message,replace?)`（replace=true 覆盖显示最新，否则仅错误条空时恢复）、`clear-error`。
- **flows store**：flows/filter/paused + init()/clear()/filtered。暂停=纯前端冻结显示（回调丢弃，后端不中断不补发）；clear 保留 Pinned。置顶 Pinned：固定顶部/不参与环形淘汰/Clear 保留/上限 200/会话内不持久化。
- 设置面板 SettingsPanel.vue：左侧 n-tabs（常规/网络/ADB代理/解密规则/过滤规则/域名组；无项目只显全局三个），宽 560-640，内容区独立滚动；watch [show,initialTab]（面板开着外部切 tab 只切 activeTab 不 loadSettings 覆盖编辑）。系统代理实时控制不随表单保存；保存错误用全局 message，warnings 留固定 footer。autoSysProxy 默认 false（startup StartProxy 成功后才接管，端口占用跳过，失败仅 log）。
- pages/ = App.vue 直接挂载的页面级；components/ = BodyViewer/HeaderTable/CopyBar/JsonTree/FreeNotice 共享件；无 vue-router。
- **FreeNotice.vue**：「本工具完全免费，为爱发电 ❤ + 开源地址 GitHub 链接」空态说明组件，点击走 window.runtime.BrowserOpenURL（浏览器预览降级 window.open）；用于 WelcomePage（无项目空态居中；有项目时列表下方 `.main-free`，`.welcome-main` flex column + margin-top:auto 贴右侧底区）与 pages/FlowDetail.vue 未选中流空态，文案/链接改动只改此件。标题 `.fn-title` 为粉→紫→蓝渐变流光文字（background-clip:text + fn-shine 3.5s），爱心 `.fn-heart` 双跳心跳动画（fn-heartbeat 1.4s，含 drop-shadow 脉冲）。**不做 prefers-reduced-motion 降级**：品牌装饰动画用户要求恒播放，且本机 MinAnimate=0（Windows 关闭动画）时降级会致动画全失（2026-09-10 踩坑）。
- **构建红线：vite build 绿 ≠ 前端可用**——vite 无 auto-import 时 Rollup 对未解析标识符静默当全局外部名，退出码 0 但运行时 ReferenceError 白屏；前端改动后必须核对 IDE 诊断零错误或 grep dist 特征文案。模板内 JS 模板字符串以 `}}` 结尾会与 Vue 插值冲突，改用拼接/计算属性。

## 数据复盘页（M12/M12.1/M12.2）
- 独立 Vite 多页入口 `review.html` + `src/review/`（独立 createApp，不依赖 Wails runtime），经 ctlapi（127.0.0.1:9595，Bearer token，静态 dist 不鉴权、`/api/` 鉴权）由系统浏览器开独立窗口；Wails v2 无多窗口能力。**Vite base 保持默认 `/` 是红线**（改 `/review/` 主窗 404 白屏）。
- 数据层 `review/api.ts`：HttpApi（fetch Bearer）+ DemoApi（无 token/?token=mock）双实现，任何新接口两端都要补；DTO：TagInfo/HistBucket 小写 json、FlowMeta/FlowDetail 大写字段。
- M12.1：列表/直方图查 `scope=archived|all`（默认 archived=打标流；具体标签恒归档，服务端宽容忽略 scope）+ `start/end` unix 毫秒含头尾半开（0=不限）；`/api/v1/tags` 根级 `total`（DISTINCT 去重，前端禁止 count 累加）/`totalFlows`；`GET /tags/{id}/histogram`（buckets 默认 120 上限 500），底图随 tagID+scope 重拉但**恒全域分桶不随窗口变焦、不随关键字/忽略联动**。ReviewTimeline.vue 纯 SVG + pointer 手势状态机（4px 阈值/选区三分区 ±6px/最小 2 桶），拖拽中不刷列表；ReviewApp 用 flowSeq/histSeq 双代际防护。
- **列表分页与关键字（2026-09-09）**：n-pagination 页码分页（非无限滚动），page 1 起、pageSize 默认 100 可选 [50,100,200,500]，limit/offset 传服务端（limit 默认 200 上限 1000）；切标签/scope/时间窗/关键字/页大小统一 `loadFlows(true)`（reset 强制 page=1），翻页 onPageChange（不清屏保留旧行）。关键字 `q` **下沉服务端**：`buildFlowQuery(tagID,scope,start,end,q)` 单一谓词构造点，列表与 CountFlows(total) 同口径——method/host/path 三列 `LIKE ? ESCAPE '\'` OR（整体括号化），likePattern 转义 `\ % _`、TrimSpace 空串不过滤；直方图显式 q=""；q 不匹配 Tags。HTTP GET /tags/{id}/flows 增 q（handler 内 url.Values 局部变量改名 params 避免与 q 混淆）；ReviewApi.listFlows 七参（…,limit,offset,q），HttpApi/DemoApi 同口径（Demo 三列小写子串、不含 Tags、limit/offset 钳制、回显 q）。前端关键字 watch 防抖 300ms。
- **空态与失败回滚（2026-09-09 审后修）**：空态在 winStart 分支后插 `v-else-if="keyword.trim()"` 分支（🔍 + 清除关键字按钮，clearKeyword 取消挂起防抖定时器后由 watcher 统一重拉，避免双请求），否则 q 无匹配会谎报"暂无流"。`loadFlows(reset, restoreOnError=false)` 普通失败仅调用方要求才恢复快照——**只有关键字 watcher 与 onPageSizeChange 传 true**（筛选维度未变），切标签/scope/时间窗默认 false（跨维度恢复=脏数据）；翻页/改页大小失败分别回滚 page/pageSize（以 !fatal 为前提）。
- ReviewApp 三栏（sidebar 220px / list 46% / detail flex1）拖拽调宽：`.splitter` 6px ×2，SIDEBAR_MIN 150/侧栏≤45%/LIST_MIN 320/DETAIL_MIN 360；listW 挂载后按 46% 换算（=0 时 CSS 兜底，fatal 重试成功 nextTick 补测）；window 级 pointermove+setPointerCapture；时间轴 ResizeObserver 自动重测桶数。
- **M12.2 忽略名单与排序**：`review_ignores` 表（kind∈host/path/proc/**method/status**（2026-09-10 增），PK(kind,value)），**忽略=查询排除隐藏不删 flows**；单一谓词点 `buildFlowQuery`（reviewQueryOpts{Q,SortKey,SortDir,ShowIgnored}），!ShowIgnored 且名单非空拼 NOT 排除；Histogram 显式空 opts 不联动。host 四 LIKE（含端口/子域，`%.host` 跨点）、path 精确+下级前缀、proc 走 `json_extract(data,'$.Process.Name')` LOWER 等值、**method=`LOWER(COALESCE(f.method,''))<>LOWER(?)` 大小写不敏感等值（method 列可空须 COALESCE 兜空串，CONNECT 隧道流一并隐藏）、status=`f.status<>?` 数字等值（NOT NULL DEFAULT 0，0=无响应/错误流不会被任何 100–599 误伤）**；**SQLite BINARY 按字节排序，文本列 ORDER BY 必须包 LOWER()**（flowOrderBy：time 默认 desc、其余 asc，同值 StartedAt desc 兜底）。归一化 NormalizeIgnore*：host 剥端口（:后纯数字）/小写/去尾点去`*.`、path 截 ?# 补前导 /（`/` 拒绝）、proc trim、**method trim+大写、status 须 100–599 三位整数（存字符串、SQL 绑数字）**；AddReviewIgnore 先 SELECT 后 INSERT/UPDATE（同毫秒幂等）。读走 reviewReader()（旧归档库可能无表须容错），写走 acquireArchive()+projGen 代际校验。
- **M12.2 HTTP 契约**：`/api/v1/tags/ignores`（ServeMux 精确模式须显式注册；path 非精确委托 handleTagSub，parts[0]=="ignores" 防御 404）：GET→`{ignores:[{kind,value,createdAt,note}]}`、POST `{kind,value,note}`→`{ignore,added}`（幂等）、DELETE `?kind=&value=`（value 可含 "/" 走 query）→`{deleted}`；GET /tags/{id}/flows 增 sort（time|method|status|host|path|size|proc）/dir（asc|desc）/showIgnored（1|true 为真），响应回显。ctlapi Service 保持 HTTP 原语参数（不依赖 persist 包），ctl_bridge 组装 persist.ReviewListOpts；**接口加方法同步 ctlapi_test.go fakeService** 与 wailsjs 三个生成文件（无 json tag→models.ts 字段大写 Q/SortKey/...），即使复盘走 HTTP 也要手动同步防脏 diff。
- **M12.2 前端**：ReviewApi listIgnores/addIgnore/removeIgnore + listFlows 末参 ListFlowsOpts（DemoApi 内存五层 Map（host/path/proc/method/status）复刻归一化/命中/LOWER 排序/眼睛过滤）。7 列（增进程 fr-proc），`.flow-head` sticky（#101014 不透明）点击排序；右键出忽略菜单的列由 FlowTable `actions.ignoreKinds` 白名单决定（2026-09-10 起复盘传全五类 host/path/proc/method/status；NDropdown trigger=manual + clientX/Y，CONNECT/空路径/空 ProcessName/空 Method/空 Status 分别禁用对应项），addIgnore 成功同步本地数组（角标实时），仅隐藏态 loadFlows(true,true) 重算。**顶栏小眼睛=忽略名单管理面板**（取代直接切换）：n-popover raw 暗卡 `.ignore-panel`（320px/max 60vh），host→path→proc→method→status 再按值排序（IGNORE_LABEL 五类中文、k-method/k-status 色卡）+ 每项垃圾桶（removingKey 单条 loading）；右上 n-switch「显示被忽略流量」（默认 false，localStorage `prismproxy:review-show-ignored-v1` 持久化，失败回滚 UI+存储）；`.eye-wrap`+`.eye-dot` 规则数角标，有规则/面板开/显示态紫色高亮。ignores 项目级与标签/scope 无关，面板每次打开 loadIgnores，refreshAll Promise.all 并行首拉；loadIgnores/removeIgnore 失败仅 message 不 fatal。侧栏计数/直方图不联动，仅列表+CountFlows。
- **复盘页全屏高度链路**：NConfigProvider 默认 abstract=false 渲染真实 `<div class="n-config-provider">`（NDialogProvider 是 Fragment、NMessageProvider 仅 teleport），插在 #app 与 .review-root 之间；review.css 必须给 `.n-config-provider { height:100% }`，否则整页只占自然高度、下方大片空白。
- **展示层复用 + 复盘重发（方案 A）**：`components/FlowDetailTabs.vue` 是主窗 pages/FlowDetail.vue 与复盘 review/ReviewDetail.vue 共用的四 Tab（概览/请求/响应/TLS）**纯展示组件**——props `{meta:ReviewFlowMeta, detail?, flowId, loader?:BodyLoader, respDisabled?, showEndpoints?}`；wails app.FlowMeta/FlowDetail 与复盘 DTO 同构（大写字段）直接互传；req-copy/resp-copy 具名插槽留给主窗 CopyBar；defineExpose({reloadBodies}) 供主窗 800ms 轮询。两个 Detail 页是薄数据外壳。`fmtCertDate` 兼容 RFC3339 字符串与 unix 秒数（PeerCerts 类型 `string|number`）。
- **复盘重发链路**：`review/ReviewComposer.vue`（主窗 pages/Composer.vue 的 HTTP 版：v-model:show 自管、不碰 pinia/wails、不做 store.select；预填走归档 flowDetail/flowBody，发送走 api.compose）。ReviewApi：`compose`（POST /api/v1/compose，后端零改动）、`liveFlowDetail`（GET /api/v1/flows/{id}）、`liveFlowBody`（GET /api/v1/flows/{id}/body）。**composer 结果流 Source=composer 因代际比对永不落归档库、只进实时 store**，故响应 BodyViewer loader 必须用 live*（归档接口取不到）。`ReviewComposedRequest` = `{method,url,headers:[{key,value}],body,skipVerify}` 小写 json。
- **Demo 模式豁免重发（用户决策，优先于"新接口双实现"默认约定）**：DemoApi 的 compose/liveFlowDetail/liveFlowBody 仅抛错桩；ReviewDetail 在 demo 模式把「调试重发」置 disabled + NTooltip 提示。
- **方案 A 审计 6 minor 全修沉淀**：①**disabled 按钮的 NTooltip 必须外层 span 接管 trigger**（原生 disabled 不派发鼠标事件，`.resend-wrap{display:inline-flex}` 承载 margin-left:auto）；②证书时间 string|number 双形态；③**compose 载荷上限 2MiB**（=ctlapi readBody 2<<20，超限被 LimitReader 静默截断→400 "unexpected end of JSON input"），ReviewComposer 发送前 `new Blob([JSON.stringify(req)]).size` 预检（pages/Composer 走 wails 无此限制）；④预填 watch 加 prefillSeq 代际防护（快速关开丢弃过期预填）；⑤`lib/composer.ts` 共享纯逻辑（COMPOSER_METHOD_OPTIONS/HOP_HEADERS/expandEditableHeaders/COMPOSE_BODY_LIMIT/composerStatusTagType/composerStatusCls），两份 scoped 样式仍各自维护需两处同步。

## M12.3 复盘列表组件复用 + 主窗内嵌复盘（2026-09-10）
- **承载决策（用户拍板）**：复盘并入主窗 Wails 视图（非系统浏览器独立页为主要形态），**零 Go 改动**；独立 review.html 浏览器入口保留（ReviewPage 的 `api` 为可选 prop，缺省才 `createApi()` 走 Http/Demo；App.vue 注入 WailsReviewApi）。模块级单例 `composables/useAppView.ts`（'capture'|'review'，同 useProjects 模式）；AppHeader 按钮切视图（review 态文案「返回抓包」），App.vue v-if 挂 ReviewPage + 隐藏 AppFooter，contentStyle 高度按视图切换（review=`calc(100% - 40px)`）。
- **共享表格 `components/FlowTable.vue`**（FlowList 与 ReviewPage 共用，运行时无关：不碰 wails/store/pinia）：props rows/columns(FlowColumn[])/sortKey/sortDir/layoutKey/defaultWidths/colMins/selectedId/actions/emptyText；列定义 props 化（key/label/cls/wi/sortVal?/text?/title?/cellCls?），**列 cls 必须为 c-host/c-path/c-proc/c-method/c-status 之一——行右键据此识别落点列给忽略项**；动作经 `FlowTableActions{buildCurl?,pin?,ignore?,ignoreKinds?,compose?}` 注入，`ignore?:(f,kind:FlowIgnoreKind)` 单一回调 + `ignoreKinds?` 维度白名单（**缺省 ['host','path','proc']——主窗 FlowList 不传故实时列表行为零变化，实时引擎 AddQuickIgnore 也只有 host/path/process；复盘 ReviewPage 显式传全五类**；窄签名 `(f,'host'|'path'|'proc')` 可赋给宽参数类型）（**复盘不传 pin=天然无置顶**）；右键菜单=复制 URL（组件内直处）+三 cURL+pin（仅注入方）+忽略（仅落点列在 ignoreKinds 内时）：host/path/proc/method（「忽略此方法（所有 X 请求）」，空 Method 禁用）/status（「忽略此状态码（所有 X 响应）」，空 Status 禁用）+调试重发；双击复制单元格内置；表头右键=显隐/左右移/重置列。排序状态留父级（主窗客户端、复盘服务端），defineExpose 仅 scrollToTop（复盘翻页后归零）。
- **布局持久化**：localStorage 按 layoutKey 隔离，主窗 `prismproxy:flowlist-col-layout-v2`（FlowList 保留 v1→v2 迁移 IIFE），复盘 `prismproxy:review-col-layout-v1`（7 数据列+状态点列；DEFAULT `[22,56,50,null,null,86,70,null]`，MINS `[22,44,40,80,100,60,48,70]`）。
- **踩坑**：①**useVirtualList 空态陷阱**——wrapper 高度随内容收缩，空行 wrapper 高 0，空态必须移出虚拟容器并绝对定位（top:28px 覆盖列表区）；②**Vue scoped 样式与插槽**：父传给子的具名插槽内容在父作用域编译，子 scoped 样式不命中——pin 图标/历史 badge/标签徽章样式放 FlowList.vue；③`.flow-table{height:100%}` 在复盘 `.list-pane` flex 列内需 ReviewPage 补 `.flow-table{flex:1;min-height:0}`（子组件根节点带父 scoped 属性可命中）；④FlowList 排序键必须 `:sort-key="sortKey"` 动态绑，静态字符串导致高亮不随选择变。
- **前端 TS cURL `lib/curl.ts`**：归档流不在实时内存、Go BuildCurl 用不了，前端用 flowDetail+flowBody('req') 拼装（`bp.Body||bp.Raw` base64→b64ToBytes）。**2026-09-10 已推翻"逐字对齐 Go"并删除 PowerShell，只留 cmd/bash，cmd 逐字节对齐 Chrome 152 DevTools「Copy as cURL (cmd)」；同日下午 Go 侧 curl.go 已同步同一套规则并删除 ShellPowerShell（见文末「cURL 复制」专节），两侧再无分叉**；body 省略 emit('curl-omitted') 父级提示。
- **Wails 多返回值的致命上限（见踩坑⑥，结论已推翻）**：最初以为 3~4 返回值会在 JS 侧给数组——**实测真实 Wails v2.15 会直接静默返回 null**，故全部改走单结构 DTO 绑定（见下）。
- **踩坑⑥（真因，F6 实测标签/列表/时间轴全空，已修）**：**Wails v2 绑定方法只支持 1~2 个返回值**。`internal/binding/boundMethod.go` 的 `Call` 仅 `case 1`（单值/error）/`case 2`（(value,error)），**输出数 ≥3 时无 case 命中，returnValue 与 err 都保持零值 → JS resolve(null)、不报错**，被前端 `?? []` 兜底成空态。HTTP 复盘（ctl_bridge 直接 Go 调用）与 Go 单测/只读连接全部正常，唯独主窗内嵌 Wails 视图空，极难定位。**修法（零侵入）**：`bindings_tags.go` 末尾新增 4 个单结构返回 `(DTO, error)` 包装绑定——`WailsTagsOverview{tags,total,totalFlows}`、`WailsFlowList{flows,total}`、`WailsAddIgnore{ignore,added}`、`WailsHistogram{start,end,buckets}`——内部转调原多返回值方法（ReviewTagsOverview/ReviewFlowList/ReviewAddIgnore/ReviewHistogram 原样保留给 HTTP 桥接与测试）；`wails generate module` 重生成 wailsjs 三文件；`review/api/wails.ts` 改调新绑定并 `r?.x ?? 默认值`。**铁律：今后任何 Wails 暴露给前端的方法返回值最多 2 个且末位为 error。**
- **踩坑⑤（主窗切复盘崩溃，已修，现被踩坑⑥方案取代）**：多返回值绑定在**空归档库（Go 返回 nil slice）或浏览器 mock（未实现绑定整体 `return null`）时，解构目标 `r` 本身可能是 `null`**——只写 `r[0] ?? []` 会先在 `r[0]` 处抛 `Cannot read properties of null (reading '0')`。改单结构 DTO 后用 `r?.tags ?? []` 即可（新绑定自身为 `(DTO,error)` 两返回值，Wails 正常 resolve DTO）。
- **mock 预览验证手段沉淀**：浏览器自动化（CDP 注入）产生的合成点击是 `isTrusted=false`，naive-ui NDropdown 选项的 `handleClick` 不响应（普通按钮/clickoutside 正常）——验证右键菜单业务分支可经组件实例 `el.__vueParentComponent.setupState.onCtxSelect(key)` / ReviewComposer 的 `send()` 直接驱动；**真实鼠标点击不受影响**。浏览器 mock 下主窗内嵌复盘无 Review* 绑定，修空值后正常渲染空态（console 有「未实现的绑定」warn 属预期）；独立 `review.html?token=mock` 走 DemoApi 才有演示数据。
- **ReviewPage 改造要点**：页面根挂独立 ReviewComposer（v-model:show + listComposerFlow，与详情页内按钮互不影响）；openListComposer=选中流+开弹窗；onSort 收 string 内 as ReviewSortKey（time 默认 desc 其余 asc）；列 cellCls 用 m-*/s-*（FlowTable 自带全套颜色）；删除旧自绘 .flow-head/.flow-row/.fr-* 全部 CSS 与 NDropdown/ctx* 状态；五分支 list-empty 仍由 ReviewPage 承担（未传 empty-text，空态只在 flows 非空时渲染表格）。

## M13 复盘 × AI 分析（2026-09-09 设计稿 v1，09-10 v2 增补 intent，仅 doc/复盘AI分析设计.md，未实施）
> **2026-09-10 启动开发**：新建 `v2` 分支（从 main），实施计划唯一入口=[doc/开发计划2.0.md](./doc/开发计划2.0.md)（P0 配置层→P1 internal/ai→P2 ctlapi/绑定→P3 AiTab→P4 复盘四模式；各阶段出口与 AC 锚点在该文档）。设计稿仍是唯一设计依据，偏离 §十一 待拍板点须先改设计稿。
> **2026-09-10 计划审计（开工前，已定稿）**：审计发现 2 高危 5 中危 8 低危，用户拍板后已全部回写设计稿（**v2.1**）与计划（**v1.1**）。定稿决策：①**主窗内嵌 AI 通道=方案 A Wails 事件桥**（`AIChatStart/AIChatStop` 绑定 + `ai:chat` EventsEmit 逐帧，HTTP SSE 与事件桥共用 StreamAIChat 编排与并发闸门，Wails 侧冲突返 error；WailsReviewApi 无 token/fetch 9595 被 CORS 拦是背景）；②**POST /ai/config 部分更新**（出现的字段才覆盖；apiKey 空串=保持、`__clear__`=清空；响应回脱敏投影）；③**温度 0 为哨兵**（UI slider min 0.1，WithDefaults 兜底 0.3）。其余修正：WithDefaults 兜底（防老配置 Redact=false 隐私倒退）；ValidateEnv 不改签名、key 空 warning 走 app.SaveSettings 追加 warnings（settings 层 WarnNoKey helper）；取数=persist 现有 LoadFlowByID/LoadBody + 新增 LoadFlowsByIDs（原稿方法名系笔误）；ids 截断 StartedAt 降序后端复查；review/api.ts 实为 review/api/ 目录（前端三实现 http/demo/wails）；test 返回补 model；FlowTable checkbox 列不进列布局持久化；UISettingsTabs 现无 "ai"（原记载有误，P2-2 正确补）。
> **2026-09-10 P0 配置层完成**（go test/vet + 全仓库测试全绿，提交 28e9c19）：`settings.go` 新增 AIConfig（十字段 json 小驼峰）/GlobalSettings.AI/AISettings 无 key 投影/AIProviderOllama 常量；DefaultGlobal 默认子值（custom/0.3/120/50/64/Redact=true）；**WithDefaults 零值兜底**——整体未初始化判定 `Provider==""&&MaxKB==0`→四数值默认+Redact=true（仅读取侧不回写；Redact bool 无哨兵，部分初始化分支不改写，测试已固化该语义）；AIConfig.Validate（Enabled=true 强制：http(s) BaseURL/Model 非空/温度[0.1,2]（0 哨兵）/超时[10,600]/流数[1,100]/预算[8,256]）；NormalizeAIBaseURL（TrimRight/ + 补 /v1）；WarnNoKey()（ollama 豁免，文案供 P2-14 追加 warnings）；ValidateEnv 转发 g.AI.Validate()（key 空不阻塞）。测试 8 个新用例（settings_test.go，含旧 JSON 零值反序列化+兜底断言、投影 JSON 无 apiKey 泄漏断言）。下一步 P1：internal/ai（client.go/prompt.go 零外部依赖）。
> **2026-09-10 P1 internal/ai 完成**（go test -timeout 90s ./internal/ai/ 全绿 20 用例、go vet 干净、gofmt 干净）：四文件 client.go/prompt.go/client_test.go/prompt_test.go，零外部依赖。**client.go 契约**：`Config{BaseURL,APIKey,Model,Temperature,Timeout,ProxyURL}`/`Message{Role,Content}`/`Delta{Text,Reason}`/`Stream(ctx,msgs,onDelta)`/`Probe(ctx,msgs)(string,error)`（非流式 max_tokens:8）；首块超时=min(30s,Timeout/4)，`time.AfterFunc`+`atomic.Bool` 看门狗+`context.WithCancel`，**禁 http.Client.Timeout**（会掐断整条 SSE 流）；错误文案三分支（首块超时「连接服务商超时：N 秒内未收到首个响应」/ctx 透传/其余「连接服务商失败：%v」）；SSE 手写 Scanner 解析（64KB 初 buf/1MB 上限，`data: [DONE]` 返 nil，无 [DONE] 关连接也算正常完成）；非 200 报「服务商返回 %d：%s」（error.message 优先，退化截 300 字）；normalizeBaseURL 补 /v1；ProxyURL→http.Transport.Proxy 出站代理（P1-5）。
> **P1 prompt.go 要点**：四模式系统模板（explain/intent/locate/flowmap）+ 流渲染行格式 `[#%d] flowId=%s | %s | %s %s | %s | %dms`；**双预算裁剪**（流数上限 50 条 StartedAt 降序 + 单侧 body 8KB 截断标注「…（已截断，原始 N KB）」+ 总预算 64KB/侧贪心丢最老整流，二进制 `[binary, N bytes]` 计 0 字节）；脱敏（默认开）：头=白名单 8 固定头+cookie（送名字、值强制 [REDACTED]）+x-* 前缀+名称含 auth/token/key/sign，Authorization/Proxy-Authorization 保 scheme；正文=JWT 整段→[REDACTED]、凭据键（含 password/token/secret/authorization/apikey 等）→`"${1}":"***"`、手机号前3+****+后4（11 位窗口两侧必邻数字不误伤 21 位订单号，AC10 测试固化）；文末 ```json 块解析 `lastJSONBlock`（多块取最后，大小写语言标签，支持内联形态）。ExtractIntents/ExtractMatches 过滤幻觉 flowId（无白名单时不过滤）、缺 seq/rank 补数组序、重复后覆盖前。
> **P1 三处关键修复（写测试揪出）**：①`lastJSONBlock` 内联形态缺陷——```` ```json{...}``` ```` 无换行时原 lang 判定含 JSON 体必失败，改 `HasPrefix(ToLower(rest),"json")`+第 5 字符∈`{' ','\t','\n','\r','{'`，start=i+3+4 再跳一换行；②`headerAllowed` 补 `|| lk=="cookie"`——否则 cookie 头整体被过滤、isSensitiveHeader 的 cookie 分支成死代码，违背「送名字不送值」；③**net/http server 陷阱（普适沉淀）**：POST 带 body 且 handler 未读完时 server 不启动 background read（断连监测），客户端断开后 `r.Context().Done()` 永不触发→httptest handler 挂死→`srv.Close()` 死锁。测试解法：handler 先 `io.Copy(io.Discard, r.Body)` + done channel select 兜底 + 断言后 close(done)。下一步 P2：ctlapi 三接口+StreamAIChat 编排+并发闸门+Wails 事件桥+persist.LoadFlowsByIDs。
> **2026-09-10 P2 ctlapi/绑定层完成**（go vet 零输出 + go test ./... 全绿 14 包；curl 经 Bearer 实测 9 项全过，含 400/409 路径）：persist 新增 `LoadFlowsByIDs`（IN 批查，缺失 ID 静默跳过）；ctlapi Service 接口 +4 方法（GetAIConfig/SaveAIConfig/AITestConnection/StreamAIChat）+ 三路由 `/api/v1/ai/config`(GET|POST)、`/ai/test`(POST)、`/ai/chat`(POST SSE)；**handleAIChat started 标志分界**：首 emit 前同步 err→aiErrCode JSON（ErrAIConflict→409/ErrAIBadReq→400/其余 500），首 emit 后只发 error 帧；**readBody 不校验 JSON**（只 LimitReader 64KB 原样透传 RawMessage，语法校验下沉实现层——烂 JSON 打到宽松实现得 200 属设计非 bug）；app 侧 `SaveAIConfigPatch` 部分更新（出现的字段才覆盖；apiKey 空串=保持/`__clear__`=清空/未知字段→ErrAIBadReq）、AITestConnection 不落盘、`ai_bridge.go` StreamAIChat 编排（LoadFlowsByIDs→StartedAt 降序截断→裁剪脱敏→ai.Client.Stream→intent/match 走文末 JSON 块解析逐帧 emit）、`aiGateAcquire` CAS 闸门（全实例并发 1，冲突路径不起 goroutine）、bindings_ai.go 五绑定（TestAIConnection/AIChatStart/AIChatStop/GetAIConfigApp/SaveAIConfigApp，全单结构 DTO+error）、dto.go `AIConfigView{hasApiKey,apiKeyMasked,keyMissingWarn}`（密钥不进 SettingsView 全量 DTO）；SaveSettings warnings 追加 WarnNoKey（AI 开且非 ollama 无 key）。单测：ctlapi 路由/405/401/400/409 + app 事件桥闸门互斥（**ctx=nil 成功路径会触发 wails runtime panic，只测冲突/Stop 幂等/手工注册，GUI 全链路留 P3/P4**）。wailsjs 三文件手动同步七新 class（app.AIChatHandle/AIConfigView/AITestResult、ctlapi.AIChatOptions/AIChatRequest、settings.AIConfig/AISettings）+ SettingsView.ai 字段 + App.d.ts/App.js 五方法（ASCII 序）。**curl 冒烟三坑**：①真实前缀 /api/v1（单测 doRequest 内拼，直连必须带）；②PS5.1 curl.exe `-d "{\"k\":...}"` 的 `\"` 经 native 传参 body 含字面反斜杠→必须写临时文件 + `--data-binary "@文件"`；③Start-Job 启动开销 >200ms，409 并发验证前台延后 ~1s 再发（后台先持 CAS 1.5s 窗口）。临时 cmd/ctldemo（nil 接口嵌入只覆 4 方法）验证后已删。
> **2026-09-11 P2 代码审计与修复**（子代理只读审计：无高危，2 中危 4 低危，用户拍板全部修复；go vet 零输出 + go test ./... 全绿）：①**中1 解析错误错误码**——SaveAIConfigPatch 坏 JSON/坏字段类型原返回裸 error 被 aiErrCode 映射 500，现统一 `fmt.Errorf("%w: ...", ctlapi.ErrAIBadReq, ...)` → 400（回归断言入 TestSaveAIConfigPatchSentinel；HTTP 层 fakeService 宽松返 200 属已知设计，400 断言放 app 层）；②**中2 兜底固化违 P0-7 红线**——合并基准原为 `g.AI.WithDefaults()` 整体落盘，改回原始 `cfg := g.AI`（落盘保留哨兵 0 值），校验改 `cfg.WithDefaults().Validate()`（哨兵按默认生效），回传视图 `aiConfigView(g, cfg.WithDefaults())` 与 GET（本就传 WithDefaults）语义一致；③低1 同②解——显式 `temperature:0` 经兜底副本校验放行（Validate 原 `Temperature < 0.1` 拒绝，与 0 哨兵语义矛盾）；④低2 SSE 首帧头补 `X-Accel-Buffering: no`（契约 §5.5，本机直连无实际影响）；⑤低3 done 帧 `"truncated": br.Truncated` 取真实值（原硬编码 false 与 meta 帧不一致）；⑥低4 流完成后 `sctx.Err() != nil` 提前 return——停止/断连恰在完成边界时静默收尾，不发残余 intent/match/done 帧（emitSafe 仅 emit 失败置 aborted，EventsEmit 恒 nil 无法中断）。回归测试：TestSaveAIConfigPatchDefaultsNotPersisted（未初始化 patch 兜底不固化落盘+视图生效值；显式 temperature:0 落盘保 0 视图显 0.3）+ TestAIChatSyncErrors meta 后错误脚本补发 error 帧并断言帧序 meta→error（error 帧由实现层发出，契约 §5.5）。另 gofmt 3 文件（ctlapi/ai.go、ctlapi/server.go、persist/tags.go 行尾注释对齐）+ ai.go 注释笔误（intent/locate/flowmap）。
> **2026-09-11 智谱 v4 端点兼容修复（P3 前置，设计稿 v2.1→v2.2 + 计划 P0-5/P0-6 措辞同步）**：NormalizeAIBaseURL（settings.go）与 normalizeBaseURL（ai/client.go 镜像）原「非 /v1 结尾一律补 /v1」会把智谱 `open.bigmodel.cn/api/paas/v4` 错拼成 `/v4/v1`，放宽为**末段 `/v<纯数字>` 保留不补**（`/v1`/`/v4` 均命中；`/v1beta` 等带后缀仍补）；新增 6 条测试用例（settings_test.go 4 + client_test.go 2），go vet/test/build 全绿。先改设计稿再改代码（§5.1 端点约定）。
> **2026-09-11 P3 主窗 AI 设置面板完成**（前端三文件；IDE 诊断×3 零错误 + npm build exit 0 + grep dist 特征「AI 分析/服务商预设/测试连接」6 处 + go build ./... 全绿）：①新建 `pages/settings/AiTab.vue`（约 230 行）——九项表单：启用 n-switch / 七服务商预设 n-select 受控（openai/deepseek/moonshot/zhipu/qwen/ollama/custom，选中回填 BaseURL+model，custom 保留当前输入不清空）/BaseURL/API Key（密码框 **keyInput 独立存取**：切 tab 重取掩码回显空、保存走 SaveAIConfigApp 哨兵、popconfirm 确认清空 `__clear__`）/模型/温度 slider 0.1–2/超时 10–600/流数 1–100/预算 8–256/脱敏受控 popconfirm；「测试连接」走 TestAIConnection 构造 `{...props.form.ai, apiKey: keyInput}` 内联 n-alert 显结果；保存=SaveSettings 全量合并不动已存 key。②SettingsPanel.vue 挂全局 tab（ADB 之后、解密 pane 之前，不进 PROJECT_TABS，effectiveTab 无需改）+ **emptyForm/loadSettings 双兜底**（SettingsView.ai 非可选，老配置缺 ai 字段时给九项默认，防 AiTab 运行时崩）。③mocks/app.ts 三桩对齐 Go 契约（先读 bindings_ai.go/ai_bridge.go 核验后写桩）：state.aiKey 内存态；GetAIConfigApp/SaveAIConfigApp 投影 hasApiKey/apiKeyMasked/keyMissingWarn，掩码=空返""、≤8 返"****"、否则 `****`+尾 4 位（与 maskAPIKey 逐字对齐），哨兵=空串保持/`__clear__` 清除/传值换 key；TestAIConnection delay(400)，url 含 error 或空返 ok:false；`aiView()` helper 复用投影。遗留：P4 复盘页 AI 四模式（ReviewAiPanel 等，待用户确认启动）。
> **2026-09-11 P3 代码审计与修复**（子代理只读审计 2608062..20aad3c：无高危中危，5 低危用户拍板全修；go vet 零输出 + app/settings/ai 三包测试全绿 + npm build 绿）：①低1 纯空白 key 三形态对齐「空白=保持」——核验 AiTab 守卫本就 `k=keyInput.trim()`（前端无漏洞，审计初报此处误判），Go SaveAIConfigPatch default 分支改 TrimSpace 后非空才存（原空白串 trim 成空会清掉已存 key），TestSaveAIConfigPatchSentinel 补 `{"apiKey":"   "}` 保持断言；②低2 mock keyMissingWarn 对齐 Go WarnNoKey 触发条件（enabled+无 key+非 ollama 才告警，读 state.settings?.ai）；③低3 mock TestAIConnection BaseURL/model 缺失改 throw「请先配置 AI 接口地址与模型」对齐 aiProbe 哨兵 reject（原返 ok:false 卡片，预览看不到错误分支）；④低4 `/v1beta` 非纯数字版本段边界补 2 测试断言（settings_test/client_test 各 1）；⑤低5 文档漂移——设计稿 §六第 2 项补「custom 不回填保留当前输入」，CONTEXT.md P3 记录「custom 清空」更正为「保留当前输入」（代码本就保留，记忆写反）。
> **2026-09-11 P4 复盘页 AI 四模式完成**（npm build exit 0 + IDE 诊断零错误 + grep dist 特征「AI 分析/标注意图」+ FlowTable 主窗回归 + wailsjs 无脏 diff，全绿；GUI 实测待用户）：新建 `review/ai-md.ts`（marked+DOMPurify，**全项目 v-html 唯一出口**）+ `review/components/ReviewAiPanel.vue`（约 520 行）+ `ReviewIntentBar.vue` + `review/useIntents.ts`（会话 Map<flowId,IntentResult>，不入库刷新重算）。四模式 tabs explain/intent/locate/flowmap；Phase 状态机 idle→streaming→done/stopped/error；**delta 50ms 节流**（非响应式 mdBuf + setTimeout 合帧）；onFrame 首行迟到帧守卫（phase!=='streaming' 直接丢弃）；locate 展示按文末 json 块标记 lastIndexOf 截断（只渲染叙述、不渲染结构化 JSON）；intent tab 不渲染 Markdown 区只渲染勾选回填摘要；**正文开关 OR 语义**（勾选或开关任一为真送正文，explain/flowmap 无开关恒全量）。抽屉 absolute 于 `.review-root`（position:relative）内 560px、z-index 30，常驻 DOM + `.open` class 切 transform translateX(104%)→0 + visibility，0.28s cubic-bezier（不用 n-drawer）。**explain 自动开始三路触发收敛**（show watch / flowId watch / loadCfg 完成回调）由 requestStart 首行 streaming 防重入守卫吞双触发；`requestStart(auto)` 遇 redact off 直接放弃（隐私链：首次 n-modal 告知 localStorage `prismproxy:review-ai-notice-v1` → redact off 每次 popconfirm → 底部外发 host 小字）。空态引导：http 形态 `openSettingsAI()` 走 POST /ui/settings{tab:'ai'} 真唤起主窗设置抽屉（demo/wails 静态提示）。`useIntents.runIntent` 第 5 参 `opts?: { includeReqBody?: boolean }` 透传（ReviewIntentBar 不传向后兼容）。色卡 k-ai-low（低置信暗黄斜体）/k-ai-needs-body（品牌紫）/k-ai-conf-high（绿）/k-ai-conf-med（黄），禁内联色值。Esc 关抽屉排除 INPUT/TEXTAREA/isContentEditable/通知显示中。接线归并 ReviewPage.vue（无独立 ReviewApp）：顶栏 AI 按钮 + 勾选批量「标注意图（N）」+ ReviewDetail「AI 解读」+ intent 摘要条。**M13 全阶段至此完成**。
> **2026-09-11 P4 最终代码审计与修复**（code-audit-specialist 审 3c69e48：1 高 3 中 3 低，用户拍板全部修复；npm build 绿（ReviewPage 共享 CSS chunk 含 AI 样式）；前端侧提交 c21bb4a，8 文件 +422/-402）：①**H1 高危·双入口构建图谱**——主窗 index.html 只挂 /src/main.ts，P4 AI 样式全放 review.css（仅独立入口 review/main.ts import），主窗内嵌形态（主要交付形态）AI 样式全缺。修复：拆 `review/ai.css` 组件级文件，由两形态公共祖先 **ReviewPage.vue** import（单点覆盖双形态）；review.css 精简为独立入口底座（386→37 行）；教训：**多入口 Vite 项目组件样式不得依赖入口级 css，须由共用祖先组件 import 进共享 chunk**。②M1 面板运行竞态——`runSeq` 运行令牌：异步续体（done/error 状态迁移、finally 句柄回收 `if (abortCtl === ctl)`）与迟到帧（`frameOf` 闭包运行级过滤）均校验 `seq === runSeq`，旧 run 停止后的 reject 不转 error 态。③M2 卸载清理——onBeforeUnmount 中 `stop()`（主窗切走复盘视图 v-if 卸载时中止在跑流防隐私外发）+ `removeEventListener('keydown', onEsc)`（跨挂载泄漏）。④M3 error 帧契约统一——http.ts SSE 帧分发抽 `dispatch()` 助手，error 帧 `throw new ApiError('http', msg)` 与 wails 层 reject 同契约（单流重析传空 onFrame 也能见错误）。⑤L1 wails.ts 空句柄兜底 throw（防桥接帧永不匹配 Promise 挂死）。⑥L2 useIntents 死代码清理（analyzing 只写无读/clear 零调用/try-finally 空壳，runIntent 简化直 await）。⑦L3 locate 显示缓冲 ```json 截断改 `matchAll(/```json/gi)` 取最后匹配（大小写不敏感对齐 Go lastJSONBlock；弃 toLowerCase 方案防 Unicode 长度变化错位）。
> **2026-09-11 P4 审计修复二轮（复审 c21bb4a + 4 低危全修，提交 01b50a6）**：code-audit-specialist 复审七项修复逐一验证通过，另发现 4 低危用户拍板全修：**P1 intent 帧守卫闭环**——useIntents.runIntent 原内部代写 upsert（在面板 runSeq 守卫之外），旧任务停止/重开后迟到 intent 帧会污染共享缓存；改「**谁消费帧谁守卫**」：runIntent 不再代写缓存，intent 帧写入移交调用方——面板在 runSeq+phase 双守卫的 onFrame case 'intent' 中回填、摘要条 onReanalyze 自吞 intent 帧。**P2 合帧定时器清理**——stop() 置 stopped 后补 flushRender()（清 50ms renderTimer+落盘最后一批 delta），onBeforeUnmount 再兜底一次（error 态卸载时 stop 早退、定时器可能仍挂）。**P3 demo analyze 契约**——三实现隐含契约「analyze resolve = 流已结束」，demo 原无 await 立即 resolve 致面板 await 抢跑置 done、后续定时器帧全被迟到守卫丢弃；改定时器段包进 new Promise，resolve 仅在 done 帧后 / abort 时 clearAll+resolve（与 http/wails 静默收尾口径一致）。**P4 跨组件互斥**——面板批量标注与摘要条单条重析并发写同一 intents Map（后完成者胜），useIntents 模块级 busy ref 单例互斥，busy 时 throw ApiError「已有意图标注任务进行中」；IntentBar 按钮 disabled + 面板 intent startDisabled 含 busy（仅互斥不做进度共享）。验证：npm build 绿 + IDE 诊断空 + grep dist 特征三重。
> **2026-09-11 P4 增强右下角调用日志抽层 + AI 分析中动画**（npm build 绿 + IDE 诊断零错误 + grep dist 特征「AI 分析中/调用日志/ai-logsheet」10 处）：①ReviewAiPanel footer 改常驻 flex 行（原 v-if="cfgOk"）：左外发告知 + 右 26px 日志按钮（CLI 风 svg 图标，streaming 时静态紫点提示）；点击展开 `.ai-logsheet` 抽层（absolute bottom:35px 恰贴 footer 上缘，height min(58%,430px)，translateY(108%)→0 同款 cubic-bezier 滑入），n-tabs segment 双 tab「对话内容」（`<pre>` 全量原始输出=rendered 节流值，非 locate 截断视图）/「调用日志」（帧级采集：start/meta/delta 首帧/intent/match/error/done/stop，每 run 独立 resetRun 清空，上限 500 条 shift，时间戳 HH:MM:SS tabular-nums，类型色卡 .lk-* 紫灰绿黄红）；打开时若在流式自动切对话 tab，watch 日志/对话长度自动滚底。②AI 分析中动画双反馈：动作区 `.ai-live` pill（3 圆点 stagger 弹跳 + 品牌紫→蓝流动渐变文字）插在停止按钮与 meta 小字间；内容区 `.ai-skel` 骨架微光（pendingStream = streaming 且 displayMd 为空时显示 5 横条 shimmer）。静态线索齐备（紫底/点色/骨架条），动画非唯一反馈；日志消息全部 script 内拼接防模板 `}}` 冲突。
> **2026-09-11 P4 增强（日志抽层+动画）代码审计**（code-audit-specialist 只读审计，主体=ReviewAiPanel.vue+ai.css，entries 波次概览）：无🔴。🟠×3：①日志 500 条上限后 push+shift 长度恒定，`watch(() => logs.value.length)` 不再触发→日志 tab 自动滚底停摆；且 `:key="i"` 索引 key+shift 头删=每次追加全列表 patch（AiTab entryKey 稳定 key 是同仓库正确姿势；修法：logSeq 自增 id 作 key，watch 源改末条 id）；②强制滚底 `scrollTop=scrollHeight` 无近底部判定，流式 delta 50ms 节流高频触发会把上翻用户持续拉回底部（修法：`scrollHeight-scrollTop-clientHeight<40` 才滚）；③**entries 波次 M13 决策漂移（本行即拍板留痕）**——AISettings.Entries 携带条目明文 apiKey 随主窗设置表单往返（AiTab v-model 编辑+SaveSettings 全量落盘），实质突破上方 L180 决策原文「apiKey 不进 SettingsView 全量 DTO」；代码自洽：顶层 key 仍不经表单链路、HTTP 面 aiConfigView→aiEntriesView 仅 hasKey 永不回原文 key、settings.json 0600 同级安全、设计意图=条目卡片逐条编辑 key。本记录视为该演进的确认，后续新决策描述以此为准。🟢×5：nextTick 滚底回调不复查 logOpen 时效（无危害）、aria-live 挂 v-if 动态 .ai-live 节点读屏无效（pill 有静态视觉线索）、Esc 不先收 logsheet、#b57edc×7 可抽 CSS 变量、.ai-logsheet bottom:35px 与 footer 高度（26+8+1）硬编码耦合。通过项：runSeq 防竞态未回归/error 帧经 http+wails 先 onEvent 后 throw|reject 可采集（三实现契约一致含 demo 定时器序列）/八类帧全覆盖/模板 }} 红线/XSS 插值防线/节流定时器清理/resetRun 清日志/ai.css import 回归（ReviewPage L375）/bottom:35px 精确贴合 footer。第二波其余通过：SaveAIConfigPatch key 补丁最后生效（entries+apiKey 同请求防静默丢失）、NormalizeAIEntries 归一化、SyncAICurrent/SyncKeyToCurrent 闭环、旧配置单顶层迁移、ListModels 1MB 限读。结论：可合入，两个🟠（滚底停摆/拉扯）建议跟进修复。
> **2026-09-11 自托管首块超时修复（设计稿 v2.2→v2.3）**：用户报 Ollama AI 分析「测试连接通过但批量标注 30s 超时」。实测根因：**首块看门狗 30s 上限对本地 Ollama 过紧**——首 token 前需冷加载+全量 prompt prefill，64KB prompt 温机 TTFB≈17s（该模型 context_length 8192，Ollama 静默截断超长 prompt，影响分析质量），冷启动叠加可破 30s；测试连接小 prompt TTFB≈0.2s 探不出差异（非 bug，属预期）。修复：`firstChunkTimeout()` 按部署形态区分——**自托管（未配 APIKey）取 Timeout 全额**（用户可调 10–600s，默认 120s），**云端维持 min(30s, Timeout/4)** 快速判死；`Timeout<=0` 兜底 30s 不变；新增 `firstChunkTimeoutErr()` 统一两处超时文案，自托管追加「本地模型冷启动/长文本推理可能较慢，可调大设置中的超时时间后重试」。测试：TestStreamFirstChunkTimeout 固化云端语义（补 APIKey:"k"），新增 TestStreamFirstChunkTimeoutSelfHosted；ai 包全绿 + go vet/build 全绿，exe 已重建（用户需重启生效）。坏模型名排查沉淀：Ollama 对不存在模型秒回 404 error.message（非超时），超时≈模型名有效但 prefill 慢。**同日代码审计（双子代理交叉验证，3 低危全修）**：①注释漂移 2 处同步 v2.3 口径（client.go Config.Timeout 字段注释 + Stream doc 注释，原无条件写 min(30s, Timeout/4)）；②firstChunkTimeoutErr 自托管文案补「若持续超时，请确认服务已启动、地址可达」（验证确认：Ollama 未启动走 refused 走「连接服务商失败」分支、不受影响，误导窗口仅服务假死/远程 DROP）；③settings.go TimeoutSec 注释旧账订正——「流式下为首块+整体上限」中「整体上限」自始无代码依据（http.Client.Timeout 从未设置、外层 ctx 仅 WithCancel，首帧后流不限时属设计），改为「流式下=首块看门狗阈值（自托管全额/云端 min(30s, Timeout/4)），首帧后不再限时」。gofmt 教训：注释行 `//（` 必须写 `// （`（gofmt 判格式错）。全绿重建。
> **2026-09-11 P4 增强（日志抽层+动画）审计修复八项**（🟠×2+🟢×5 全修，用户拍板；npm build 绿 + IDE 诊断零错误 + grep dist 特征「上一轮/ai-sr-live/ai-log-prev/--ai-brand/--ai-foot-h」）：①**A1 日志 500 上限停摆+索引 key**——LogEntry 加自增 `id`（模块级 logSeq 跨 run 单调不重置，prevLogs 与 logs 两数组 key 全局唯一），`:key` 从索引改 `l.id`（shift 头删不再全列表 patch）；watch 源从 `logs.length` 改「末条 id」（上限后长度恒定仍能触发）。②**B1+C1 滚底拆双 watch**——开合/切 tab 属导航意图无条件滚底；流式追加（末条 id/convText）仅视口近底部（`scrollHeight-scrollTop-clientHeight<40`）才跟随，尊重上翻回看；nextTick 回调均复查 logOpen 时效。③**R1 读屏播报**——pill 保留 v-if 纯视觉，新增常驻 sr-only live region（`.ai-sr-live` aria-live=polite，文本随 phase 切换）：live region 须先于消息常驻无障碍树才被播报，v-if 动态插入无效；未采纳审计原建议「pill 常驻 DOM+class 显隐」——常驻 pill 占布局致 idle 空隙，且 display:none/visibility 一样移出无障碍树。④**Z1 Esc 分层**——logOpen 时 Esc 先收抽层，再按一次才关面板。⑤**G1 色值收口**——ai.css 7 处 `#b57edc` 抽 `--ai-brand`（+`--ai-brand-2` 流动渐变），变量定义在 ai.css 自身 `:root`（组件样式不得依赖入口级 css，H1 教训）。⑥**G2 footer 高度解耦**——`--ai-foot-h: 35px` 两处共用（.ai-foot 显式 `box-sizing:border-box; height:var(--ai-foot-h)`，.ai-logsheet `bottom:var(--ai-foot-h)`）。⑦**G3 上一轮日志归档**——resetRun 时若 logs 非空移入 `prevLogs` 供回看（stop 后当前轮日志仍即时可见，归档延至新 run 启动，比审计建议的 stop 时归档更优）；空态判定改 `!logs.length && !prevLogs.length`，新增 `.ai-log-prev` 开关行（chevron 随 prevOpen 翻转，`border-bottom: 1px dashed`）+ `.is-prev` 0.55 弱化。
> **2026-09-11 M13 GUI 实测收官（v2 分支，真实 Ollama qwen3.5:4b @ 192.168.0.69:11434/v1）**：demo 形态（vite dev：隐私告知/脱敏开关/抽屉动画/Esc/四模式流式渲染/意图标注回填与摘要条互斥）+ 主窗 exe 形态全过。**两处代码修复（GUI 实测揪出）**：①**IntentItem/MatchItem 缺 json tag**——Go 结构体无 tag 经 Wails 序列化为 PascalCase，前端按小驼峰取字段全空（intent 回填零效果）；prompt.go 两结构体补 `json:"..."` tag 修复。②**MatchItem 缺 method/url**——locate 匹配帧只回 flowId/seq/rank，匹配卡片无「方法+路径」可显示；ai_bridge.go 在解析文末 JSON 块后按 flowId 从送审流回填 method/url 修复，「查看→」跳转联动验证通过。**四模式全链路结论**：explain/intent/locate/flowmap 全部正常（流式渲染、intent 逐条回填、locate 匹配卡片+跳转、flowmap「业务链路分析」结构化输出：标题+概述+步骤三段（接口调用/作用/依据嵌套证据）+证据不足说明+总结）；4B 模型单流流式约 30–50s（自托管首块超时修复生效）；模型遵循脱敏（Cookie 值 [REDACTED]、User-Agent 保留）与证据标注规范（推测/证据充分/证据不足三类措辞）；单流预算场景显示「送审 1/1 条 · 预算 50 流/64KB」正确。分析完成态特征：edit 恢复、开始分析 enabled、停止按钮消失、「AI 分析中」消失。
- **范围**：复盘页四模式（explain 单接口解读 / **intent 接口意图批量标注** / locate 自然语言定位接口 / flowmap 业务链路整理）+ 主窗设置抽屉新增全局「AI 分析」tab；一期不做主窗实时入口、结果不落库、不引 mermaid、不做 `cli ai`。
- **intent 模式（v2 新增，用户拍板：批量标注 + 详情顶部摘要条展示）**：对勾选流（FlowTable 新增**可选**勾选列，仅复盘开启，主窗零影响）或无勾选时当前已加载页**单次请求**标整批（非每流一次，控成本）；默认只送元数据+关键头不送正文，模型判不准置 `needsBody:true` 由用户手动开正文重析（不做服务端自动二轮）；SSE 新增 `intent` 事件（{flowId,seq,intent,confidence,needsBody}，复用 locate 文末 JSON 块解析路线 A）逐条回填；前端 `review/useIntents.ts` 会话级 `Map<flowId,IntentResult>`（不入库，刷新重算）；新增 `review/ReviewIntentBar.vue` 挂 ReviewDetail 顶部（有结果才占位，「重新分析」单流重跑/「完整解读」跳 explain）；翻页/切标签/切 scope 清空勾选。一期 intent 无「全部匹配项」服务端扩选。
- **AI 配置归属（关键决策，2026-09-11 随 entries 波次演进修订，原表述「apiKey 不进 SettingsView 全量 DTO」已收窄）**：全局唯一一份 OpenAI 兼容配置（baseURL/apiKey/model/temperature/timeout/maxFlows/maxKb/redact + entries 多条目），挂 `settings.GlobalSettings.AI`（跨项目，与 ADB/持久化同类）；**顶层 apiKey 不经主窗设置表单链路**——独立 `GET/POST /api/v1/ai/config`（GET 只回 hasApiKey+掩码；POST 空串=保持原 key、`__clear__`=清空），AiTab keyInput 独立存取、SaveSettings 合并不动已存 key；**entries 条目级 apiKey 随主窗设置表单往返**（AiTab 条目卡片 v-model 编辑 + SaveSettings 全量落盘，2026-09-11 拍板确认的演进，设计意图=条目卡片逐条编辑 key，见上方审计记录拍板留痕）；HTTP 面 aiConfigView→aiEntriesView 仅回 hasKey 永不回原文 key；apiKey 仅存 settings.json（0600）。
- **后端**：新包 `internal/ai`（零外部依赖，net/http + SSE 解析；Stream(ctx,msgs,onDelta)；首块超时 + ctx 整体兜底；出站复用 UpstreamMode 代理装配）；prompt.go 纯函数做四模式 Prompt/双预算裁剪（默认 50 流/64KB）/脱敏（Authorization/Cookie/JWT/密码键/手机号，默认开）。ctlapi 新增 `/api/v1/ai/config`、`/ai/test`（测试连接不落盘）、`/ai/chat`（SSE：自定义帧 meta/delta/intent/match/error/done，**fetch+ReadableStream 带 Bearer**，非 EventSource；全实例并发 1 否则 409；断连 ctx 取消即停上游）；Service 接口加方法同步 fakeService。intent/locate 结构化走单轮 + 文末 ```json 块解析（路线 A，tool-calling 留二期）。
- **前端**：主窗 `pages/settings/AiTab.vue`（全局 tab，不进 PROJECT_TABS；server.go UISettingsTabs 加 "ai"；测试连接走新 Wails 绑定 TestAIConnection，同步 wailsjs 三文件）；复盘新增 `review/components/ReviewAiPanel.vue`（自绘 560px 右抽屉，非 n-drawer）+ `review/ai-md.ts`（marked + dompurify，**AI HTML 不可信，v-html 仅此一处且必经净化**）；ReviewApi 加 analyze（Http SSE 解析 / Demo 本地定时器模拟流——intent demo 逐条发 intent 事件；与 compose 的 demo 禁用相反，AI demo 可玩）；入口：ReviewPage 顶栏 AI 按钮 + ReviewDetail 标题栏「AI 解读」+ 批量「标注意图（N）」+ 详情意图摘要条（无独立 ReviewApp，接线归并 ReviewPage.vue，见上方 P4 记录）；match 卡片点击选中流（一期仅当前已加载页）。新增 npm 依赖 marked、dompurify。
- **安全红线**：apiKey 仅存 config/settings.json（0600），不回传原值、不入 Prompt、不写日志；首次分析告知弹窗（数据外发到 baseURL host）；脱敏关闭每次 popconfirm。

> **2026-09-11 AI 面板 segment 切换器"进度条"修复（两轮，浏览器实测收官）**：用户报顶部四模式切换器"像进度条看不懂"，首轮误诊为 dark.mjs 配色缺陷（真实为次要因素），**真根因=naive-ui 2.40.4 normalizeSlots bug 导致标签文字根本没渲染**：`vue-jsx-vapor/vdom.mjs` 的 `normalizeSlots(slots)` 对任何非函数非对象输入（含 undefined）包装成 `{default: withCtx(() => [normalizeVNode(() => slots)])}` truthy slot（null/undefined → createComment `<!---->`）；自闭合 `<n-tab-pane :tab="..."/>` children=null 时 Tabs.mjs 传 `normalizeSlots(void 0)` → Tab 组件 `defaultSlot ? ...` 分支短路 → **props.tab 永不渲染，DOM 为 `<span class="n-tabs-tab__label"><!----></span>` 空，capsule 被压成 8px 高"进度条"**（对照：主窗 SettingsPanel/FlowDetailTabs 的 n-tab-pane 带子内容是对象原样返回故有字——line 型有字/segment 自闭合无字由此完全解释）。修复：ReviewAiPanel.vue 两处（顶部四模式 + 底部日志抽层）**自闭合 n-tab-pane 改 `n-tab`**（纯导航 tab，`__TAB__:true`，label 走 default slot 文字，Tabs.mjs 对 tabChildren 直接复用原 vnode 完全绕开 bug 路径；segment 模式内容本就在 ai-body 自绘，语义更贴切）。次因=naive-ui 把主题变量以内联 style 挂组件根（`--n-color-segment` 等），**普通 CSS 规则盖不过内联，必须 `!important`**（样式表 !important > 内联 normal）：ai.css `.ai-tabs.n-tabs, .ai-log-tabs.n-tabs` 六变量加 !important——轨道 0.06 白、选中胶囊品牌紫 rgba(181,126,220,0.25)、文字 0.55/0.85 白/激活 #fff、激活字重 600。**验证红线再证：静态全绿 ≠ 生效**——首轮只改 CSS 变量未加 !important 且没发现文字未渲染，build+grep dist 全过但用户实测"还是一样的"；本轮起 dev server + browseruse 实测：两处 tabs labels 全渲染、点「定位」紫胶囊跟随、rail 35px/tab 29px/文字 21px、computed 变量 0.06/0.25/#fff/600 全中（内联 style 仍旧值但被 !important 压制属预期）。注：naive-ui 内部在 wrapper 首渲染一个 0×8 空 `.n-tabs-tab` 测量元素（w=0 不可见，无害）；segment 激活字重实际 500（naive-ui 内部另有规则压过变量），13px 下与 600 不可辨，未追。

> **2026-09-11 AI 日志抽层拖拽调高 + 高度缓存（浏览器实测五项全过）**：需求=AI 面板底部日志抽层（`.ai-logsheet`）支持拖拽调高并缓存高度值。实现（对齐复盘页 `.splitter` 列宽拖拽模式）：①ReviewAiPanel.vue——section 挂 `ref="logsheetEl"` + `:class="{dragging}"` + `:style="logHStyle"`，顶部新增 `.ai-log-grip` 把手（8px 高 ns-resize、::before 34×3 短横条 hover/拖拽中品牌紫，`@pointerdown="startLogDrag"` + `@dblclick="resetLogH"`）；②拖拽逻辑——pointerdown 记起点→`setPointerCapture` **try/catch 包裹**（合成事件无活动指针抛 NotFoundError）→window 级 pointermove/pointerup/pointercancel→`body.row-resizing` 全局光标（对齐既有 col-resizing）→pointerup 落盘；向上拖（clientY 减小）增高，`logH = clamp(startH + (startY - clientY), 200, drawerH-155)`；③缓存——key `prismproxy:review-ai-logsheet-h-v1`（遵循项目惯例），读写全 try/catch（存储不可用仅本会话生效）；logH=0 表示未自定义走 CSS 默认 `min(58%, 430px)`；④**钳制策略（关键设计）**——缓存值以内联 `height: min(<px>, calc(100% - 155px))` 生效，100%=绝对定位包含块（.ai-drawer）高度，**窗口变小时 CSS 自动就近钳制不溢出抽屉，无需 JS 响应 resize**；JS 拖拽侧同公式 `logMaxH() = drawerH - 155`（footer 35 + 正文可视保留 120）；⑤`.ai-logsheet.dragging { transition: none }`（默认 translateY 0.26s 滑入过渡会吃掉高度变化，必须关）+ user-select:none；双击把手 `resetLogH()` 清 key 回默认；onBeforeUnmount 拖拽中卸载兜底摘监听。**验证红线再证 + 新坑**：①Vue 响应式 DOM 更新是异步微任务——browser_evaluate 里同步 dispatchEvent 后立刻读 DOM **读不到新高度**（内联 style 仍 null），必须 wait 1s 后再读；②合成 PointerEvent（bubbles+pointerId+pointerType:mouse）驱动 window 级监听完整可行。实测五项全过：默认 431px（min(58%,430px)）→拖 150px→582 + localStorage "581"→刷新重开恢复 582→双击回 431 + key 清除→上拖 1000px 钳制 759（= drawerH 914 − 155，缓存 "759"）；测试后 localStorage 已清。

> **2026-09-12 AI 日志抽层拖拽审计修复 I1/I2/I3（I4 建议不修，浏览器实测全过）**：对拖拽调高功能 TRAE-code-review 审计出 4 问题，用户拍板修 I1/I2/I3。**I1 右键/中键可触发拖拽**——startLogDrag 首行加 `if (e.button !== 0) return`（仅左键）；实测合成 pointerdown(button:2)+move+up 序列完全无效（h 不变/style null/row-resizing 清/cached null），对照左键 button:0 正常拖拽。**I2 `155px` 与 `--ai-foot-h` 跨文件耦合**——footer 高单一事实源归 ai.css `:root { --ai-foot-h: 35px }`：logHStyle 内联改 `min(<px>, calc(100% - var(--ai-foot-h) - 120px))`（var 在内联 calc 中合法，从 :root 继承；LOG_H_RESERVE_EXTRA=120 常量），JS 侧 `logMaxH()` 运行时 `parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--ai-foot-h')) || 35` 回读，钳制数值 35+120=155 与旧公式完全等价（实测拖 1000px 仍钳 759）。**I3 拖拽中 Esc 不终止拖拽态**——onEsc 收抽层分支加 `if (logDragging.value) onLogDragEnd()`（先落盘当前钳制值+摘 window 监听+清 row-resizing 再收抽层）；实测拖拽中 dispatch Escape：row-resizing 移除、cached 落盘、后续 pointermove 不再改变高度（监听确摘）、抽层滑出视口（transform translateY 820.8 / top 940>914——**注意收起判定看 transform/rect.top 而非 display，.ai-logsheet 收起后 display 恒 flex，靠 translateY(108%) 滑出**）。I4（落盘值与渲染值差 1px border）建议不修，用户未选。构建 npm run build 绿 + IDE 诊断 0。**新教训：browser_evaluate IIFE 陷阱**——script 用 `(() => {...})()` 包裹会被引擎二次包装返回 undefined（skill 有明文警告），多语句也返回 undefined；**解法=逗号单表达式** `(v1 = x, v2 = y, ..., JSON.stringify({...}))` 配隐式全局变量。另：dblclick 后 0ms 立即拖拽会撞 height transition（起始 rect 仍旧值），非 bug 但测试时需 wait。

> **2026-09-12 P4 增强（对话 tab 四区：提问→提示词→思考→输出）**：AI 日志抽层「对话内容」tab 由单一模型原始输出扩为完整过程视图（npm build 绿 + IDE 诊断零错误 + grep dist 特征「ai-conv-fold/思考过程/提示词（实际送审）」+ go vet/test 全绿）。①**帧契约扩展**：ai_bridge.go meta 帧补 `system`/`user`（BuildResult 现成字段，已按配置脱敏，本地展示无外发）；ctlapi/ai.go 帧名注释同步；delta 帧 `reason`（推理模型 reasoning_content）后端本就分列 emit、前端原只取 text 丢弃——纯前端接住。设计稿 §5.3 示例与说明同步。②**ReviewAiPanel.vue conv tab 四区**：提问区（locate/flowmap 显示 question，explain/intent 无输入回显 scopeText）；提示词折叠区（meta.system/user + tabular-nums 字数，默认收起）；思考过程折叠区（reason 增量 50ms 合帧进 reasonBuf→reasoned；**自动开合三态**：思考增量先到自动展开 / 首个正文增量自动收起 / 用户手动开合置 reasonTouched 退出自动逻辑+pushReason 的 `!sawDelta` 守卫防正文后穿插思考误展开）；输出区（原 rendered）。③**合帧统一出口** scheduleRender()：pushDelta/pushReason 共用同一 50ms timer 同时落盘 rendered+reasoned，flushRender 同步双写；resetRun 清 reasonBuf/reasoned/两折叠态/reasonTouched；滚动跟随 watch 源加 reasonText。④**样式**（ai.css，经 ReviewPage 共享 chunk 双形态覆盖）：折叠体 `grid-template-rows 0fr→1fr` 过渡（可中断、无 JS 测高）+ chevron rotate 0.18s + 思考弱化 0.55 白 + live 品牌色。⑤demo.ts meta 补提示词桩（形态对齐 BuildPrompt）。

> **2026-09-12 批量截断/思考字段双 bug 修复**：①client.go sseChunk delta 补 `reasoning` 字段解析（Ollama ≥0.5 OpenAI 兼容端点把思考放 delta.reasoning；原先只认 reasoning_content → qwen3.5 思考模型增量全丢），reasoning_content 优先、reasoning 兜底；②sseChunk choices[0] 补 `finish_reason` 收集，Stream 签名改 `(finishReason, error)`（流内末帧生效），ai_bridge done 帧如实透传（空兜底 stop），不再硬编码 stop 掩盖 length 截断；③ReviewAiPanel.vue done 帧消费 finishReason=length → 日志追加「输出被截断…减小批量流数」提示（done 帧类型本就是 {finishReason,truncated} 无需改契约）。测试：TestStreamFrames 补双字段+finish_reason=stop 断言、新增 TestStreamFinishReasonLength；go build/vet/test 全绿 + npm build 绿 + 诊断零错误。注意：Ollama 运行时 ~8K ctx 截断本身是上游行为，PrismProxy 侧修复=不再静默（如实透传+前端告警）；批量规避仍按 ≤10 条/批。
> **2026-09-12 intent 批量标注加「单条轮询处理」开关（纯前端，npm build 绿 + 诊断零错误）**：ReviewAiPanel.vue 意图模式 opts 行新增复选框「单条轮询处理」（`singleMode` ref，会话级不持久化，与 includeReq 同类）。**实现关键**：①doStart 分支——勾选后不再一次 runIntent(全部 ids)，改 for 循环逐条 `runIntent(api,[id],itemFrame,…)`（每条独立 meta/预算/done，天然规避批量 prompt 红线）；②**done/error 帧语义降级是核心坑**——单条轮询下每条的 done 帧不能走批量 onFrame 的 done 分支（会置 phase='done' 终止整轮、后续条目帧被迟到守卫全吞），itemFrame 拦截：done→flushRender+log「第 i/N 条完成」（finishReason=length 照常告警）后 return；error→直接 return（http/wails 实现都是 onEvent 后再 throw，循环 catch 统一 `failed++`+log，不置 error 态、失败续跑）；③循环守卫 `seq!==runSeq||signal.aborted` 每条前检查（停止/重开/换模式立即退出），结束后 failed>0 补「成功 X/N · 失败 Y」汇总；④进度适配——metaText 勾选时改显「单条轮询 X/N 条」（phase!=='idle' 门控，否则 idle 就显示 0/N），intentPct 分母改 intentIds.length（单条 meta.total 恒 1 会让进度条逐条跳 100%）；⑤scopeText 动词随开关变「逐条分析/批量分析」。busy 互斥不受影响（逐条 await，间隙为微任务级）。**复审修复（同日，2 子代理交叉验证）**：①候选列表循环开始快照 `ids=[...intentIds.value]` + 新增 `singleTotal` ref（进度分母同步快照，resetRun 清零）——AI 抽屉无遮罩、列表复选框/翻页运行期不禁用，翻页清空 checkedIds 会使实时读 computed 静默膨胀为全视图错标未选流、筛选重置产生 undefined id；②全失败语义对齐批量：`failed>=total` 置 `phase='error'`+errMsg「单条轮询全部失败(0/N)」（部分失败仍 done+汇总日志）；③demo.ts 提示词桩 qhead 计数改用 slice 后 `shown.length`。npm build 绿 + 诊断零错误。

> **2026-09-12 F6 项目批量 intent 实测收官（真实流量 83 条回填，全 finish=stop）**：用 ctlapi `/ai/chat` mode=intent 脚本（`$env:TEMP\prism_intent_test.ps1`，参数 -Limit -Offset -Scope -BatchTag -TagId，PS5.1 纯 ASCII；SSE 帧统计产出 `$env:TEMP\prism_intent_batch_<tag>.log/.summary.json`）对 F6-保养资料等 5 个标签跑批量意图标注。**汇总**：A=10/10（79.7s）· B5=17/17（60.3s）· C=27/27（拆 C1/C2/C3 各 9 条，28.2/28.3/27.8s）· D=13/13（D1 33.2s+D2 19.2s）· E=5/5（17.1s）· F=7/7（23.3s）· G=4/4（13.0s）。quality：conf 高为主（high≈85%），needsBody 合理（上报埋点/昵称变更/选车等判不准的才置 true），9 条/批的 prompt eval + 生成全程 13–33s。
> **三个产品侧 bug（本轮实测确认；①思考字段 ③done 硬编码已修复见上条，②Ollama 运行时 ctx 为上游行为只能透传告警+拆批规避）**：①Ollama 0.33.2 的 OpenAI 端点把思考放 `reasoning` 字段，PrismProxy client.go 只解析 `reasoning_content` → qwen3.5 思考模式下 delta 全丢（内容 0）；②**Ollama 运行时 ctx 预算 ≈8K tokens**（远小于 qwen3.5:4b 标称 262144，那是模型上限非运行时值）——input+思考+输出合计超预算 → finish=length 静默截断、content=0，大批量必踩；③ai_bridge.go done 帧硬编码 `"stop"` 掩盖真实 finish_reason（truncated 已修，finishReason 仍失真）。**/no_think 软开关对 qwen3.5:4b 实证不可靠**（system 末尾/user 末尾双注入仍出 11–14K 字符思考，模拟 body 成功属概率性）——**唯一可靠解=Ollama 原生 `/api/chat` 的 `think:false` 硬开关**。**shim v4**（`$env:TEMP\nothink_shim\main.go`，127.0.0.1:11534）：OpenAI `/v1/chat/completions` SSE ↔ Ollama NDJSON 双向转换 + 强制 `think:false` + `num_ctx:32768`，非 chat 路径 ReverseProxy 透传；PrismProxy baseUrl 指向 shim 后 17 条批 60.3s 全回填。
> **settings 热切换实测链路（免重启）**：ctlapi PUT `/settings` 必须带 `rulesProject` 令牌（=当前项目 ID，否则「项目已切换」拒绝）；改 AI baseUrl 必须同时改顶层 `ai.baseUrl` 与 `entries[0].baseUrl`（`SyncAICurrent` 用 current 条目四字段覆盖顶层，entries 是决定性来源）；`NormalizeAIEntries` 保留条目 apiKey；PUT 后内存热应用（`*a.gcfg = g`），AI client 每请求新建读内存→立即生效。切换/恢复脚本 `$env:TEMP\prism_switch_shim.ps1`（参数 -NewBase，备份 `$env:TEMP\prism_settings_backup.json`）。已恢复原 baseUrl（192.168.0.69:11434/v1）。
> **批量 prompt 尺寸红线**：intent 单批打包 prompt 正比于流数，9 条≈5.5KB（安全）；27 条整批 16KB 在该 Ollama 上 94s 才出 done，首响应超时被判死——本轮实测报「30 秒内未收到首个响应」，说明**运行实例仍是 v2.3 修复前的旧 exe**（v2.3 已把自托管首块超时改为 Timeout 全额默认 120s，重建重启后 27 条整批应可过）+ 用户可把超时调大；稳妥批量策略=≤10 条/批。

> **2026-09-12 AI 面板左缘宽度拖拽 + 宽度缓存（npm build 绿 + IDE 诊断零错误 + grep dist 特征「ai-grip-x/ew-resizing/review-ai-panel-w-v1」命中 ReviewPage CSS+JS 两 chunk）**：需求=AI 分析面板左缘把手左右拖拽调宽并缓存。实现与日志抽层高度拖拽（L191/L193）完全同构：①ReviewAiPanel.vue aside 挂 `ref="drawerEl"` + `:class="{open, 'w-dragging'}"` + `:style="aiWStyle"`，作为首子元素新增 `.ai-grip-x` 把手（8px 宽 ew-resize、`::after` 竖线 hover/拖拽中品牌紫、`@pointerdown="startWDrag"` + `@dblclick="resetW"`）；②拖拽逻辑——仅左键 `e.button !== 0` 守卫（I1 对齐）→startX/startW 记起点→window 级 pointermove/up/cancel 三监听 + `body.ew-resizing` 全局光标 + setPointerCapture try/catch（合成事件无活动指针）→`aiW = clamp(startW + (startX - clientX), AI_W_MIN, aiMaxW())`（向左拖增宽）；③缓存——key `prismproxy:review-ai-panel-w-v1`，`aiW=0` 哨兵=未自定义走 CSS 默认 560px，读写全 try/catch；④**钳制策略**（与日志抽层同款就近钳制）——缓存值以内联 `width: min(<px>, calc(100% - 400px))` 生效（100%=absolute 包含块 .review-root 宽），窗口变小 CSS 自动钳制无需 JS 响应 resize；JS 侧 `aiMaxW() = drawerEl.parentElement.clientWidth - AI_W_RESERVE(400)`，`AI_W_MIN=420`（四模式 tabs+正文可读下限）；RESERVE 为脚本内常量无跨文件耦合（规避 I2 类问题）；⑤宽上限双保险：拖拽实时钳 + CSS 内联 min 兜底；⑥终止兜底——onEsc 分支先 `if (wDragging.value) onWDragEnd()`（Esc 语义继续走层级收合，I3 对齐）+ onBeforeUnmount 卸载兜底（摘监听/清光标/落盘）。**样式细节（ai.css，经 ReviewPage 共享 chunk 双形态覆盖）**：竖线 ::after 上下内缩 26px 避开 24px 圆角；左缘 8px 落在 header/body ≥16px 左 padding 内不挡内容；**`.ai-drawer.w-dragging` class 维持高亮**——pointer capture 下指针移出把手 :hover 必丢，须用 class 补偿；width 不在 .ai-drawer transition 属性内（transition 仅 transform/visibility）故拖拽无需关过渡。双击 resetW 清 key 回默认。

> **2026-09-12 usage 透传 + 单条轮询对话聊天气泡（go test 全绿 + npm build 绿 + IDE 诊断零错误）**：需求=单条轮询下每条流做成一对「消息发送→模型返回」聊天气泡、各自显示 token 与字数（根因：单条轮询 6 条流的 delta 全拼进同一 mdBuf/reasonBuf，meta.value 被逐条覆盖只剩最后一条提示词）。①**后端 usage 透传链路**：client.go 请求加 `stream_options.include_usage:true` → OpenAI 兼容端点末帧 choices 空数组仅带 usage → sseChunk 解析出 `Usage{PromptTokens,CompletionTokens}` → Stream 三返回值 `(finishReason, usage, err)`（零值 usage 不透出）→ ai_bridge done 帧条件附带 `usage` 字段（帧契约 `{finishReason,truncated,usage?}`，client_test 8 处调用点三接收+usage 帧断言）；demo.ts 故意不加 usage 桩（保留前端估算展示路径）。②**前端 turns 聊天模型（ReviewAiPanel.vue）**：`ConvTurn{system,user,reason,text,open,sysOpen,usrOpen,touched,finishReason,usage?}` + `convTurns` 数组；**核心机制=in-flight 轮次用 `reactive()` 代理推入数组后身份稳定**（Vue 代理缓存保证 `convTurns[i]===curTurn`，直接改代理属性即触发更新）；`beginTurn`（meta 帧开轮）/`closeTurn`（done/error/stop/unmount 全路径幂等关轮：清 renderTimer+落盘缓冲+清空 curTurn，flushRender 并入其中删除）；mdBuf/reasonBuf 仍作 50ms 合帧 in-flight 缓冲，flush 目标改写 curTurn；fullText=`convTurns.map(t=>t.text).join('\n\n')` 聚合替代单缓冲（locate ```json 截断逻辑作用于 fullText 行为不变，批量模式=单 turn 天然统一）；单条轮询循环 done 分支**先取 mdBuf.length 再 closeTurn**（closeTurn 清缓冲），error 帧被 itemFrame 拦截后 throw 进 catch / 外层 catch 网络中断两条路径均补 closeTurn。③**token 展示**：`estTokens` CJK(codePoint≥0x2e80)×0.7+其他×0.25 向上取整，`tokText` 真实 usage（usage.promptTokens=send 气泡、completionTokens=recv 气泡）无前缀精确显示、缺失时估算带 `≈` 前缀；send 气泡头=「消息发送 · system+user 字数 · prompt tok」，recv 气泡头=「模型返回 · reason+text 字数 · completion tok」。④**per-turn 自动开合**（与原全局三态同构）：思考增量先到自动展开（`!reasonBuf&&!touched&&!open`）、本轮首个正文增量自动收起（`!mdBuf&&!touched&&open`）、手动开合置 `touched` 退出自动逻辑；System/User 提示词各自折叠默认收起；finishReason=length 时 recv 气泡内暗黄截断提示。⑤**CSS**（ai.css）：`.ai-turn` 间 hairline 分隔、`.ai-bubble` fit-content 收缩+max-width 92%、send 右对齐品牌紫染色/recv 左对齐中性白叠加、同心圆角 12px、tabular-nums 计数。

## cURL 复制（BuildCurl binding / lib/curl.ts）
- **前端 lib/curl.ts 现行规则（2026-09-10 用户给 Chrome 152 样例拍板，只支持 cmd/bash，PowerShell 已删）**：形态=多行 DevTools 风，`curl --url <URL>` 首段（裸 curl 不加引号；URL 内 `{} []` 一律加 `\` 防 curl globbing），其后 `-X`/`-H`/`-b`/`--data-raw` 各一段，参数段 ≥3 用分隔符多行（cmd ` ^\n  ` / bash ` \\\n  `，两空格缩进），<3 单行空格连。方法推断=**有内联 body→POST 否则 GET，不一致才输出 -X**（body 被省略时推断 GET，故二进制/超限 POST 会显式补 `-X POST`，否则 curl 误发 GET）。cookie 头且值含 `=`→`-b`（无 `=` 回退 -H 防 curl 读 cookie 文件）；值纯空白→`-H 'k;'`。跳过头小写集合=connection/proxy-connection/keep-alive/transfer-encoding/host/content-length/**accept-encoding**；headers key ASCII 字典序（Go DTO 是 map，JSON 序本就如此，与浏览器样例序一致）；逐跳头代理层记录前已剥离（server.go removeHopHeaders），样例里的 Connection 我们无源数据。body 文本（无 NUL）且 ≤64KB 且未截断才内联，否则 bodyOmitted UI 提示。
- **cmd 转义=逐字节移植 DevTools escapeStringWin**（顺序不可换）：`\`→`\\`、`"`→`\"`，白名单 `[A-Za-z0-9\s_-:=+~/'.,?;*()]` 之外的 **ASCII 字符（仅 \x21-\x7e）** 前加 `^`（故 `"`→`^\^"`、`{`→`^{`、`|`→`^|`），后跟标识符的 `%`→`%^`，其余控制字符（含 TAB/DEL）→空格、换行→`^`+两个换行，整体 `^"..."^` 包裹。**与 DevTools 唯一刻意差异：非 ASCII（中文等）原样保留不加 ^ 也不替换空格**（替换会毁重放；现代 Windows Terminal/cmd 按活动代码页可粘贴）。bash=DevTools escapeStringPosix：含控制字符/`!`/`'` 时 ANSI-C `$'...'`（!→`\u0021`），否则普通单引号。
- **Go 侧 internal/capture/curl.go 已同步同套规则（2026-09-10 下午，go test/vet 全绿 + wails 增量构建过，未提交）**：`ShellCmd/ShellBash` 两常量（ShellPowerShell 已删，传 powershell 报错）；裸 `curl --url` 首参（旧 `shellExe()`/显式 curl.exe 已删）；方法推断基于内联 body、cookie -b、空头 k;、7 跳过头、key 字典序、≥3 段多行、--data-raw 全部对齐；`quoteCmd`/`quoteBash` 逐字节移植 DevTools（bash 按 **rune** 扫描，避免 UTF-8 续接字节 0x80-0x9f 被误判控制字符；body 直接用原始字节 string，非法 UTF-8 按 RuneError 处理，与前端 TextDecoder fatal:false 等价）；`isText` 由「前 1024 字节控制字符比例」收窄为**仅判 NUL**（对齐前端，纯控制字符 body 也内联）。默认 shell：HTTP `/flows/{id}/curl` 与 CLI `flows curl --shell` 均由 powershell 改为 **cmd**（server.go/cli.go 帮助与校验同步；ctlapi_test 默认断言改 cmd）。curl_test.go 全量重写（样例 `^{`/`^|`/`^\^"`、`^%^PATH^%`、白名单 ()、中文保留、cookie -b、空头、多行阈值、省略 body 保 -X POST）。mock mocks/app.ts BuildCurl 删 PS 死分支并简化为新形态。
- 教训：跨 shell 引用规则差异必须逐字实测（`\"` 只在 cmd 有效，PS 中直接闭合字符串）；**白名单必须以 DevTools 源码 + 用户真实样例双重核对（main 源码白名单不含 `()`，但 Chrome 152 样例 UA 括号无 `^`，以样例为准）**；用户贴回的命令可能被聊天框 markdown 渲染篡改，排查先本地逐字节比对。Git for Windows 精简版无 curl。

## 构建与验证
- 前端：`frontend/` 下 `npm run build`（vite build；无独立 type-check 脚本，用 IDE 诊断；**必须 grep dist 特征或诊断核对，vite 退出码 0 不代表可运行**）。
- Go 增量：`go build -tags desktop,production -trimpath -ldflags "-s -w" -o build\bin\PrismProxy.exe .`（项目根，保留 config；**-tags desktop,production 是 Wails v2 wails build 内部约定**——desktop=桌面平台、production=生产模式，项目源码无此条件编译但 GUI 实测缺 tags 直构 exe 行为异常）；**不要用 `wails build -clean`**（清 config/ca 换 CA）；改 Go 后 taskkill /F /IM PrismProxy.exe 重起。
- 一键 `build.bat`（项目根，全英文输出）：无参=release（-trimpath -webview2 embed -ldflags "-s -w"，约 16MB）；debug/nsis/upx/clean/help 可组合。clean 已保护 build/bin/config（move 到 _config_keep 再迁回）。构建前 tasklist 检测运行中 exe 报错退出。
- build.bat 两大 cmd 陷阱（已修）：**bat 必须 CRLF 行尾**（LF 导致解析错乱）；**rem 注释禁用 `&`/`|`**（会被当命令分隔符执行，曾误跑根目录残留旧 exe）——根目录绝不放 exe 产物。PS 管道时 wails stderr 被报 NativeCommandError 属误报，以 exit code 为准。
- **版本发布（v1.2 起，2026-09-10）**：版本号在 build.bat `VERSION=x.y.z`（发布前 bump，它只决定 zip 名不写进二进制）；tag 为 annotated（`git tag -a v1.x -F <UTF-8文件>`，中文信息走文件 `-F` 避 PS 编码）。**仓库从不入库二进制**（.gitignore 拦 *.exe/dist/build/bin），编译产物只作为 **GitHub Release 附件**。本机未装 gh CLI：用 `"protocol=https`nhost=github.com" | git credential fill` 取 GCM 缓存 token（40 位，含 repo 权限），PS5.1 调 `https://api.github.com/repos/<o>/<r>/releases` POST 建 Release，再 PUT/POST `https://uploads.github.com/.../releases/{id}/assets?name=` 传 zip（body 用 `[Text.Encoding]::UTF8.GetBytes` + `charset=utf-8` 保中文；PS5.1 Invoke-RestMethod 无 -SkipCertificateCheck，别加）。自动化跑 build.bat：Trae 禁 `cmd /c`，用 `Start-Process .\build.bat -Wait -PassThru -RedirectStandardInput <预建空文件>` 绕过末尾 pause 挂起并拿 ExitCode。发布前必须 taskkill 所有 PrismProxy.exe（GUI+headless，bat 检测到实例直接退出）。历史 tag：v1、v1.1、v1.2、v1.2.1。
- **2026-09-10 v1.2.1 发布实操**：①`Write` 工具写的 .ps1 脚本会带 BOM，`git credential fill` 读脚本内 here-string 首行被 BOM 污染报 "credential missing protocol field"——取 token 必须**内联命令**或用无 BOM 文件；②发布三件事顺序=bump VERSION 单独提交（f211761）→push main→`git tag -a -F` 中文文件→push tag→API 建 Release（id 386117933）→uploads.github.com 传 zip；③Release body 用 `[System.IO.File]::ReadAllText` + UTF8 body 保中文；zip 9.6MB（exe 21MB embed）。
- 代码风格：注释中文，Naive UI 组件按需 import；scoped 样式，深度选择器 `:deep()`。
- main 包必须留仓库根（go:embed frontend/dist、wails.json）；业务代码全在 internal/app（bindings_*.go 按职责拆分 + ctl_bridge/headless/session_*/webview2_*），导出 main 仅 NewApp/Startup/Shutdown/EnsureWebView2/RunHeadless；wails 绑定路径 wailsjs/go/app/App（包名从 main 改 app 后 13 处 import+23 处类型引用已同步）。
- WebView2 自检：启动前查注册表 `{F3017226-FE2A-4295-8BDF-00C3A9A7E4C5}` pv（HKLM WOW6432Node+原生+HKCU），缺失 MessageBox 引导 + rundll32 evergreen bootstrapper。
- 冒烟：独立 smoke/ 目录放 exe（配置落 smoke/config 不污染 build/bin）；headless `-addr 127.0.0.1:9099`；造流用 powershell.exe Invoke-WebRequest（**curl.exe 在快捷忽略进程名单内，经代理流量被过滤不入列表**；区分进程归因可 curl.exe→curl.exe、IWR→powershell.exe）；GUI 验证前先 taskkill 所有实例（另一实例占 9090+9595 会让新 GUI 两端口都不监听）。

## GUI 自动化（MCP Computer Use 操作 WebView2）
- WebView 内容无 a11y 节点于主窗桥接前；桥接后按钮/输入有**独立 id，click id 直接命中**（比 element_id="1"+坐标可靠）；**set_value 是输入首选**（触发 v-model 同步）。
- **每个 MCP 动作（含 set_value/set_focus/RunCommand 外部扰动后）消耗 UI tree revision**，下一步前必须重新 get_app_state，否则 "No cached UI revision"/"Unknown element_index"；id 全量重排不可跨步骤缓存；`diffMode="no-change"` 时 id 不变可复用；流量列表大撑爆 a11y 树→先清空。
- n-select 浮层失焦自动收起：焦点在别的进程时先 perform_action set_focus；`perform_action expand` 对 n-select 不支持，用 set_focus+click。
- 原生文件对话框是同 pid 独立 window（独立 windowId/id 空间），set_value 可填完整绝对路径；**对话框关闭后再 click 报 "No window found for pid" 是动作已生效信号**，不要重试，直接 Test-Path/读文件核验。n-modal 在同一 webview 文档内同树，id 连续。
- 截图：须显式 windowId 指定主窗口（默认可能截到通知小窗）；像素坐标可直接用于 click 但**窗口移动/缩放后映射漂移**，每次拖拽前以最新截图重量坐标。可点中 opacity:0 悬停才显的按钮。
- **MCP 无法合成双击**（clickCount=2 不产生 WM_LBUTTONDBLCLK）；替代 P/Invoke user32（须 ShowWindowAsync+SetForegroundWindow 并校验），WebView2 仍易丢双击，不可靠请用户手点。
- 复制验收硬证据 `Get-Clipboard`（值=展示文本）；toast 1.5s 生存期 MCP 往返 2-4s 抓不到。
- 长连接 CLI（watch）：`Start-Process -RedirectStandardOutput file -WindowStyle Hidden -PassThru` 后台跑、读文件、Stop-Process 收尾（直接阻塞执行会随工具调用结束被杀）。Ctrl+C 退出码自动化：CreateProcess 带 CREATE_NEW_PROCESS_GROUP + GenerateConsoleCtrlEvent(CTRL_BREAK=1)（CTRL_C 无法定向非零进程组；不能加 CREATE_NO_WINDOW 否则信号无处投递）。
- **n-tabs segment 切 tab（AI 抽屉四模式实测，2026-09-11）**：UIA 树 tab group 需 max_depths≥14 才可见（10 会截断）；tab 元素 tabindex=-1 **键盘不可达**（press_key Tab/方向键路线死路）；click 每个 tab 文本前的**空占位 group 可点中相邻 tab 但落点偏移一格**（实测 click 空 group 194→点中「意图标注」、196→「定位」、197→「流程」），成功与否以 get_app_state 验证效果（如 edit 解除 disabled）而非 id 字样判断。
- click 的 x/y 坐标参数被 SDK 忽略（返回 screen(0,0)），坐标点击不可用；press_key 每次成功按键后 revision 缓存失效，下次按键前必须 get_app_state。
- **视口虚拟化裁剪**：滚动容器视口外的元素 UIA 树中缺失（flowmap 批量标注单个 checkbox 截图可见但树中无；locate 匹配卡片 reason/按钮偶发缺失）——先滚动到位或以截图辅助判定，勿据树缺失断言"元素不存在"。
- WebView2 子进程（msedgewebview2.exe 多个 pid）不承载 UIA 主树，操作一律走 Wails 主进程 pid 的 get_app_state。
