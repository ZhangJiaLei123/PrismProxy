# PrismProxy 进度记忆

> 开发时间线与波次记录（做了什么+验证状态+提交/遗留）。实施计划唯一入口=[doc/开发计划2.0.md](../doc/开发计划2.0.md)；设计依据=[doc/复盘AI分析设计.md](../doc/复盘AI分析设计.md)；里程碑级进度另见 doc/开发进度.md。契约与实现细节见 [前端代码记忆](./前端代码记忆.md) / [后端代码记忆](./后端代码记忆.md)，教训见 [教训记忆](./教训记忆.md)。

## M13 复盘 × AI 分析（v2 分支，2026-09-10 启动）

> 2026-09-10 启动：新建 `v2` 分支（从 main），实施顺序 P0 配置层→P1 internal/ai→P2 ctlapi/绑定→P3 AiTab→P4 复盘四模式（各阶段出口与 AC 锚点在计划 2.0）。设计稿 v1（09-09）→ v2（09-10 增补 intent）。
> 2026-09-10 计划审计（开工前定稿）：2 高危 5 中危 8 低危全部回写设计稿（v2.1）与计划（v1.1）。定稿决策：①主窗内嵌 AI 通道=方案 A Wails 事件桥（`AIChatStart/AIChatStop` 绑定 + `ai:chat` EventsEmit 逐帧，HTTP SSE 与事件桥共用 StreamAIChat 编排与并发闸门）；②`POST /ai/config` 部分更新（出现的字段才覆盖；apiKey 空串=保持、`__clear__`=清空；响应回脱敏投影）；③温度 0 为哨兵（UI slider min 0.1，WithDefaults 兜底 0.3）。其余修正：WithDefaults 兜底防老配置 Redact=false 隐私倒退；ValidateEnv 不改签名、key 空 warning 走 app.SaveSettings 追加 warnings；取数=persist 现有 LoadFlowByID/LoadBody + 新增 LoadFlowsByIDs；ids 截断 StartedAt 降序后端复查；review/api.ts 实为 review/api/ 目录（前端三实现 http/demo/wails）；test 返回补 model；FlowTable checkbox 列不进列布局持久化；UISettingsTabs 原记载有误现无 "ai"（P2-2 正确补）。

### P0 配置层完成（2026-09-10，提交 28e9c19，go test/vet + 全仓库测试全绿）
- `settings.go` 新增 AIConfig（十字段 json 小驼峰）/GlobalSettings.AI/AISettings 无 key 投影/AIProviderOllama 常量；DefaultGlobal 默认子值；**WithDefaults 零值兜底**（整体未初始化判定 `Provider==""&&MaxKB==0`→四数值默认+Redact=true，仅读取侧不回写）；AIConfig.Validate；NormalizeAIBaseURL（TrimRight/ + 补 /v1）；WarnNoKey()；ValidateEnv 转发 g.AI.Validate()（key 空不阻塞）。测试 8 个新用例（含旧 JSON 零值反序列化+兜底断言、投影 JSON 无 apiKey 泄漏断言）。→ 语义细节见后端代码记忆「settings AI 配置」。

### P1 internal/ai 完成（2026-09-10，go test/vet/gofmt 全绿 20 用例）
- 四文件 client.go/prompt.go/client_test.go/prompt_test.go，零外部依赖。→ 契约细节见后端代码记忆「internal/ai」。写测试揪出三处关键修复（内联 ```json 块、cookie 头白名单、net/http server 陷阱→教训记忆）。

### P2 ctlapi/绑定层完成（2026-09-10，go vet 零输出 + go test ./... 全绿 14 包；curl 经 Bearer 实测 9 项全过含 400/409 路径）
- persist 新增 LoadFlowsByIDs；ctlapi Service 接口 +4 方法 + 三路由（/ai/config GET|POST、/ai/test、/ai/chat SSE）；app 侧 SaveAIConfigPatch 部分更新、AITestConnection 不落盘、ai_bridge.go StreamAIChat 编排、aiGateAcquire CAS 闸门、bindings_ai.go 五绑定（全单结构 DTO+error）、dto.go AIConfigView；SaveSettings warnings 追加 WarnNoKey。单测含事件桥闸门互斥（GUI 全链路留 P3/P4）。wailsjs 三文件手动同步七新 class。→ 细节见后端代码记忆。
- curl 冒烟三坑（真实前缀 /api/v1、PS5.1 `-d` 转义须临时文件 --data-binary、Start-Job 开销>200ms 需前台延后发 409）→ 见环境备忘。

### P2 代码审计与修复（2026-09-11，子代理只读审计：2 中危 4 低危全修，go vet/test 全绿）
- ①解析错误错误码统一 ErrAIBadReq→400；②兜底固化违 P0-7 红线——合并基准改回原始 `cfg := g.AI`（落盘保留哨兵 0 值），校验改 `cfg.WithDefaults().Validate()`；③显式 temperature:0 经兜底副本校验放行；④SSE 首帧头补 X-Accel-Buffering: no；⑤done 帧 truncated 取真实值；⑥流完成后 sctx.Err() 提前 return 静默收尾。回归：TestSaveAIConfigPatchDefaultsNotPersisted + TestAIChatSyncErrors 帧序断言。另 gofmt 3 文件+注释笔误。

### 智谱 v4 端点兼容修复（2026-09-11，P3 前置，设计稿 v2.1→v2.2 + 计划 P0-5/P0-6 措辞同步）
- NormalizeAIBaseURL（settings.go）与 normalizeBaseURL（ai/client.go 镜像）原「非 /v1 结尾一律补 /v1」会把智谱 `open.bigmodel.cn/api/paas/v4` 错拼成 `/v4/v1`，放宽为**末段 `/v<纯数字>` 保留不补**（`/v1beta` 等带后缀仍补）；新增 6 条测试（settings 4 + client 2），go vet/test/build 全绿。先改设计稿再改代码。

### P3 主窗 AI 设置面板完成（2026-09-11，前端三文件；IDE 诊断零错误 + npm build exit 0 + grep dist 特征 6 处 + go build 全绿）
- 新建 pages/settings/AiTab.vue（九项表单/七服务商预设/keyInput 独立存取/popconfirm 清空/测试连接 n-alert）；SettingsPanel 挂全局 tab + emptyForm/loadSettings 双兜底；mocks/app.ts 三桩对齐 Go 契约（掩码=空返""、≤8 返"****"、否则 `****`+尾 4 位）。→ 细节见前端代码记忆。遗留当时：P4 复盘页 AI 四模式待启动。

### P3 代码审计与修复（2026-09-11，子代理只读审计 2608062..20aad3c：5 低危全修）
- ①纯空白 key 三形态对齐「空白=保持」（Go SaveAIConfigPatch default 分支 TrimSpace 后非空才存+回归断言）；②mock keyMissingWarn 对齐 WarnNoKey 触发条件；③mock TestAIConnection 缺 BaseURL/model 改 throw 对齐 aiProbe 哨兵 reject；④`/v1beta` 边界补 2 测试断言；⑤文档漂移（custom 不回填保留当前输入——代码本就保留，记忆写反）。

### P4 复盘页 AI 四模式完成（2026-09-11，npm build exit 0 + 诊断零错误 + grep dist 特征 + FlowTable 主窗回归 + wailsjs 无脏 diff；GUI 实测待用户）——**M13 全阶段至此完成**
- 新建 review/ai-md.ts（marked+DOMPurify，全项目 v-html 唯一出口）+ review/components/ReviewAiPanel.vue（约 520 行）+ ReviewIntentBar.vue + review/useIntents.ts。四模式 tabs/Phase 状态机/delta 50ms 节流/迟到帧守卫/locate 截断/正文开关 OR 语义/抽屉自绘动画/explain 自动开始三路收敛/隐私链（告知 modal+popconfirm+外发 host 小字）/空态引导/Esc。接线归并 ReviewPage.vue（顶栏 AI 按钮+批量标注意图+详情 AI 解读+意图摘要条）。→ 细节见前端代码记忆。

### P4 最终代码审计与修复（2026-09-11，code-audit-specialist 审 3c69e48：1 高 3 中 3 低全修，前端提交 c21bb4a，8 文件 +422/-402）
- **H1 高危·双入口构建图谱**：AI 样式全放 review.css（仅独立入口 import），主窗内嵌形态样式全缺——拆 `review/ai.css` 由两形态公共祖先 ReviewPage.vue import（单点覆盖双形态）；review.css 精简为独立入口底座（386→37 行）。→ 教训见教训记忆。
- M1 面板运行竞态 runSeq 运行令牌（异步续体+迟到帧均校验 seq）；M2 卸载清理（onBeforeUnmount stop()+removeEventListener Esc）；M3 error 帧契约统一（http.ts 抽 dispatch()，error 帧 throw ApiError）；L1 wails.ts 空句柄兜底 throw；L2 useIntents 死代码清理；L3 locate 截断改 matchAll(/\`\`\`json/gi) 取最后匹配。

### P4 审计修复二轮（2026-09-11，复审 c21bb4a + 4 低危全修，提交 01b50a6）
- P1 intent 帧守卫闭环（「谁消费帧谁守卫」：runIntent 不再代写缓存，intent 帧写入移交调用方）；P2 合帧定时器清理（stop() 后补 flushRender，onBeforeUnmount 兜底）；P3 demo analyze 契约（analyze resolve=流已结束，定时器段包进 new Promise）；P4 跨组件互斥（useIntents 模块级 busy ref 单例互斥，busy 时 throw「已有意图标注任务进行中」）。

### P4 增强：右下角调用日志抽层 + AI 分析中动画（2026-09-11，npm build 绿 + 诊断零错误 + grep dist 特征 10 处）
- ReviewAiPanel footer 常驻 flex 行（左外发告知+右 26px 日志按钮）；`.ai-logsheet` 抽层（n-tabs segment 双 tab「对话内容」全量原始输出/「调用日志」帧级采集上限 500 条）；`.ai-live` pill 动画 + `.ai-skel` 骨架微光。静态线索齐备，动画非唯一反馈。→ 细节见前端代码记忆。

### P4 增强（日志抽层+动画）代码审计（2026-09-11，code-audit-specialist：无🔴，🟠×3 🟢×5）
- 🟠：①日志 500 上限后 push+shift 长度恒定 watch 失效+索引 key 全列表 patch；②强制滚底无近底部判定；③**entries 波次 M13 决策漂移**——AISettings.Entries 携带条目明文 apiKey 随主窗设置表单往返（顶层 key 仍不经表单链路、HTTP 面仅 hasKey、settings.json 0600 安全），本审计记录视为该演进的确认（拍板留痕）。🟢5 项与通过项详见原审计（八类帧全覆盖/三实现契约一致/模板 }} 红线/XSS 防线/节流清理等）。

### 自托管首块超时修复（2026-09-11，设计稿 v2.2→v2.3；同日审计 3 低危全修）
- 用户报 Ollama「测试连接通过但批量标注 30s 超时」。根因：首块看门狗 30s 上限对本地 Ollama 过紧（64KB prompt 温机 TTFB≈17s，冷启动可破 30s；测试连接小 prompt 探不出差异）。修复：`firstChunkTimeout()` 按部署形态区分——自托管（未配 APIKey）取 Timeout 全额，云端维持 min(30s, Timeout/4)；`Timeout<=0` 兜底 30s；firstChunkTimeoutErr() 统一超时文案+自托管追加提示。同日修：注释漂移 2 处同步 v2.3、自托管文案补「若持续超时…」、settings.go TimeoutSec 注释旧账订正（首帧后流不限时属设计）。→ 语义见后端代码记忆。gofmt 教训（`//（` 须写 `// （`）见教训记忆。

### P4 增强（日志抽层+动画）审计修复八项（2026-09-11，🟠×2+🟢×5 全修，用户拍板）
- A1 日志 LogEntry 加自增 id 作 key+watch 源改末条 id；B1+C1 滚底拆双 watch（导航无条件滚/流式仅近底部<40 跟随）；R1 读屏播报（常驻 sr-only live region，v-if 动态插入无效）；Z1 Esc 分层（先收抽层再关面板）；G1 色值收口 `--ai-brand`/`--ai-brand-2`（变量定义在 ai.css 自身 :root）；G2 footer 高度解耦 `--ai-foot-h: 35px`；G3 上一轮日志归档 prevLogs（归档延至新 run 启动）+ `.ai-log-prev` 弱化开关。

### M13 GUI 实测收官（2026-09-11，真实 Ollama qwen3.5:4b @ 192.168.0.69:11434/v1；demo 形态+主窗 exe 形态全过）
- **两处代码修复（GUI 实测揪出）**：①IntentItem/MatchItem 缺 json tag（Wails 序列化 PascalCase，前端取小驼峰全空）→ prompt.go 两结构体补 tag；②MatchItem 缺 method/url → ai_bridge.go 解析文末 JSON 块后按 flowId 从送审流回填。四模式全链路正常（流式渲染/intent 回填/locate 匹配卡片+跳转/flowmap 结构化输出）；4B 模型单流流式约 30–50s；脱敏与证据标注规范验证通过；单流预算显示正确。分析完成态特征：edit 恢复、开始分析 enabled、停止按钮消失、「AI 分析中」消失。

### AI 面板 segment 切换器「进度条」修复（2026-09-11，两轮，浏览器实测收官）
- 用户报四模式切换器"像进度条看不懂"。首轮误诊 dark.mjs 配色；**真根因=naive-ui 2.40.4 normalizeSlots bug**（自闭合 n-tab-pane 的 tab 文字根本没渲染，DOM 空 → capsule 压成 8px）；修复=自闭合 n-tab-pane 改 `n-tab`（纯导航 tab，绕开 bug 路径）。次因=naive-ui 主题变量挂内联 style，普通 CSS 盖不过须 `!important`（ai.css 六变量）。**验证红线再证：静态全绿 ≠ 生效**——首轮只改 CSS 变量未加 !important 且没发现文字未渲染，build+grep 全过但用户实测无效；本轮起 dev server + browseruse 实测（computed 变量逐项断言）。→ 详见教训记忆「naive-ui」条。

### AI 日志抽层拖拽调高 + 高度缓存（2026-09-11，浏览器实测五项全过）
- `.ai-log-grip` 把手 + pointer 拖拽 + `prismproxy:review-ai-logsheet-h-v1` 缓存；钳制策略=缓存值内联 `height: min(<px>, calc(100% - 155px))` 窗口变小 CSS 自动就近钳制；`.ai-logsheet.dragging` 关 transition；双击 resetLogH 清 key。→ 实现细节见前端代码记忆。新坑：browser_evaluate IIFE 陷阱、Vue 响应式 DOM 异步（见教训记忆）。

### 拖拽审计修复 I1/I2/I3（2026-09-12，TRAE-code-review 审 4 问题，用户拍板修 I1/I2/I3，I4 不修；浏览器实测全过）
- I1 右键/中键可触发拖拽→startLogDrag 首行 `e.button !== 0` return；I2 `155px` 与 `--ai-foot-h` 跨文件耦合→内联 calc 用 `var(--ai-foot-h) + 120px`，JS 侧 getComputedStyle 回读；I3 拖拽中 Esc 不终止拖拽态→onEsc 收抽层分支先 `onLogDragEnd()`。注意：收起判定看 transform/rect.top 而非 display（.ai-logsheet 收起后 display 恒 flex）。

### 对话 tab 四区（2026-09-12，提问→提示词→思考→输出；npm build 绿 + 诊断零错误 + go vet/test 全绿）
- 帧契约扩展：ai_bridge meta 帧补 system/user（BuildResult 现成字段已脱敏）；delta 帧 reason（推理模型 reasoning_content）前端接住。ReviewAiPanel conv tab 四区：提问区/提示词折叠区/思考过程折叠区（增量合帧+自动开合三态+reasonTouched）/输出区；合帧统一出口 scheduleRender()；折叠体 grid-template-rows 0fr→1fr 过渡；demo.ts meta 补提示词桩。设计稿 §5.3 同步。

### 批量截断/思考字段双 bug 修复（2026-09-12，go build/vet/test 全绿 + npm build 绿）
- ①client.go sseChunk delta 补 `reasoning` 字段解析（Ollama ≥0.5 OpenAI 兼容端点把思考放 delta.reasoning；reasoning_content 优先、reasoning 兜底）；②sseChunk 补 finish_reason 收集，Stream 签名改 `(finishReason, error)`，ai_bridge done 帧如实透传（空兜底 stop），不再硬编码 stop 掩盖 length 截断；③ReviewAiPanel done 帧消费 finishReason=length → 日志提示减小批量流数。测试：TestStreamFrames 补断言 + 新增 TestStreamFinishReasonLength。注意：Ollama 运行时 ~8K ctx 截断是上游行为，PrismProxy 侧修复=不再静默；批量规避仍按 ≤10 条/批。

### intent 批量标注加「单条轮询处理」开关（2026-09-12，纯前端；npm build 绿 + 诊断零错误；同日复审 2 子代理交叉验证修复）
- 意图模式 opts 行新增复选框（singleMode ref 会话级不持久化）：勾选后 for 循环逐条 runIntent（每条独立 meta/预算/done）；**done/error 帧语义降级是核心坑**——itemFrame 拦截 done/error 不置终态，循环 catch 统计 failed 续跑；循环守卫 seq!==runSeq||signal.aborted 每条前检查；结束后 failed>0 补汇总；进度 metaText/ intentPct 分母适配。复审修复：①候选列表循环开始快照 `ids=[...intentIds.value]` + singleTotal ref（AI 抽屉无遮罩，运行期翻页/筛选会静默膨胀候选集）；②全失败置 phase='error'（部分失败仍 done+汇总）；③demo.ts qhead 计数改 slice 后 shown.length。

### F6 项目批量 intent 实测收官（2026-09-12，真实流量 83 条回填，全 finish=stop）
- ctlapi `/ai/chat` mode=intent 脚本（`$env:TEMP\prism_intent_test.ps1`，参数 -Limit -Offset -Scope -BatchTag -TagId，PS5.1 纯 ASCII；SSE 帧统计产出 `$env:TEMP\prism_intent_batch_<tag>.log/.summary.json`）对 F6-保养资料等 5 个标签跑批量标注。汇总：A=10/10（79.7s）· B5=17/17（60.3s）· C=27/27（拆 C1/C2/C3 各 9 条，28.2/28.3/27.8s）· D=13/13（D1 33.2s+D2 19.2s）· E=5/5（17.1s）· F=7/7（23.3s）· G=4/4（13.0s）。quality：conf 高为主（high≈85%），needsBody 合理，9 条/批 prompt eval+生成全程 13–33s。
- **三个产品侧 bug 结论**：①Ollama 0.33.2 OpenAI 端点思考在 `reasoning` 字段（已修复见上）；②**Ollama 运行时 ctx 预算 ≈8K tokens**（远小于 qwen3.5:4b 标称 262144）——input+思考+输出超预算 → finish=length 静默截断、content=0，大批量必踩（上游行为，只能告警+拆批）；③done 帧硬编码 "stop"（已修复）。**/no_think 软开关对 qwen3.5:4b 实证不可靠**（双注入仍出 11–14K 字符思考）——唯一可靠解=Ollama 原生 `/api/chat` 的 `think:false` 硬开关。**shim v4**（`$env:TEMP\nothink_shim\main.go`，127.0.0.1:11534）：OpenAI `/v1/chat/completions` SSE ↔ Ollama NDJSON 双向转换 + 强制 `think:false` + `num_ctx:32768`，非 chat 路径 ReverseProxy 透传；PrismProxy baseUrl 指向 shim 后 17 条批 60.3s 全回填。
- **settings 热切换实测链路（免重启）**：ctlapi PUT `/settings` 必须带 rulesProject 令牌（=当前项目 ID）；改 AI baseUrl 必须同时改顶层 `ai.baseUrl` 与 `entries[0].baseUrl`（SyncAICurrent 用 current 条目四字段覆盖顶层，entries 是决定性来源）；PUT 后内存热应用（`*a.gcfg = g`），AI client 每请求新建读内存→立即生效。切换/恢复脚本 `$env:TEMP\prism_switch_shim.ps1`（备份 `$env:TEMP\prism_settings_backup.json`），已恢复原 baseUrl。
- **批量 prompt 尺寸红线**：intent 单批打包 prompt 正比于流数，9 条≈5.5KB（安全）；27 条整批 16KB 在该 Ollama 上 94s 才出 done 首响应超时被判死；稳妥批量策略=≤10 条/批。（另提示：该实测报 30s 超时说明当时运行实例仍是 v2.3 修复前旧 exe。）

### AI 面板左缘宽度拖拽 + 宽度缓存（2026-09-12，npm build 绿 + 诊断零错误 + grep dist 特征命中 ReviewPage CSS+JS 两 chunk）
- 与日志抽层高度拖拽同构：`.ai-grip-x` 把手（ew-resize）+ pointer 拖拽 + `prismproxy:review-ai-panel-w-v1` 缓存；AI_W_MIN=420，aiMaxW()=包含块宽-400（RESERVE 脚本内常量无跨文件耦合）；内联 `width: min(<px>, calc(100% - 400px))` 就近钳制；`.ai-drawer.w-dragging` class 补偿 hover 丢失；width 不在 transition 属性内无需关过渡；双击 resetW 清 key。→ 实现细节见前端代码记忆。

### usage 透传 + 单条轮询对话聊天气泡（2026-09-12，go test 全绿 + npm build 绿 + 诊断零错误）
- 需求=单条轮询下每条流做成一对「消息发送→模型返回」聊天气泡、各自显示 token 与字数（根因：单条轮询 delta 全拼进同一 mdBuf/reasonBuf，meta.value 被逐条覆盖）。①后端 usage 透传链路：client.go 请求加 `stream_options.include_usage:true` → sseChunk 解析 Usage → Stream 三返回值 `(finishReason, usage, err)` → ai_bridge done 帧条件附带 usage（client_test 8 处调用点三接收+断言）；demo.ts 故意不加 usage 桩（保留前端估算路径）。②前端 turns 聊天模型：ConvTurn{system,user,reason,text,open,sysOpen,usrOpen,touched,finishReason,usage?} + convTurns 数组；**in-flight 轮次用 reactive() 代理推入数组后身份稳定**；beginTurn/closeTurn（done/error/stop/unmount 全路径幂等关轮）；estTokens（CJK×0.7+其他×0.25 向上取整），真实 usage 无前缀、缺失时估算带 `≈`；per-turn 自动开合与原全局三态同构；finishReason=length 时 recv 气泡内暗黄截断提示。→ 细节见前端/后端代码记忆。

### AI 面板「开始分析」按钮禁用修复：解读目标跟随列表选中流（2026-09-12，npm build 绿 + 诊断零错误）
- 用户报解读/定位/流程三 tab 开始分析按钮全禁用、列表选中无效。根因=接线缺陷（面板 flowId 仅由详情页/摘要条入口赋值，列表点选从不传入→恒 '' 恒禁用）+定位/流程设计语义（需先输入问题，非 bug）。修复：ReviewPage 模板 `:flow-id="selectedFlow"` 直绑、删 aiPanelFlowId；flowId watch 守卫收紧为 `props.show && activeTab==='explain'`（防浏览类变化 resetRun 打断在跑任务）。已知轻微行为变化：intent 模式点已选中流摘要不再自动开始解读。

### AI 面板宽度拖拽代码审计 + 低危修复 G1~G3（2026-09-12，G4 仅注释；npm build 绿 + 诊断零错误）
- code-audit-specialist 审计（0🔴0🟠4🟢，13 项必查全过）。用户拍板全修：G1 拖拽起点 rect 含 1px border → 落盘值逐会话 +1px 漂移——两处起点统一改 `el.clientWidth/clientHeight`；G2 logOpen 时宽度拖拽中 Esc 只收抽层不终止拖拽——logOpen 分支内补 `if (wDragging.value) onWDragEnd()`（未采纳审计 onEsc 首行 return 方案，那会破坏层级收合语义）；G3 纯点击把手落盘 "0" 残留——onDragEnd 落盘前加 `if (x >= MIN)` 守卫；G4 窄窗口 CSS min() 钳制可低于 420——既定就近钳制设计不修码，AI_W_MIN 注释补记。**流程发现：宽度拖拽改动被混入提交 5ad0c82（聊天气泡波次）且提交信息未覆盖——一波次一提交（教训记忆）**。

### 解读目标跟随选中流——审计修复（2026-09-12，入口信号模式；npm build EXIT=0 + 双文件诊断零错误 + grep 残留清零）
- TRAE-code-review 审出 3 问题（双子代理 2/2 确认）全修：①Major 回归——flowId watch 在浏览类变化（翻页/切筛选/关键字等置空 selectedFlow 的 10+ 触发入口）时 resetRun abort 在跑任务+清已完成结果，违背修复意图；②explain 面板关闭后经显式入口重开、自动开始被放弃时旧结果错挂新标签；③浏览点行 flowId 变化但结果仍旧。**方案=入口信号模式**：ReviewPage 新增 `aiExplainAutoTick` ref 仅在显式入口递增（列表浏览不递增）；ReviewAiPanel 删 flowLabel prop，新增 explainAutoTick?: number + runTarget ref 内化结果目标名（doStart 在 resetRun **之后**快照 props.flowId）；flowId watch 整段替换为 explainAutoTick watch（守卫 !show||activeTab!=='explain'||!cfgOk → return，否则 resetRun+requestStart(true)）；show watcher 删 explain 自动开始两行（统一 tick+loadCfg 回调兜底驱动）。时序保障：Vue pre-flush watcher 按创建顺序 id 升序执行（mode→activeTab→show→tick）。行为变化：列表浏览面板完全不动；显式解读入口=换目标重置并自动解读。

### AI 会话持久化阶段三 + 四模式视图拆分（2026-09-12，提交 03aa96f（v2，19 文件 +2655/-583，含 memory/ 六子记忆首次入库）；npm build exit 0）
- **持久化阶段三**：新建 `review/useAiSession.ts` 模块级单例 store（convTurns/logs/prevLogs/matches + IndexedDB `prismproxy/kv/ai-session:v1`），hydrate 幂等（epoch 守卫/restored.concat 防 竞态/仅空时恢复/logSeq max 续号），resetRun 拆 stopRun（切 tab/收面板/卸载，保留记录）与 resetRun（doStart 专用清场+archiveLogs）；ConvTurn 加 question 快照，LogSheet props 删 question/scopeText；上限 200 轮/3M 字符/500 日志 prune + 500ms 防抖落盘 + pagehide flush；LogSheet header NPopconfirm 清空（clearSession）。package.json +mermaid ^12（flowmap 渲染）。
- **四模式拆分**：ReviewAiPanel 模板缩为 4 组件标签，新建 6 组件——ReviewAi{Explain,Intent,Locate,Flowmap}View（每模式完整视图，文案内聚）+ ReviewAiRunActions（共用动作行，needConfirm 时 NPopconfirm 二次确认）+ ReviewAiMdView（renderMarkdown+骨架屏）；父持引擎，函数 props 下发（flowLabelOf→Intent、confCls/confLabel→Locate）；displayMd 由 fullText 全轮 join 改 lastText 末轮取值（防恢复后旧轮拼接）。
- 详细记录见外层 app抓包/CONTEXT.md 近期任务记录。

### 未提交改动双子代理代码审计（2026-09-12，code-audit-specialist ×2 只读审计：无 Critical/Major(UI侧)，数据层 Major×3（1 待确认）+ Minor×11；拆分等价性通过）
- **审计对象**：上述两条未提交任务全部改动（useAiSession.ts/ReviewAiPanel/LogSheet/6 新组件/ai.css/package.json）。
- **拆分等价性：通过**——渲染结构/类名/v-if 分支/phase 分支/disabled/aria/文案/事件链逐项与旧版等价；3 处行为差异（切模式保留数据、displayMd 末轮化、question 快照）均为配合持久化的有意变更。
- **正面结论**：XSS 面干净（v-html 仅 2 处均经 DOMPurify，mermaid strict+二次净化）；取消链路正确（runSeq+phase 双守卫、abortCtl finally 回收、http abort 静默/wails AIChatStop 均衔接）；epoch 守卫无插入点风险；logSeq 单 context 无碰撞；类型迁移无断引用。
- **数据层 Major×3**：M-1 clearSession 与 streaming run 交错无定义（LogSheet 直调清空，运行链路存活→孤儿 curTurn 复活半截回复，建议清空上收父面板先 stopRun）；M-2 lastText 取全局末轮，跨模式切换后 locate 显示 intent 文本+locate 匹配错配（建议 ConvTurn 加 mode 字段按模式过滤）；M-3（待确认）http 多标签并发写 IndexedDB last-writer-wins+logSeq 跨 context 碰撞（wails 单 WebView 不存在）。
- **Minor×11**：数据层 8（doStart catch 缺 runSeq 守卫/关面板用 stop 残留 stopped 态/stopRun 清 runTarget 致切回错挂/intent question 用实时 scopeText/相同 question 无 run 分隔/explainAutoTick 先 resetRun 再校验致静默终止/prune 运行中删历史+:key 用索引/hydrate 失败不重试+单轮超限永不 prune）；UI 侧 3（.ai-match-go 幽灵类无 CSS/Phase union 7 处重复内联建议收敛 useAiSession 导出/2 处注释仍引用已删的 fullText）。
- **Major 修复（用户拍板修 2 个确定 Major，M-3 多标签记录不修；修复由并行会话落地，本审计任务复核验证通过）**：M-1 采用「streaming 禁用清空按钮」方案替代上收父面板——LogSheet 清空按钮 `:disabled="phase==='streaming'"` + title 提示「停止分析后才能清空」（clearSession 全项目仅此一处调用；非 streaming 时 curTurn 必为 null：stopRun 经 stop→closeTurn 且显式置 null，done/error/stop 全路径覆盖，交错窗口封闭）；M-2 ConvTurn 加 `mode?: string`（createTurn 第 4 参传 activeTab 快照、toPersistTurn/hydrate 均含 mode），Panel lastText 改按 activeTab 反向过滤取本模式末轮、无匹配返回 ''（旧落盘数据无 mode 静默降级空正文），displayMd 的 locate 截 json 块作用于过滤后文本不误伤他模式。验证：IDE 诊断全项目零错误 + npm run build exit 0（17.40s）。Minor×11 留档未修，随迭代消化。

### AI 面板 TRAE-code-review 复审 + 修复（2026-09-12，换代理独立复审 5/5 成立：1 major 4 minor 全修；与上条审计交叉印证）
- **复审关系**：上条 code-audit-specialist 审计之后的 TRAE-code-review 复审（独立双子代理 5/5 问题成立）。对应关系：复审问题1（跨模式串显）≈M-2、问题4（streaming 清空交错）≈M-1、问题5（过时注释）≈Minor「2 处注释引用 fullText」——三者与上条记录的 M-1/M-2 修复为**同一批已落地变更**（并行会话各记一份，代码仅落地一份，本条补记复审视角）；M-3（多标签 IndexedDB）维持不修。
- **复审新修 2 项（前审未发现）**：①日志抽层滚随 watch 依赖废 fullText 全量 join（每 50ms 合帧 O(N) 重算、MB 级字符串分配徒增 GC 压力）→ 依赖改末轮 `text/reason.length` + 轮次数（number 值比较）；②hydrateMermaid hydrating 期间新触发静默丢弃 → `mmdPending` 标记 + finally 补跑一次（done 后末轮完整图源不再停留代码块态）。
- **注释修复覆盖 3 处**：Explain/Flowmap/MdView「父级 fullText」→「父级 displayMd：当前模式末轮输出」（覆盖前审口径之 2 处）。
- 验证：三文件 IDE 诊断零错误 + grep fullText 无代码残留（仅历史说明注释一处）+ npm run build exit 0。契约细节见前端代码记忆「聊天气泡」节。

### 解读目标小件折叠/展开/滚动（2026-09-12，npm build exit 0 + 5 文件诊断零错误；浏览器实测未做）
- 新建 review/components/ReviewAiScopeText.vue：四模式共用「解读目标」小件——默认单行省略、溢出才出现展开箭头、点击展开全显、超高滚动（滚动区仅文本，箭头固定）；explain/intent/locate/flowmap 四视图同点替换为 `<ReviewAiScopeText :text="scopeText" />`。
- 短文案零打扰（无溢出不渲染箭头）；溢出检测/复测契约与交互细节见前端代码记忆「解读目标小件」节；样式在 review/ai.css `.ai-scope` 系。

### 对话聊天卡片高度优化：正文折叠 + 展开体限高滚动（2026-09-12，4 文件诊断零错误；浏览器实测未做）
- 需求=思考/正文可能极长，聊天卡片提供默认折叠+展开两模式且默认折叠，展开提供滚动。①useAiSession.ts ConvTurn 加 `txtOpen/txtTouched`（UI 态不入盘，hydrate/createTurn 补默认值）；②ReviewAiPanel pushDelta 首个正文增量自动展开直播轮正文（txtTouched 退出自动，与思考区 touched 互相独立不打扰）；③ReviewAiLogSheet recv 气泡正文改「正文输出」折叠开关（字数计数）+ 折叠体，system/user/reason 三块同挂 `.ai-conv-scroll` 一致限高；新增 pinLiveFolds（末轮内滚动体近底部 40px 阈值钉底，开层/切 tab force 无条件）；④ai.css `.ai-conv-fold > .ai-conv-scroll{max-height:280px}` + 仅 `.open` 态 overflow-y:auto（收起态 hidden 保 grid 折叠动画）。→ 契约细节见前端代码记忆「聊天气泡」节。

## M13 之前里程碑（摘要）
- M1-M11：代理引擎/MITM/系统代理/ADB/规则引擎/Composer/ctlapi/M9 多项目/M11 无项目态——关键决策与契约见后端代码记忆对应节，里程碑详情见 doc/开发进度.md。
- M12/M12.1/M12.2：数据复盘页（Vite 独立入口 review.html + review/ 目录；列表/直方图/分页/关键字下沉/忽略名单/排序）——见前端代码记忆「复盘页」与后端代码记忆「persist 复盘查询」。
- M12.3：复盘列表组件复用（FlowTable 共享表格）+ 主窗内嵌复盘（useAppView）——见前端代码记忆。
- v1.2.1 发布（2026-09-10）：发布流程实操与坑见环境备忘「构建与发布」。
