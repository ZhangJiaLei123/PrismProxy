# PrismProxy 项目记忆

> 只保留关键记忆，便于跨任务、多任务协同开发。开发进度见 [开发进度.md](./开发进度.md)，两者不是一个概念。

## 项目定位

- **PrismProxy（棱镜）**：Windows 桌面 HTTP(S) 抓包工具，MITM 代理 + 按进程名归因流量；独立开源工具，定位与文档均不绑定任何特定业务。
- 核心场景：抓取**模拟器（雷电/MuMu）内 App**、真机与 PC 软件的 HTTPS 流量。
- 方案文档：[方案.md](./方案.md)（已完成全面审计并修订，作为开发依据规范）。
- **文档位置（2026-09-04 起）**：三份 md 文档统一移至 [`doc/`](./) 目录，文档内相对链接仍互相有效。
- **Git 仓库（2026-09-04 开源）**：`https://github.com/ZhangJiaLei123/PrismProxy.git`（main 分支）。初始提交含 M1-M3 代码 + `domains/` + `doc/`；`.gitignore` 排除 `build/bin/`、`frontend/dist/`、`frontend/node_modules/`、`*.exe`、`frontend/package.json.md5`。文档已去除业务绑定（修车助手/F6 等第三方描述）。**仓库公开，后续写文档/commit/注释时勿引入任何具体业务方名称，保持独立开源工具定位。**

## 技术栈

- 代理引擎：**Go**（标准库为主）
- 桌面壳：Wails v2 + Vue 3 / TS + Naive UI + @vueuse/core（虚拟滚动）
- 平台：Windows 优先（WebView2 运行时）

## 关键技术决策与陷阱（审计结论，必须遵守）

1. **ForceAttemptHTTP2 陷阱**：为记录上游真实证书链需自定义 `TLSClientConfig`，而 Go 一旦自定义该字段便保守禁用 h2 —— 必须显式 `ForceAttemptHTTP2: true`。
2. **流式捕获 tee 语义**：边转发边捕获，Flow 状态机 `pending/streaming/done/error`；WebSocket/101 升级连接 `http.Transport` 无法接管，必须 Hijack。
3. **默认绑定 127.0.0.1**：避免 LAN 开放匿名代理；局域网绑定是显式开启项。
4. **PID 进程映射**：`GetExtendedTcpTable` 须同时查 AF_INET + AF_INET6 两表；accept 时即时查询（不做长期缓存）；目标进程权限不足时取路径失败 → 降级显示 + 提权提示。
5. **解压**：gzip/deflate（标准库）+ br（andybalholm/brotli）+ **zstd（klauspost/compress）**（透传 Accept-Encoding 后 CDN 会返回 zstd）。
6. **系统代理**：注册表真实路径是 `HKCU\Software\Microsoft\Windows\CurrentVersion\Internet Settings`（**方案文档里的 `HKCU\Internet Settings` 是简写，`reg query` 必须用完整路径否则报"找不到项"**；PowerShell 的 `HKCU:\` PSDrive 有时也异常，稳妥用 .NET `[Microsoft.Win32.Registry]::CurrentUser.OpenSubKey(...)`）；`InternetSetOption` 广播 39(SETTINGS_CHANGED)+37(REFRESH)；原始配置落盘 `%APPDATA%/PrismProxy/` + 崩溃自愈；已运行的部分桌面应用进程（如 Trae）生效不完全常需重启（新启动进程立即生效，curl 实测即时）。
   - **ProxyOverride 绕过语义**：分号分隔；**裸域名同时匹配自身与全部子域**（`trae.cn` 覆盖 `api.trae.cn`/`x.trae.cn`）；`<-loopback>` 绕过本地回环。绕过发生在客户端侧（WinINET/Chromium 直接拨号，**代理根本收不到 CONNECT**），比代理内"解密黑名单"更靠前——是"代理进程死了、注册表残留"断网场景的**系统级免疫**。
   - **已内置的开发机绕过列表（2026-09-03 应用）**：`trae.cn / trae.ai / trae.com / trae.com.cn / trae.volces.com / zijieapi.com / volces.com / volcengine.com / volcengineapi.com / bytedance.com / bytedance.net / byted.org / byteoversea.com / byteintl.net / bytepluses.com / snssdk.com / doubao.com / byted-static.com / tiktokcdn.com`（备份 `%APPDATA%\PrismProxy\proxyoverride-backup.txt`，原值 `<-loopback>;api.trae.cn;mcs.zijieapi.com`）。Trae CN 域名事实取自其 `resources/app/out/main.js`：API/遥测走 `*.trae.volces.com`、`mcs/mon.zijieapi.com`、`gator/tab/apmplus.volces.com`、`abtestvm.bytedance.com`，前端 CDN `*.byted-static.com`。
   - **M4 sysproxy 模块硬要求**：接管时**合并去重** ProxyOverride（内置绕过列表常量 + 用户已有值，前缀保留 `<-loopback>`），**绝不覆盖**；恢复时还原备份原值。
   - **退出清理三场景（2026-09-05 补齐关机场景）**：①关窗口/Alt+F4 → Wails `OnShutdown`（app.shutdown）；②**系统关机/注销/重启 → Wails v2.15 主窗口 winc WndProc 只处理 WM_CLOSE/WM_DESTROY，不处理 WM_QUERYENDSESSION/WM_ENDSESSION（源码已核实），OnShutdown 不触发**——自建隐藏顶层窗口接收（`session_windows.go`：独立 goroutine + `runtime.LockOSThread` + RegisterClassExW/CreateWindowExW/GetMessage 消息循环；**必须普通顶层隐藏窗口，HWND_MESSAGE message-only 窗口收不到 ENDSESSION 广播**），WM_QUERYENDSESSION 返回 1、WM_ENDSESSION(wParam=TRUE) 同步还原；③`taskkill /F`/强杀/崩溃 → 任何程序无法即时处理，下次启动 SelfHeal 兜底。还原逻辑抽为 `App.restoreSystemProxy()`（Status==StateOn 才 Disable，幂等），shutdown 与会话回调共用；非 Windows 有 `session_other.go` 空桩。headless 模式不管系统代理无需处理。
7. **CA**：私钥 ACL 限当前用户（可选 DPAPI）；叶子证书 ECDSA P-256、有效期 ≤1 年、LRU 缓存 + singleflight。
8. **规则引擎**：捕获/解密/进程三层，有序列表首条命中生效 + 默认动作。
9. **内存**：环形缓冲 2000 条/256MB 按 body 实际字节统计；body 2MB 截断；事件批量节流 ~50ms + `flow:evict` 淘汰通知。

## M1 阶段的临时技术选型（重要）

- **M1 零外部依赖**：不用 `golang.org/x/sys/windows`、不用 `oklog/ulid`，改用纯 `syscall` + `LazyDLL` 直调 iphlpapi.dll（规避 go mod tidy 网络依赖风险）。
- **Flow ID 临时格式**：`时间戳毫秒-序号`（M2 前后再考虑换 ULID）。
- 监听地址 flag：`-addr`，默认 `127.0.0.1:9090`。

## 里程碑

- 当前阶段：**M5 已完成（2026-09-04，效率增强 V1.1，GUI 验收通过）→ 下一步 M6（调试重发 Composer）**；M6-M8 已规划未开发（见下节）
- M1 代理引擎骨架（明文 HTTP + CONNECT 透传 + 错误记录，命令行/日志验证）
  - **硬性退出条件：雷电/MuMu 模拟器进程归因实测**
- M2 MITM 解密（**关键路径**）→ M3 UI 流量面板 → M4 过滤/设置/打包 = MVP

## V1.x 关键决策（2026-09-04 规划，M5-M8 开发时必读）

1. **需求拆分**：M5=右键复制 URL/cURL + 详情复制 + 列表置顶 + **快捷忽略域名/进程** + 规则导入导出；M6=调试重发 Composer；M7=SQLite 持久化；M8=AI CLI 控制。详见方案 §4.9-4.12。
2. **cURL 生成在 Go 侧**（BuildCurl binding，2026-09-05 修订为三 shell + 单行输出）：**单行输出**（Chrome DevTools 标准形态，杜绝 `^`/`` ` ``/`\` 续行符跨终端粘贴兼容问题）。cmd 与 PowerShell 版双引号包裹、引号内 `""` 转义（MSVC CRT 与 PS 均还原为字面 `"`，**cmd/PS 双兼容**；cmd 交互模式 `%var%` 无法完美转义属已知残留）；PowerShell 版 `` ` ``→`` `` ``、`$`→`` `$ `` 必须转义（PS 双引号串内 $var 会展开、` 是转义符）；bash 版单引号 `'\''`；三者均显式 `curl.exe`（规避 PS 5.1 别名）。body 文本 ≤64KB 内联，二进制不内联。**教训：① `\"` 转义只在 cmd 有效，PowerShell 中 `"` 直接闭合字符串；跨 shell 引用规则差异必须逐字实测，不能用简单请求（无特殊字符）代替验收。② 三版语法互不兼容是本质特性（bash 版贴 PowerShell 报语法错误，反之亦然），须按目标终端选菜单项；Git for Windows 精简版无 curl。③ 用户贴回的"命令内容"可能被聊天框 markdown 渲染篡改（如 URL 两侧加反引号）——排查先本地复现逐字节比对，勿直接采信。**
3. **置顶语义**：Flow.Pinned——固定顶部展示、不参与环形淘汰、Clear 保留、上限 200、会话内状态不持久化。
4. **调试重发**：独立 http.Client **直连防回环**（跟随上游代理设置）；重发 Flow `Source="composer"` 入 store 可迭代；HTTP/1.1 only。
5. **SQLite 选型 `modernc.org/sqlite`**（纯 Go 无 CGO）——`mattn/go-sqlite3` 依赖 CGO/mingw 破坏单 exe 构建链，**禁用**。持久化默认关闭；异步队列批量写，队列满丢弃，绝不阻塞代理热路径。
6. **AI CLI 形态**：同 exe 子命令 `PrismProxy.exe cli <cmd>` + 本地控制 API（127.0.0.1:9595 可配，独立于代理端口 9090）+ token 认证（启动生成落盘 `%APPDATA%/PrismProxy/cli-token`）；默认 JSON 输出；与 Wails Bindings 共用内部服务层。
7. **规则导入导出**：JSON 含 version；文件/URL 两种来源；整体替换 + schema 校验；域名组引用缺失时提示（导出可内嵌组清单）；domains/ txt 仍是域名唯一事实源。
8. **快捷忽略（M5 已实施，落点=双内置黑名单组）**：域名列→忽略自身+全部子域（裸域名语义）写入 `_quick_ignore_hosts`（名称"快捷忽略-域名"，hosts 维度）、进程列→按进程名写入 `_quick_ignore_procs`（名称"快捷忽略-进程"，processes 维度）；两组均 blacklist/enabled，**组间 OR 各管一维**。**必须双组不能单组**：组内维度 AND——若 hosts 与 processes 混在同一 `_quick_ignore` 组，"忽略域名"加的 host 会与"忽略进程"加的 proc 互相收窄（host 命中但 proc 不命中→整条不屏蔽），两个快捷动作彼此失效；旧混合组由 `settings.Migrate`（splitQuickIgnoreGroup）启动时自动按维度拆分、幂等可重入。幂等防重复；只影响后续流量存量不删；热更新+落盘 settings.json；设置页双组卡片与普通组同构（可见/可删/可开关）。Binding：`AddQuickIgnore(target, value)`。
9. **过滤规则组改造（2026-09-04 设计定稿 + 审计修订 + 同日实施完成 = M4.6）**：黑名单/白名单两模式（默认黑名单）+ 多规则组。**详细设计见 [规则设计.md](./规则设计.md)（唯一事实源）**，要点：`FilterGroup{id,name,enabled,mode,hosts,paths,processes}` 替换 captureRules/processRules（自动迁移、迁移幂等、迁移组按黑白区分命名，decryptRules 不动）；组内维度 AND、同维度多条目 OR、组间 OR、deny-override（黑名单恒优先，无白名单组默认全显示）；path glob（`*`跨`/`；**无通配符=精确或子路径段边界（`/telemetry`≠`/telemetryx`），字符串比较不走正则**），只匹配 path 不含 query；引擎统一 `ShouldDisplay(host,rawURL,procName)`（nil receiver 安全返回 true）。**审计核心修订——信息缺失维度移除规则（§2.3）**：进程未知（模拟器归因 VBoxNetNAT.exe）/盲透传隧道无 path → 该维度从组约束移除，剩余约束空则组对该流不匹配（纯进程黑名单组不误伤未知进程=兼容现状；path 维度仅对 MITM/明文流量生效；纯 path 白名单组放不了隧道流是已知局限）。其余修订：@引用校验存在性但只 warning 不阻塞（Validate 返回 error+warnings）；`url.Parse` 失败降级空 path 走维度移除（不拿原始串匹配）；hosts 禁裸 `*`；NewEngine 启动失败=log warn+空引擎全放行、保存失败=拒绝保存；UI 状态条须用与引擎一致的 `isActiveWhitelist` 谓词（enabled+mode+非空）、组卡片默认折叠、未知 @引用本地即时提示 + 保存时后端 warnings 透传；已拍板：urlRe/method 直接移除、组不做排序、热更新在途流沿用现状不处理。M5 快捷忽略落点为 `_quick_ignore_hosts`/`_quick_ignore_procs` 双内置黑名单组（见上节第 8 条）。**实施备注**：`SaveSettings` 返回 `SaveSettingsResult{warnings}`（前端有 warnings 时留面板展示）；迁移在启动时 Load→Migrate→Save 即落盘；NewEngine 失败空引擎兜底全放行；Engine 保留 groups map 仅供解密层 @引用（过滤层热路径用编译期预展开）。

## M3 关键认知（GUI/验收实测）

- **store 事件语义**：`Add` 按 ID upsert——已存在则 `*old = *f` 原位更新发 "update"，否则值拷贝追加发 "new"；环形覆盖先发 "evict"。`Subscribe` 锁外回调。**字节预算（256MB）M4 已实现为配置项 maxBodyMB**；M3 内存上限 = 2000 流 × MaxBodyCapture(2MB) 兜底（压测 +79MB 驻留是设计行为非泄漏）。
- **事件合帧**：store 事件 → app.go pendUp map（入队即转 DTO 值拷贝）→ `time.AfterFunc(50ms)` flush → Wails `flow:upsert`/`flow:evict`。前端只做展示，状态全在 Go。
- **展示层解压**：`capture.DecodeBody`（gzip/deflate/br/zstd）在 GetFlowBody 调用时发生，不在捕获路径。
- **go mod tidy 陷阱**：tidy 会移除"go.mod 里声明但代码尚未 import"的依赖——先写 import 再 tidy，或 tidy 后确认依赖仍在。
- **Vue 模板字面量陷阱**：模板内 JS 模板字符串以 `}}` 结尾时会与 Vue 插值 `{{ }}` 闭合符冲突解析报错，改用字符串拼接或计算属性规避。
- **PowerShell 终端是 5.1**（$PSVersionTable 5.1.26100.9168）：**无 `ForEach-Object -Parallel`**，并行压测用 `1..N | % { Start-Job { ... } }`；**curl 是别名必须 `curl.exe`**，https 仍要 `--ssl-no-revoke`。
- **大文件测试源**：cloudflare speed 403、hetzner 502 均不可用；可用 `https://cdn.npmmirror.com/binaries/node/v22.11.0/node-v22.11.0-win-x64.zip`（34MB，30MB/s，支持 Range）。
- **Computer Use MCP 操作 WebView2 要点**：WebView 内容无 a11y 节点；click/scroll 必须传 element_id——用主树 `element_id="1"`（pane "PrismProxy"）+ x/y 坐标生效；**截图像素坐标可直接用于 click**（2026-09-04 实测命中；M3 时观察到的 +28px 纵向偏移未复现，可能源于当时窗口状态，若点击落空先换 ±28px 试）；交互前必须先 get_app_state 否则报 "No current Stable UI Tree revision"；**Ctrl+End/Ctrl+Home 可跳虚拟滚动列表底/顶**（比 pages 滚动快得多，新流 unshift 会抵消 pages 滚动）。
- **MCP 可点中 opacity:0 的按钮**：悬停才显现的 DOM 按钮（如排序按钮）在 a11y 树中可见，用坐标 click 可直接命中（opacity 不阻碍点击）；a11y 树中 button 的 val（"▲"/"▼"/"↕"）是验证按钮状态机的好抓手。
- **MCP 无法合成双击**：Computer Use 后台派发的 click（clickCount=2）合成不了 `WM_LBUTTONDBLCLK`，双击交互（如双击复制）MCP 测不了。替代：PowerShell P/Invoke user32.dll（`ClientToScreen`+`SetCursorPos`+`mouse_event(0x2/0x4)×2`）发真实输入——**必须先 `ShowWindowAsync(h,9)`+`SetForegroundWindow(h)` 并用 `GetForegroundWindow` 校验为 True**，否则点击落到上层窗口；且该法对本窗口也不稳定（WebView2 合成输入易丢双击计数），不可靠时直接请用户双击验证。
- **复制功能验证法**：`Get-Clipboard` 是最硬证据（剪贴板值精确等于单元格展示文本即通过）；toast 截图须在其存活期内抓（MCP 往返 2-4s，1.5s toast 抓不到）。
- **MCP drag 可驱动 WebView2 Pointer Events 拖拽**（列宽手柄实测生效）：image 像素坐标即可，但**窗口被移动/缩放后映射会漂移**（实测同一点映射比例从 1.03 变 1.38），每次拖拽前必须以最新截图重新量坐标；且用户常在并发操作本应用（分栏/列宽可能被用户先动过），截图取证与合成拖拽互相干扰时，直接请用户手拖确认最快。

## M4 关键认知（GUI/验收实测，2026-09-04）

- **设置面板布局（2026-09-04 重构）**：`SettingsPanel.vue` 由"单面板堆 7 个 section"改为**左侧竖排 n-tabs 五分组**——常规（系统代理+根证书）/ 网络（监听+上游+存储+ProxyOverride）/ 解密规则 / 过滤规则（M4.6 起为规则组卡片 UI：状态条+折叠卡片+添条目行（先选维度再填值，2026-09-05 定型详见下）；CaptureTab.vue + FilterGroupCard.vue）/ 域名组（DomainsTab.vue：内置组+自定义组列表、本地导入、URL 导入、导出、删除）；抽屉宽 560→640，tab 标签宽 92px，内容区独立滚动；系统代理仍是实时控制不随表单保存。**组卡片添加入口（2026-09-05 定型）**：先选维度（n-select 下拉：域名/路径/进程）再填值，值控件按维度切换——域名维度是 tag-select 二合一（下拉=引用域名组 @id，也可手输域名回车）、路径维度 n-input+添加按钮、进程维度 tag-select（下拉=**系统全部运行进程** ListSystemProcesses binding 合并已捕获流量 ProcessName，可手输；internal/procs 包 Toolhelp32 快照枚举，小写去重排序、排除 PID 0/4）；域名组引用只在"域名"维度出现，切到"进程"时无此入口（旧实现域名手输与引用域名组分两个控件、且类型切换有独立"引用域名组"附加行，已合并进域名维度的二合一控件）。

- **Computer Use MCP 修订（补充/修正上文 M3 要点）**：主树桥接后 WebView 内按钮/输入框在 a11y 树有**独立 id，click id 直接命中**（比 element_id="1"+坐标可靠）；**set_value 是输入首选**（n-input / n-input-number 均适用，触发 Vue v-model 响应式同步）；**每个 MCP 动作（含 set_value/set_focus）消耗 UI tree revision，下一动作前必须重新 get_app_state**，否则报 "No cached UI revision"；**n-select 下拉浮层在窗口失焦时自动收起**——get_app_state 提示焦点窗口是别的进程时，先 `perform_action set_focus` 把 PrismProxy 提前台再 click；`perform_action expand` 对 WebView n-select **不支持**（无 ExpandCollapse pattern），fallback 用 set_focus+click；`diffMode="no-change"` 返回时树 id 不变可直接复用；流量列表大时 a11y 树被撑爆截断——先 click "清空" 再取树。
- **Trae CN.exe 自家网络栈不尊重 ProxyOverride**（zijieapi.com 在绕过列表仍被抓到）——绕过发生在 WinINET/Chromium 客户端侧，属预期行为非 bug。
- **崩溃自愈链路实测通过**：startup SelfHeal（备份存在=上次强杀→还原→删备份）→ OnShutdown Disable（干净退出删备份）；备份 `%APPDATA%\PrismProxy\proxy-backup.json`。强杀 PID→重启自动还原注册表，干净退出（window_close）注册表精确回基线且无备份残留。
- **设置持久化**：**exe 同级 `config/settings.json`（2026-09-04 起便携模式，`settings.DefaultConfigDir()`；旧 %APPDATA%\PrismProxy 配置一次性自动搬迁不删旧文件）**；字段 listenAddr/upstreamMode/upstreamProxy/maxFlows/maxBodyMB/bypassList/filterGroups/decryptRules（**M4.6 起 captureRules/processRules 仅作迁移源，迁移后清空**），GUI 保存即落盘；字节预算 256MB（maxBodyMB）M4 已实现。注意 wails dev 下 exe 在 build/bin，配置落在 build/bin/config。
- **注册表基线（M4 验收参照）**：ProxyEnable=0、ProxyServer=127.0.0.1:9090、ProxyOverride 22 项（`<-loopback>` + 19 个 trae/字节系裸域 + api.trae.cn/mcs.zijieapi.com/www.trae.cn）。接管=合并去重、恢复=精确还原备份，均已实测。

## M4.5 关键认知（2026-09-04，MVP 打磨）

- **顶栏快捷开关（2026-09-04 新增）**：App.vue 顶栏（模式 tag 后）有「监听」「系统代理」两个 n-switch，直连 `StartProxy('')`/`StopProxy`/`SetSystemProxy`。关键语义：停止监听前若系统代理已接管须先 `SetSystemProxy(false)` 恢复注册表（防"代理停了注册表残留"断网）；后端 `StartProxy` 成功后清除 `startErr`（手动重启后错误条不再复活）；开关动作失败复用顶部 n-alert 错误条（模板不再硬编码前缀，前缀在各 set 点拼接）；`refreshStatus` 并行拉 `GetProxyStatus`+`GetSystemProxyStatus`，设置面板 `@changed` 也会同步两开关。同日新增可见性配置：`Settings.showSysProxySwitch`（默认 true；Load 以 Default 兜底故旧 settings.json 缺字段=显示），设置-常规-「工具栏」n-checkbox 控制顶栏系统代理开关显隐（走表单保存流，GeneralTab 现收 `:form` prop）；App.vue `loadPrefs` 在 onMounted 与 `@changed` 时拉取，监听开关恒显。另：SettingsPanel 保存错误改用全局悬浮 message（App.vue 套了 `n-message-provider`，面板内 `useMessage().error(..., {closable, duration:6000})`）；warnings 提示保留在固定 footer（按钮上方，可关闭）——嵌入滚动区底部时需滚动才能看到。
- **开机自动接管系统代理（2026-09-05 新增）**：`Settings.autoSysProxy`（bool，**默认 false**，零值兜底旧配置=不自动）；设置-常规-系统代理区 n-checkbox「启动程序时，自动开启系统代理」，走表单保存流（GeneralTab `form.autoSysProxy`）。后端 `startup`：StartProxy 成功后若 `cfg.AutoSysProxy` 为 true 则调 `SetSystemProxy(true)`（内部复用 Enable+备份+合并绕过语义）；**代理启动失败（端口占用）则跳过自动接管**（避免无监听却改注册表导致断网）；接管失败仅 LogError 不弹错误条、不阻塞启动。shutdown 恢复注册表逻辑不变（已接管则还原备份），故自动接管的退出清理与手动接管完全同链路。
- **常规页「生效中绕过列表」展示（2026-09-05 优化）**：GeneralTab 系统代理接管时，注册表 ProxyOverride 原文是分号分隔单行串，不再整段小字堆叠——`overrideItems` computed 按 `;` 拆分 trim/过滤空项，用 n-tag（small/bordered=false/monospace）flex wrap 标签云展示，标题带项数，容器 `.override-tags` max-height 96px 溢出滚动；`.override-box` 灰底圆角卡片。
- **顶栏 GitHub 入口（2026-09-04 新增）**：App.vue 顶栏「清空」按钮后有 GitHub 图标 n-button（内联官方 octocat SVG 16px、`fill="currentColor"`，无 @vicons 依赖）。点击 `openGithub()`：桌面端直接调 `window.runtime.BrowserOpenURL`（系统默认浏览器=应用外新窗口；不走 wailsjs/runtime 包装以便判空降级），纯浏览器预览环境 `windowRuntime` 不存在则降级 `window.open(url,'_blank','noopener,noreferrer')` 新标签页。URL 常量 `GITHUB_URL` 用无 `.git` 后缀的仓库页地址。
- **WebView2 运行时自检**（`webview2_windows.go`，其他平台 `_other.go` 空实现）：wails v2.15 对运行时缺失只返回裸错误无引导；启动前查注册表 `{F3017226-FE2A-4295-8BDF-00C3A9A7E4C5}` 的 `pv`（HKLM WOW6432Node + HKLM 原生 + HKCU 三处），缺失则 MessageBoxW 引导 + rundll32 打开 evergreen bootstrapper。
- **端口占用友好报错**：`app.go` StartError 字段 + listen 错误 wrap（提示端口占用/换端口）+ startup 失败发 `proxy:start-error` 事件；前端 App.vue 顶部 n-alert 错误条 + "打开设置"按钮。
- **LDPlayer14 = Android 14（SDK 34）**：自带 `/system/bin/curl`（8.0.1，shell 内 curl **不读全局代理**，需显式 `-x`）；自带 AOSP 浏览器 `com.android.browser`（WebView 146，**信任用户 CA**——端到端验证 HTTPS 的最简路径）。
- **Android 14 root 直写用户 CA（免系统"安装证书"UI）**：PEM 写到 `/data/misc/user/0/cacerts-added/<subject_hash_old>.0`，hash 用 Git 自带 `C:\Program Files\Git\usr\bin\openssl.exe x509 -noout -subject_hash_old` 计算；权限 644 root:root + `restorecon`；**需重启模拟器生效**；adbd 重启后从 root 降回 shell(2000)，用 `su -c '...'` 操作。**当前模拟器内留有本工具用户 CA `9e85ecda.0`**（2026-09-04 装入，后续冒烟直接可用；删除：`su -c 'rm /data/misc/user/0/cacerts-added/9e85ecda.0'` + 重启）。
- **MCP 参数修订**：`get_app_state` 参数名是 `pid/app/windowId/disableDiff/max_depths`（参数名错报 "pid is not running"；进程已退出也报同错，先 Get-Process 确认）；截图默认目标可能是"新通知"小窗，**须显式 `windowId` 指定主窗口**（雷电主窗口标题"雷电模拟器"）；`set_value(pid, element_id, value)`；UI revision 前进后操作旧元素报 "Unknown element_index"，须重新取树。

## M5 关键认知（GUI/验收实测，2026-09-04）

- **组内 AND 对"快捷忽略"这类自动维规则是陷阱**：向同一黑名单组混写 hosts + processes 时，两类条目互为约束导致彼此收窄（host 命中但 proc 不命中→不屏蔽）。凡"不同触发入口、各管一维、期望独立生效"的自动规则，必须拆成单维组走组间 OR。M5 快捷忽略据此从单组 `_quick_ignore` 修订为 `_quick_ignore_hosts`/`_quick_ignore_procs` 双组，旧组由 settings.Migrate 启动拆分（迁移逻辑要幂等可重入，拆分后旧组删除）。
- **规则导入导出链路（已验收）**：`ExportRules(embedGroups)` 锁内拷贝后走 Wails 原生 SaveFileDialog（取消返回空串），JSON 字段 version/exportedAt/filterGroups/decryptRules/bypassList/groups（groups 无用户域名组时 omitempty）；`ImportRules(src)`：src="" 弹 OpenFileDialog、`http(s)://` 前缀走 httpGet、否则按本地路径；version 必须为 1；nil 切片兜底空；内嵌 groups 自动 domains.WriteUser 补建；**整体替换** filterGroups/decryptRules（bypassList 非 nil 才替换，防旧文件缺字段清空绕过）；Validate 的 err 阻塞、warns（未知 @引用）非阻塞弹窗；落盘后 SetLimits→rebuildEngine→监听/上游变化才重启代理。前端导入前 useDialog 强确认（closable/maskClosable=false，Promise<boolean>），成功后 emit('imported') 让 SettingsPanel 重新 GetSettings 刷新表单。
- **httpGet 用默认 http.Client（`&http.Client{Timeout:20s}`）不读系统代理注册表**——所以本机起 HTTP 服务做 URL 导入测试源时，即使 PrismProxy 自己是系统代理也不会回环，127.0.0.1 直连可达；PowerShell `System.Net.HttpListener`（`http://127.0.0.1:9181/` + `AddDirectoryPrefix`/`http://+:port/` ACL 免管理员用 localhost 前缀）后台进程托管测试 JSON 是验收 URL 导入的最简法。
- **原生文件对话框（保存/打开）是同 pid 的独立 window，有独立 windowId 与 id 空间**：set_value 可直接给文件名 edit 填**完整绝对路径**（不必浏览目录），重取树后点"打开(O)/保存(S)"。**对话框完成动作后窗口即销毁——对已关闭对话框再 click 报 "No window found for pid" 不是失败，而是动作已生效的信号**：不要重试，直接用 PowerShell（Test-Path / 读 settings.json / 主窗口树）核验结果。
- **每个 MCP 动作（含 RunCommand 等外部扰动后）都消耗 UI revision，元素 id 树重建后整体重排**（如 URL 导入按钮 185→177→159）；动作链中每步都要用新鲜树的 id，不能跨步骤缓存。
- **进程归因区分验证法**：制造流量时用 `curl.exe`（归因 curl.exe）与 PowerShell `Invoke-WebRequest`（归因 powershell.exe）分别触发，可在同机得到两个不同进程名，用于验证"忽略此进程"只屏蔽目标进程不误伤另一者。
- **复制类验收硬证据仍是 Get-Clipboard**；cURL 验收必须覆盖"含引号 header / JSON body / 多参数多行"的真实形态——简单请求（无特殊字符）测通≠可粘贴执行：cmd 版 `\"` 转义与 `^` 续行在 PowerShell 全部失效（2026-09-05 已修复为三 shell：cmd / powershell / bash，见 V1.x 关键决策第 2 条）。

## Windows 证书信任链（M2 实测关键认知）
- **Root CA 位置**：`%APPDATA%\PrismProxy\ca\prism-ca.pem`（10 年，已装入**当前用户**受信根存储，指纹 sha1=1953B19B...）。
- **certutil 免管理员**：`certutil -addstore -user -f Root <cer>` 装当前用户根存储，免管理员但弹"安全警告"对话框需人工点"是"（可用 Computer Use MCP 辅助）；全机版 `-addstore -f Root` 需管理员。
- **curl-schannel 强查吊销（大坑）**：Windows 版 curl（schannel 后端）**默认强制吊销检查**。对无 CRL/CDP 的私有 CA：根未装存储时报 `CERT_TRUST_REVOCATION_STATUS_UNKNOWN`（exit 60）；根装好后吊销检查仍失败 → TLS1.3 服务端报 `bad record MAC`、TLS1.2 直接 EOF。**解法：curl 加 `--ssl-no-revoke`**。浏览器（Edge/Chrome）与 .NET 等系统默认栈**不硬查**，零 flag 直接通——与 Charles/Fiddler 的 CA 无 CRL 同理。
- **openssl s_client 是 schannel 问题的最佳分诊工具**：`openssl s_client -proxy 127.0.0.1:9090 -connect <host>:443 -servername <host> -brief`（本机 `D:\soft\anaconda3\Library\bin\openssl.exe`；Git for Windows 精简版无 curl 但有 openssl.exe）。OpenSSL/Go 客户端全部兼容我们的 tls.Server，唯 schannel 特殊。
- **Edge 实测**：CA 装当前用户存储即被 Chromium 信任；系统代理切到 9090 后桌面全量 HTTPS（msn/aliyun/Trae 自身 API）零 bypass 全解密。

## 模拟器网络架构（关键认知，已实测）

- 雷电 9（LDPlayer14）= VirtualBox 架构，guest 网段 `172.16.1.0/24`（wlan0=172.16.1.4），**NAT 由独立进程 `VBoxNetNAT.exe` 完成**——模拟器内 App 流量的进程归因 = `VBoxNetNAT.exe`（不是 dnplayer.exe/Ld9BoxHeadless.exe）。多开时多个实例可能共享同一归因，粒度受限。
- `172.16.1.2` = VBox NAT 的**宿主机回环别名**（ping TTL=128 即 Windows）。模拟器配代理用它最可靠；`.1` 是虚拟路由器不可用于代理。
- **VBox NAT 把 172.16.1.2 流量转发到宿主回环（M4.5 实测）**：代理绑 `127.0.0.1:9090` 即可被模拟器经 `172.16.1.2:9090` 访问，**无需改绑 0.0.0.0**；宿主侧连接来源进程 = VBoxNetNAT.exe（归因验证点）。
- 雷电会把 Windows 系统代理同步到 Android `global http_proxy`（实测同步值 `127.0.0.1:9999`），但**不同步我们的 9090 修改**（触发时机不明，疑似仅启动时）。且 VBox NAT 不感知系统代理——**路径 A（仅设系统代理）对雷电无效，必须 Android 内设代理**（已实测验证）。
- 雷电自带 adb：`D:\leidian\LDPlayer14\adb.exe`，设备 `emulator-5554`（另有真机 PBIBWCL7OBL7NFM7，勿误操作）。可控命令：`settings put global http_proxy 172.16.1.2:9090` / 清除 `:0` / `am start -a android.intent.action.VIEW -d <url>`。
- CONNECT 隧道 flow 日志在**连接关闭时**才打印（M1 预期行为，实测 Edge 长连接延迟 1 分钟+）；明文 HTTP 即时。日志仅在启动终端可见。
- **系统代理**：`127.0.0.1:9999` 是**另一款抓包软件**的端口（不是梯子，用户 2026-09-03 更正），后续**不要再启用/恢复它**；旧备份 `sysproxy-backup.json`（9999）已作废。开发环境直接把系统代理指向本项目 `127.0.0.1:9090` 即可。

## 域名组配置（domains/，M4 规则数据源）

- **源码保留、不打包（2026-09-04 定稿）**：`PrismProxy/domains/` 目录下保留全部 22 个域名组 txt + 完整 `index.json`（含国内互联网/手机厂商/海外平台/云计算/金融支付/AI开发工具/游戏等分类），**提交到 git 作为公开发布源**，供所有用户通过 URL 导入（推荐 jsDelivr CDN 镜像）。**程序不再 `//go:embed domains`**（已移除 main.go 的 embed 指令与 domainsFS），exe 内不含任何默认域名组，用户首次启动列表为空，需自行导入。
- 位置：[`PrismProxy/domains/`](./domains/)，源码资源目录（git 分发用，非运行时加载）；用户导入组见下节。
- **索引 [`domains/index.json`](./domains/index.json)**：登记每组元数据（id/file/中文名/category/tags/related/note）+ 文件格式与匹配语义说明；**域名清单以各 txt 为唯一事实源，index.json 不重复域名**（防双写漂移）。新增/删除 txt 时必须同步该索引。
- 用途：M4 规则引擎（捕获/解密/进程三层）与系统代理 ProxyOverride 绕过列表的共享数据源；抓包/绕过某家业务时整组引用，避免散落硬编码。
- 格式：每行一个域名；`#` 开头注释/空行忽略；裸域名匹配自身+全部子域，`*.x.com` 通配等价裸域名（语义同方案 §4.7）。
- **注意**：Go 侧加载/解析已于 M4 落地（internal/domains）；当前程序侧用 `domains.LoadUser(userDir)` 只加载用户目录，`Load`/`LoadMerged` 保留但运行时不再调用（测试仍可用）。

### 用户域名组管理（2026-09-04 新增，M4）

- **存储位置**：用户导入组落盘 **exe 相对路径** `config/domains/<id>.txt`（`settings.DefaultConfigDir()` = `<exe目录>/config`，wails dev 下即 `build/bin/config/domains/`）。仓库内 `domains/` 目录是源码分发资源（git 公开，不 embed 进 exe），**不要往里写测试文件**。
- **加载语义**：程序侧只用 `domains.LoadUser(userDir)` 加载用户目录，所有组均标记 Custom=true（列表中全部带"自定义"tag，可编辑/删除）。覆盖导入同 id 组即覆盖旧文件；删除即移除。
- **URL 导入双格式**：后端 `ProbeURLImport(url)` 探测——GET 后尝试 json.Unmarshal 为含 `groups` 数组的结构，成功即 `kind=index`，否则 `kind=txt`（直接域名组文件）。索引条目 `file` 字段相对索引 URL 解析（`base.ResolveReference`）。推荐用户用 jsDelivr 镜像导入本仓库 `domains/index.json`。
- **索引勾选下载**：`ImportDomainGroupsFromIndex(url, ids)` 重拉索引 → 逐项解析 file → httpGet → `domains.WriteUser` 落盘；每项返回 `IndexImportResult{ID,Count,Err}`，最后统一 reloadGroups 一次。前端 DomainsTab.vue 探测到 index 时弹 n-modal 勾选框（全选/清空/已选计数/批量导入汇总提示）。
- **新增 bindings（app.go）**：`ListDomainGroups`、`ImportDomainGroupFile`、`ImportDomainGroupURL`、`ExportDomainGroup`、`DeleteDomainGroup`、`ProbeURLImport`、`ImportDomainGroupsFromIndex`、`GetDomainGroupText`、`SaveDomainGroupText`；新 model：`DomainIndexEntry{ID,File,Name,Category}`、`URLImportProbe{Kind,Entries}`、`IndexImportResult{ID,Count,Err}`。
- **编辑功能（2026-09-04 新增）**：每个组行都有「编辑」按钮；点击弹 n-modal + monospace textarea 加载原始文本（含注释），保存调 `SaveDomainGroupText(id, content)` 复用 `importDomains` 落盘+热更新；空内容/无有效域名被 `domains.WriteUser` 拒绝（解析后 0 条报错）。
- **批量导入进度条 + 索引列表滚动（2026-09-04 新增）**：后端 `ImportDomainGroupsFromIndex` 循环内每处理一个组前后用 `runtime.EventsEmit(a.ctx, "index-import-progress", {current,total,id,done})` 发射进度事件（helper `emitImportProgress`）；前端 `DomainsTab.vue` 用 `EventsOn`/`EventsOff`（wailsjs/runtime）监听更新 `n-progress` 百分比 + 当前下载组 id，导入结束/异常在 finally 里 EventsOff 防泄漏。索引勾选列表包 `.idx-list {max-height:50vh; overflow-y:auto}` 滚动容器，文件多时不再撑开对话框。
- **批量导入同名组冲突处理（2026-09-04 新增）**：索引勾选弹窗中本地已存在的组（`existingIDs` computed，源自 `ListDomainGroupDetails` 的 id 集合）行内显示橙色「已存在」n-tag；点「导入所选」时若勾选里含冲突组，弹 `useDialog` warning 一次性选择（positiveText=全部覆盖/negativeText=跳过已存在，closable+maskClosable=false，返回 Promise<boolean|null>，null=中止导入）。Binding 签名改为 `ImportDomainGroupsFromIndex(url, ids, overwrite bool)`：`overwrite=false` 时在下载前 `os.Stat(config/domains/<id>.txt)` 命中即置 `IndexImportResult.Skipped=true` 跳过（不下载不写盘）；前端结果分 ok/skipped/fail 三类提示（skipped 用 message.info）。冲突检测纯前端复用列表数据，未新增后端查询接口。**App.vue 已在 n-message-provider 内套 n-dialog-provider（useDialog 必需，缺失会抛 "useDialog/warning of null"）**。
- **网络限制**：仅 http/https，20s 超时，4MB 上限（`io.LimitReader`），2xx 校验。
- **txt 标准头部标题解析（2026-09-04 新增）**：各域名组 txt 头部标准格式 `# 域名组：<显示名>`（参见 domains/alipay.txt L1-12 头注释块；仓库 22 个组均已带此头）。后端 `domains.ParseTitle(data)` 扫描首个非空行起的注释块提取标题（首个非注释行前未命中=无标准头返回空串）；`Groups.Titles map[id]title` 在 Load/LoadMerged/LoadUser 中统一填充（LoadMerged 覆盖内置组时无标题则 delete 防残留）。**显示名优先级：txt 头部标题 > index.json name > 组 id**（app.go `groupDisplayName`）。`ListDomainGroups` 返回新增 `titles` map；`ListDomainGroupDetails` 的 `DomainGroupInfo.Name` 直接用该优先级。前端：DomainsTab.vue 组行改为「加粗显示名 + @id tag + 自定义/内置 tag + 分类 tag + N 个域名」（空列表有 n-empty），CaptureTab.vue @引用下拉 label 同优先级（标题>index name>id，带 category 后缀）。wails dev 会重新生成 wailsjs 类型；`ListDomainGroups` 返回 map 故前端按 `res.titles as Record<string,string>` 读取无需改 models.ts。

## 环境备忘

- 开发机：Windows，用户 ZhangJiaLei，时区 Asia/Shanghai。
- **GitHub 域名可达性差异（2026-09-04 实测）**：`github.com` 解析/访问正常（20.205.243.166，仓库页 302 跳转即时）；**`raw.githubusercontent.com` 被 DNS 污染**（解析到 `0.0.0.0`/`::`，curl exit 6）——`/raw/` 链接虽 302 但跟随即失败。**可用直链替代：jsDelivr CDN 镜像** `https://cdn.jsdelivr.net/gh/<user>/<repo>@<branch>/<path>`（实测 200、2.5s、SHA256 与本地一致），另有 `https://fastly.jsdelivr.net/...`、`https://gcore.jsdelivr.net/...` 备用；镜像有缓存延迟（~数小时），需最新 commit 时走上游代理（梯子）。**M5 规则 URL 导入的默认示例/文档链接优先用 jsDelivr 而非 raw.githubusercontent.com**。
- **本机直连 GitHub 间歇性不稳定（2026-09-04 实测）**：同一时段 curl 直连有时 15s 超时/RST(exit 7)、有时 200（1-8s）。**经 PrismProxy(9090, upstreamMode=direct) 的失败模式与直连完全同构**（间歇 000@2s/超时，成功时 200 慢），证明代理链路本身无问题、根因在出口网络。Trae 浏览器接管实测：baidu 秒开、github 仓库页 ~30s 打开成功。访问海外站点稳定解法：设置里上游代理改**手动**模式填梯子地址（浏览器→9090→梯子）。**`upstreamMode=system` 在接管后无效**：UpstreamFromSystem 读当前注册表，此时 ProxyServer 已是本工具自己，防环路返回空=直连。
- **Go 1.27.0**：2026-09-03 经 `winget install GoLang.Go --source winget` 安装，位于 `C:\Program Files\Go\bin`。安装前已开着的终端会话 PATH 不生效，需重开终端或手动 `$env:Path` 前缀。
- **杀软短暂锁定**：首次 `go test` 对新生成的 test exe 报 `Access is denied`（Defender 扫描竞争），重跑即过；方案风险表已含杀软/SmartScreen 误报项。
- M1 已实测：curl 明文 200 / CONNECT 隧道 200 / 上游错误 502，日志正确归因 `curl.exe(pid=...)`。
- **PowerShell 编码教训**：绝不用 `Set-Content`/`-replace | Out-File` 重写含中文的源码文件（默认 GBK 输出毁掉 UTF-8 中文注释，gofmt 报 illegal UTF-8）。改源码一律用 Write/SearchReplace 工具。
