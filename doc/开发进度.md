# PrismProxy 开发进度

> 里程碑与任务级进度跟踪。关键技术决策见 [项目记忆.md](./项目记忆.md)，开发依据见 [方案.md](./方案.md)。

## 里程碑总览

| 里程碑 | 内容 | 状态 |
| --- | --- | --- |
| M1 | 代理引擎骨架：明文 HTTP 代理 + CONNECT 透传 + 错误记录（命令行/日志验证） | 已完成（2026-09-03） |
| M2 | MITM 解密（Root CA + 动态签证书，**关键路径**） | 已完成（2026-09-03） |
| M3 | UI 流量面板（Wails + Vue3） | 已完成（2026-09-03） |
| M4 | 过滤/设置/打包 → MVP | 已完成（2026-09-04） |

## M1 详细进度

**硬性退出条件**：雷电/MuMu 模拟器流量进程归因实测通过。

**验收标准**（方案 §六 #1/#2）：
- #1 明文 HTTP：curl/浏览器经代理访问 http 站点，日志记录完整请求/响应
- #2 CONNECT 透传：https 站点隧道建立，字节双向转发正常

### 任务分解

| # | 任务 | 状态 | 备注 |
| --- | --- | --- | --- |
| 1 | 环境检查 + 创建项目记忆/开发进度文档 | 完成 | Go 1.27.0 经 winget 安装（`C:\Program Files\Go\bin`） |
| 2 | Go 骨架与 proxy 引擎（main.go / server / tunnel） | 完成 | 零外部依赖，-addr 默认 127.0.0.1:9090 |
| 3 | capture/store：Flow 记录与日志输出 | 完成 | Flow ID 临时用「时间戳毫秒-序号」，tee 捕获 2MB 截断 |
| 4 | rules：端口→PID 映射（IPv4/IPv6 双栈 + 降级） | 完成 | 纯 syscall LazyDLL 调 iphlpapi.dll，Toolhelp 快照降级 |
| 5 | 构建 + 集成测试 + curl 实测 | 完成 | 3 项集成测试通过；curl 明文 200 / CONNECT 200 / 上游错误 502，日志正确归因 curl.exe |
| 6 | 模拟器进程归因实测（雷电/MuMu） | ✅ | 雷电 9 实测通过：Android 内 adb 设 `http_proxy=172.16.1.2:9090`，百度全系域名捕获，归因 `VBoxNetNAT.exe(pid=44660)`。注意：仅设 Windows 系统代理对雷电无效（VBox NAT 不感知），详见项目记忆 |

> 里程碑状态：**M1 已完成**（2026-09-03，含模拟器归因实测）→ 下一步 M2

### M1 实测记录（2026-09-03）

```text
flow ... [curl.exe(pid=19260)] GET http://example.com/ -> 200 state=done down=559B
flow ... [curl.exe(pid=59528)] CONNECT https://example.com:443 state=done up=662B down=5745B
flow ... [curl.exe(pid=65272)] GET http://127.0.0.1:1/dead state=error err="dial tcp ... refused"
```

已知现象：首次 `go test` 遇到杀软对新生成 test exe 的短暂锁定（Access is denied），重跑即过。

### 代码结构（M1 已落地）

```text
PrismProxy/
├─ go.mod                    # module prismproxy，零外部依赖
├─ main.go                   # CLI 入口，flag -addr 默认 127.0.0.1:9090
├─ prismproxy.exe            # 已构建，可直接运行
└─ internal/
   ├─ proxy/
   │  ├─ server.go           # http.Server + ConnContext，分流 CONNECT/明文
   │  ├─ tunnel.go           # CONNECT Hijack 双向 io.Copy + 字节计数
   │  └─ proxy_test.go       # 3 项集成测试（明文/隧道/错误流 + 进程归因断言）
   ├─ capture/
   │  ├─ flow.go             # Flow/Message/ProcessInfo/Timing + BodyCapture(tee)
   │  └─ recorder.go         # ID 分配、落库、日志
   ├─ store/
   │  └─ store.go            # 内存环形缓冲
   └─ rules/
      └─ pidmap_windows.go   # GetExtendedTcpTable 双栈 + 进程路径解析
```

## M2 详细进度

**验收标准**（方案 §六 #3-#5）：客户端信任本 CA 后，HTTPS 请求/响应明文完整捕获；curl 与浏览器全链路验证。

### 任务分解

| # | 任务 | 状态 | 备注 |
| --- | --- | --- | --- |
| 1 | mitm 包：Root CA 生成/持久化 | 完成 | `internal/mitm/ca.go` LoadOrCreateCA，RSA-2048/10 年，落盘 `%APPDATA%\PrismProxy\ca\prism-ca.pem`，私钥 ACL 限当前用户（`acl_windows.go`） |
| 2 | mitm 包：叶子证书动态签发 | 完成 | `internal/mitm/cert.go` CertCache：ECDSA P-256 + SAN + LRU + singleflight |
| 3 | CONNECT MITM 拦截 | 完成 | `internal/proxy/mitm.go`：hijack→bufferedConn（读 hijack 缓冲）→tls.Server（仅 http/1.1 ALPN）→ReadRequest 循环；100-continue 立即回写；Upgrade bypass；握手失败入 bypass set 降级盲透传 |
| 4 | 上游 TLS | 完成 | 共享 Transport 自定义 TLSClientConfig + 显式 ForceAttemptHTTP2 + InsecureSkipVerify；双向 TLS 版本/上游证书链入 Flow.TLS |
| 5 | /ca 证书下载页 | 完成 | `internal/proxy/capage.go`；Windows 段给 `certutil -addstore -user -f Root`（免管理员）+ curl `--ssl-no-revoke` 提示 |
| 6 | 测试 | 完成 | `mitm/ca_test.go` + `proxy/mitm_test.go` 全链路集成测试，全部通过 |
| 7 | 实测：curl + .NET + 浏览器装 CA 抓明文 | 完成 | 见下方实测记录；根因：curl-schannel 默认强查吊销，私有 CA 无 CRL 需 `--ssl-no-revoke`（详见项目记忆） |

> 里程碑状态：**M2 已完成**（2026-09-03）→ 下一步 M3 UI 流量面板

### M2 实测记录（2026-09-03）

```text
flow 1788442975905-1 [curl.exe(pid=63680)] GET https://www.163.com/ -> 403 state=done   # curl --ssl-no-revoke，明文捕获
flow ... [powershell.exe(pid=33840)] GET https://www.baidu.com/ -> 200                   # .NET 零 flag 直连
flow ... [curl.exe(pid=65968)] POST https://www.163.com/ -> 403 up=31B                   # POST body 捕获
```

- 系统代理切 9090 后桌面全量 HTTPS（msedge→msn.cn/aliyun、chrome.exe、Trae 自身 API）零 bypass 全解密。
- bypass 机制按预期工作：握手失败 host 入 bypass 后透传（注意会造成"成功"假象，排查须重启代理清 bypass）。
- 排查工具：OpenSSL s_client 走同一 CONNECT 路径成功 → 锁定 schannel 特异（完整根因链见项目记忆「Windows 证书信任链」）。
- CA 已装入当前用户受信根存储（sha1=1953B19B...）；实测后系统代理已恢复梯子 `127.0.0.1:9999`。

### 代码结构（M2 已落地）

```text
PrismProxy/
├─ main.go                    # -addr / -no-mitm flag + LoadOrCreateCA
└─ internal/
   ├─ mitm/
   │  ├─ ca.go                # Root CA 生成/持久化（RSA-2048/10 年）
   │  ├─ acl_windows.go       # 私钥 ACL 限当前用户
   │  ├─ cert.go              # 叶子签发：ECDSA P-256 + SAN + LRU + singleflight
   │  └─ ca_test.go
   ├─ proxy/
   │  ├─ server.go            # NewServer(addr, rec, ca)；roundTrip/streamResponse 复用件
   │  ├─ tunnel.go            # handleConnect 分流 bypass/MITM/透传
   │  ├─ mitm.go              # handleMITM：hijack→bufferedConn→tls.Server→请求循环
   │  ├─ capage.go            # /ca 证书下载与安装指引页
   │  ├─ mitm_test.go         # MITM 全链路集成测试
   │  └─ proxy_test.go        # M1 集成测试（保持通过）
   ├─ capture/                # Flow + TLSInfo（ClientVersion/ServerVersion/PeerCerts）
   ├─ store/
   └─ rules/
```

## 当前阻断

- 无。**待办（非阻断）**：M2 里程碑末真实场景冒烟（方案 §十）——雷电模拟器装 CA（adb push + 安装证书或 root 挂 system cacerts）+ 走系统代理的桌面应用 + CEF/Electron 桌面端抓包验证，需用户配合，可并入 M4 前冒烟。

## M3 详细进度

**验收标准**（方案 §六 #6/#7/#12）：
- #6 流量列表虚拟滚动 2000 条不卡
- #7 详情面板（Headers 表格 + Body 多视图）
- #12 流式响应与大文件不撑爆内存

### 任务分解

| # | 任务 | 状态 | 备注 |
| --- | --- | --- | --- |
| 1 | 环境：Wails CLI + wails doctor | 完成 | GOPROXY 走 goproxy.cn |
| 2 | store 事件订阅 + 50ms 合帧事件总线 | 完成 | `store.Subscribe` 锁外回调；`Add` 按 ID upsert（new/update/evict 三类事件）；app.go pendUp map 入队即转 DTO 值拷贝，`time.AfterFunc(50ms)` flush 发 `flow:upsert`/`flow:evict` |
| 3 | app.go Wails Bindings 控制面 | 完成 | StartProxy/StopProxy/ListFlows/GetFlowDetail/GetFlowBody/ClearFlows 等 |
| 4 | main.go：Wails 入口 + 保留 -headless | 完成 | |
| 5 | 前端脚手架 | 完成 | Vite + Vue3 + TS + Naive UI + @vueuse/core |
| 6 | flows store + FlowList 虚拟滚动 | 完成 | 事件订阅维护有序列表，选择中被 evict 自动回占位 |
| 7 | FlowDetail 详情面板 | 完成 | 概览/请求/响应/TLS 四个 tab；Headers 表格；Body 自动视图（JSON 树/文本/图片/Hex/表单）；进程信息含完整路径 |
| 8 | 展示层解压 | 完成 | `capture.DecodeBody`：gzip/deflate（标准库）+ br（andybalholm/brotli v1.2.3）+ zstd（klauspost/compress v1.20.0），GetFlowBody 调用时解压，右标「解压后大小 · encoding · content-type」 |
| 9 | wails build + 实测验收 | ✅ | 见下方验收记录 |

> 里程碑状态：**M3 已完成**（2026-09-03）→ 下一步 M4 过滤/设置/打包

### M3 验收记录（2026-09-03，GUI 实测，Computer Use MCP 操作）

- **#7 详情面板**：清空功能（462→19 条，evict 全链路正确）；图片视图（httpbin /image/png 猪图完整渲染）；Hex 视图（offset+hex+ascii 三列，PNG 魔数可辨）；gzip（221 B→284 B 解压 JSON 正确）；br（190 B→272 B 解压 JSON 正确）；压测后选中流详情仍正常渲染（状态/协议/耗时/上下行/进程路径全）。
- **#6 虚拟滚动**：15 路并行 1500 请求灌满至「2000 条流」（环形满、旧流 evict、被选流淘汰后详情自动回占位）；列表中段/底部（Ctrl+End）/顶部（Ctrl+Home）渲染均正常，滚动流畅。
- **#12 流式大文件**：16×34MB=544MB 吞吐（cdn.npmmirror.com node zip），进程 WS 140→219MB（+79MB=每流 2MB body 截断×流数，**符合设计**），静置无增长——亚线性有界。字节预算（256MB）随 M4 配置项引入，M3 内存上限=2000 流×2MB 兜底。

### 代码结构（M3 新增）

```text
PrismProxy/
├─ wails.json                 # Wails 工程配置
├─ app.go                     # Wails Bindings 控制面 + 50ms 合帧事件总线
├─ main.go                    # Wails 入口（-headless 保留命令行模式）
├─ build/bin/PrismProxy.exe   # 已构建 GUI 产物
├─ internal/
│  ├─ store/
│  │  ├─ store.go             # +Subscribe/Event/upsert/evict
│  │  └─ store_test.go        # 5 项测试（upsert/evict/clear/subscribe）
│  └─ capture/
│     └─ decode.go            # 展示层解压 gzip/deflate/br/zstd
└─ frontend/
   ├─ src/stores/flows.ts     # 事件订阅 + 有序列表状态
   ├─ src/App.vue             # 顶栏（状态/计数/清空）+ 左右分栏
   └─ src/components/
      ├─ FlowList.vue         # 虚拟滚动列表
      ├─ FlowDetail.vue       # 概览/请求/响应/TLS tabs
      ├─ BodyViewer.vue       # Body 多视图自动切换
      ├─ JsonTree.vue         # JSON 树
      └─ format.ts            # 字节/耗时格式化
```

## M4 详细进度

**验收标准**（方案 §六 #8-#11/#13）：
- #8 过滤与搜索：按域名/方法/状态码/关键字实时过滤，正则可选
- #9 设置与规则：端口/绑定地址/上游代理/解密名单/捕获规则/进程过滤可配并持久化
- #10 系统代理开关：一键接管/恢复，ProxyOverride 合并不覆盖，退出按原值恢复
- #11 打包分发：`wails build` 产出单 exe
- #13 崩溃自愈：接管后强杀进程，重启自动恢复原代理设置

### 任务分解

| # | 任务 | 状态 | 备注 |
| --- | --- | --- | --- |
| 1 | settings 模块：配置结构与持久化 | 完成 | `internal/settings`；`%APPDATA%/PrismProxy/settings.json`；监听地址/上游/存储上限/maxBodyMB/绕过列表/三层规则 |
| 2 | sysproxy 模块：接管/恢复 + 崩溃自愈 | 完成 | `internal/sysproxy`；注册表完整路径 + InternetSetOption 39/37；ProxyOverride 合并去重（内置+用户值，绝不覆盖）；备份 `proxy-backup.json` + 启动 SelfHeal |
| 3 | rules 引擎：捕获/解密/进程三层 + domains/ 加载 | 完成 | `internal/rules` + `internal/domains`；有序首条命中 + 默认动作；域名组整组引用 |
| 4 | proxy 集成：上游代理 + 规则接入 + store 字节预算 | 完成 | Transport ProxyURL；捕获规则判入 store、解密规则判 MITM/透传、进程规则判 include/exclude；maxBodyMB 256MB 预算落地 |
| 5 | app.go 控制面 bindings + main.go 接线 | 完成 | GetSettings/SaveSettings/SetSystemProxy/GetSystemProxyStatus/GetLocalAddrs/ListDomainGroups/InstallRootCA/AddDecryptBypass；启动 SelfHeal + OnShutdown Disable；新模块测试全过 |
| 6 | 前端：过滤搜索栏 | 完成 | 底部工具栏：关键字（Host+URL 子串）+ `.*` 正则开关（非法正则降级子串）+ 方法/状态下拉 + 匹配计数 `filtered/flows`；空态文案区分"无匹配/暂无流量" |
| 7 | 前端：设置页 | 完成 | `SettingsPanel.vue` NDrawer：系统代理开关（实时状态）+ 基本配置 + ProxyOverride 绕过（n-dynamic-tags）+ 解密/捕获/进程规则（n-dynamic-input）+ 域名组 + 根证书安装 |
| 8 | 测试 + wails build 打包验证 | ✅ | 单 exe 产出；GUI 实测全过，见下方验收记录 |

> 里程碑状态：**M4 已完成**（2026-09-04）→ **MVP 达成**

### M4 验收记录（2026-09-04，GUI 实测，Computer Use MCP 操作）

- **#8 过滤与搜索**：关键字 `baidu` → 1/4 命中；方法下拉选 POST → 仅 POST 302 一条（1/3）；状态下拉选 4xx → 仅 GET 404 一条（1/3）；正则模式 `baidu\.com/not-exist` → 精确命中 404 一条（1/3，含转义符证明非子串）；计数 `filtered/flows` 实时正确。
- **#9 设置持久化**：存储上限 2000→3000 保存 → `settings.json` `maxFlows:3000` 写入，全部字段（listenAddr/upstream/bypassList/三层规则）结构正确。
- **#10 系统代理**：接管 → ProxyEnable=1、ProxyServer=127.0.0.1:9090、ProxyOverride=基线 22 项+内置列表**合并去重**（`<-loopback>` 前缀保留），备份 json 落盘；桌面真实流量（WeChatAppEx/msedge/Trae CN/微信开发者工具）涌入且进程归因正确；关闭 → 注册表精确还原基线、备份删除。
- **#11 打包**：`wails build` 单 exe（build/bin/PrismProxy.exe）独立运行。
- **#13 崩溃自愈**：接管状态下 `Stop-Process -Force` 强杀 → 备份残留 + 孤儿代理复现 → 重启 exe → SelfHeal 自动还原基线 + 删备份。
- **干净退出**：off 状态关窗 → 进程退出、注册表维持基线、无备份残留（无副作用）。
- **附带发现**：Trae CN.exe 自家网络栈不尊重 ProxyOverride（zijieapi.com 在绕过列表仍被抓到）——预期行为（绕过发生在 WinINET/Chromium 客户端侧），非 bug。

## 更新日志

- 2026-09-04：**M4 完成 = MVP 达成**。过滤搜索（关键字/正则/方法/状态码 + 计数）、设置面板与持久化（settings.json）、系统代理接管/恢复（ProxyOverride 合并去重）、崩溃自愈（强杀→SelfHeal 还原）、body 字节预算 256MB、domains/ 域名组 Go 侧加载落地；`wails build` 单 exe 验证通过。GUI 验收 #8-#11/#13 全部通过（详见 M4 验收记录）。

- 2026-09-03：方案审计完成并修订方案.md；启动 M1，创建项目记忆与开发进度文档；winget 安装 Go 1.27.0；M1 代码落地，构建/vet/3 项集成测试通过，curl 实测通过（含 curl.exe 进程归因）；雷电模拟器归因实测通过（VBoxNetNAT.exe），M1 关闭。
- 2026-09-03：**M2 完成**。MITM 全链路落地（Root CA 持久化 + 叶子动态签发 + CONNECT MITM + 上游 h2 + /ca 分发页），单元/集成测试全通过；实测 curl `--ssl-no-revoke`、.NET、Edge/桌面全量 HTTPS 明文捕获零 bypass。根因排查：curl-schannel 默认强查吊销，私有 CA 无 CRL（TLS1.3 bad record MAC / TLS1.2 EOF），OpenSSL s_client 分诊定位；/ca 页已加提示，CA 装当前用户根存储（certutil -user 免管理员）。实测后系统代理已恢复 9999。
- 2026-09-03：**M3 完成**。Wails 壳 + 流量实时列表（虚拟滚动）+ 详情面板（Headers 表格、Body JSON树/文本/图片/Hex/表单、TLS、进程信息）落地；store 增加 upsert 事件订阅 + 50ms 合帧事件总线；展示层解压 gzip/deflate/br/zstd。验收 #6（1500 并发灌满 2000 条，全区域滚动渲染正常）/#7（图片/Hex/gzip/br/清空/选中详情全过）/#12（544MB 大文件吞吐 WS +79MB 有界）全通过。
- 2026-09-04：**UI 增强小版本**（用户临时需求，非里程碑条目）。① 列表新增时间列（`fmtTime` = HH:mm:ss.SSS）+ 全列点击表头排序（升/降/恢复默认三态）；② 详情面板概览"开始时间"改完整格式 `fmtDateTime` = YYYY-MM-DD HH:mm:ss.SSS（format.ts 新增），列表时间格式保持不变；③ 列表全部 8 个数据单元格支持**双击复制**（`@dblclick.stop` + `navigator.clipboard`，execCommand 降级）+ 底部居中 toast 视觉反馈（1.5s 淡出）。复制功能经用户真实双击验证（剪贴板精确等于单元格值）；详情完整时间多次截图确认。
- 2026-09-04：**排序触发机制改造**。移除表头整体点击排序（防误触），改为每列表头内嵌独立排序按钮：仅悬停表头时显现（opacity 0.15s 过渡），当前排序列常显；激活列显示 ▲/▼ 方向、非激活列显示 ↕；按钮固定于列右侧垂直居中（absolute + right:0），表头文字 padding-right:18px 预留空间不遮挡。GUI 实测：点击域名列按钮 → a11y 树 button 状态 ↕→▲、时间列回 ↕；用户悬停"大小"列时 ↕ 按预期显现（截图确认）。
- 2026-09-04：**表格列拖动调宽**（用户临时需求）。列表从固定 `grid-template-columns` 改为 `colWidths` ref 驱动（表头/数据行共用 `:style="gridStyle"`，grid 天然对齐）；每列表头右缘内嵌 6px 拖宽手柄（Pointer Events + window 级 move/up，拖动中 body 锁 `col-resize` 光标与选中）；每列最小宽 `colMins` + 上限 400px；路径列默认 1fr 弹性，被拖时按当前像素固化。配套调整：排序按钮从 absolute 改为 **inline-block 跟随文字**（`visibility:hidden` 隐藏防幽灵点击，手柄独占列右缘），`.th` padding-right 18→8px；状态列默认宽 48→56px（内联按钮占位致"状态"省略号）。验证：用户亲测拖拽成功（MCP drag 坐标受窗口移动干扰，最终以用户手动验证为准）。
- 2026-09-04：**底部工具栏预留**。App.vue 新增 `n-layout-footer`（28px，bordered，flex 横排 + gap:8px），内容区高度改 `calc(100% - 40px - 28px)`；当前空置，后续放过滤/设置等快捷操作（M4）。
